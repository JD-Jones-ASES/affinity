# SOURCES — the honesty register

Quote/data validation is a hard rule: every value or rule in this repo is machine-verified, sourced here,
or labeled as a model assumption (ADR-0003, ADR-0006). Empirical datasets are registered **before first
use**; ChemKernel reads them only from `data/`, and the validate-reference gate fails on any source id
that does not resolve to a row below.

## Verification methods (register method tags, and the badge each renders under)

These tags classify *register entries*; they map onto the three reader-facing badges (ADR-0003) as noted.
Model assumptions are **not** register entries — they live in each lesson's `assumptions[]` and render as
the **model-assumed** badge.

- **`chemkernel-derived`** — the value/relationship is machine-proven by the producer (conservation
  proofs, balanced coefficients, stoichiometric results, unit chains). Renders as **machine-checked**.
  No external source needed for the *math* — though its empirical inputs (atomic masses etc.) each carry
  their own source.
- **`standard-result`** — a textbook-universal relationship (e.g. $PV = nRT$, Henderson–Hasselbalch)
  whose form is source-invariant; verified structurally by the producer and linked to a derivation or a
  more primitive node where one exists. Renders as **machine-checked** when the producer proves it,
  otherwise **data/rule-sourced**.
- **`cited`** — a value, rule, or convention taken from a named source, with edition/version (or URL +
  access date), license/terms, and conditions of validity. Solubility rules, atomic weights, pKa/Ksp/E°
  tables, electronegativities, trend exceptions all live here. Renders as **data/rule-sourced**.

## Register

| Source id | Points to | Method/covers | Version & license | Notes |
|---|---|---|---|---|
| `ciaaw-2021-atomic-weights` | `data/elements.toml` atomic weights | `cited` | IUPAC/CIAAW abridged standard atomic weights, Atomic Weights 2021 report (table rev. 2024). Values are scientific facts (not copyrightable, Feist); © CIAAW 2007–2024 covers presentation only | accessed 2026-07-05; extended 2026-07-09 (ADR-0052) to every element with a CIAAW standard atomic weight — 84 of the 118 (each with its abridged `uncertainty`); the 34 with no standard atomic weight carry a `mass_number` from `iupac-periodic-table` instead · https://www.ciaaw.org/abridged-atomic-weights.htm |
| `iupac-periodic-table` | `data/elements.toml` group/period/block + the `mass_number` of no-standard-weight elements | `cited` | IUPAC periodic table (definitional element positions; and the bracketed mass number of the longest-lived isotope for elements with no standard atomic weight) | structural facts; used to place all 118 elements on the Valence Table (ADR-0052). f-block convention: La/Ac are group 3 (d-block); Ce–Lu, Th–Lr carry no group number. `mass_number` (Tc, Pm, Po, At, Rn, Fr, Ra, Ac, and every Z ≥ 93) is the IUPAC longest-lived-isotope bracket value — display provenance only, never arithmetic; accessed 2026-07-09 · https://iupac.org/what-we-do/periodic-table-of-elements/ |
| `openstax-chemistry-2e` | `data/ions.toml` ion charges; `data/solubility.toml` rules; `data/bonding.toml` ΔEN bond classes; `data/acids-bases.toml` acid/base identities; `data/decomposition.toml` gas-forming intermediates; `data/specific-heats.toml` specific heats; `data/formation-enthalpies.toml` standard enthalpies of formation; `data/vsepr.toml` VSEPR geometries; `data/ionization-constants.toml` acid ionization constants Kₐ + base ionization constants K_b + the water ion-product K_w + polyprotic staged Kₐ (H₃PO₄); `data/solubility-products.toml` solubility products Kₛₚ; `data/rate-constants.toml` rate constants k + reaction order (orders 0/1/2); `data/reduction-potentials.toml` standard reduction potentials E° | `cited` | OpenStax *Chemistry 2e*, CC BY 4.0 (ion charges; solubility rules from Table 4.1, §4.2; ΔEN bond classification from Fig 7.8, §7.2 — pure covalent < 0.4, polar covalent 0.4–1.8, ionic > 1.8, "a general guide with many exceptions"; reaction classification incl. acid-base neutralization and gas-forming reactions from §4.2; the oxidation-number assignment rule hierarchy (free element 0, monatomic ion = charge, F −1, group 1 +1 / group 2 +2, H +1, O −2, remainder fixed by sum-to-charge) from §4.1; specific heat capacities from Table 5.1, §5.1 (Energy Basics) — water 4.184 down to gold 0.129 J/g·°C; standard enthalpies of formation ΔH_f° from Appendix G — e.g. CH₄(g) −74.6, CO₂(g) −393.51, H₂O(l) −285.83 kJ/mol; VSEPR electron-domain geometries + ideal angles from §7.6 — 2 domains linear 180°, 3 trigonal planar 120°, 4 tetrahedral 109.5°, with lone-pair molecular-shape variants; the intermolecular-forces boiling-point trend (the CH₄ ≪ NH₃ ≪ H₂O hydride comparison) from §10.1, Fig 10.12 — the trend the `intermolecular-forces` concept teaches, while the precise boiling-point VALUES are sourced to `nist-webbook-boiling-points`; acid ionization constants Kₐ from Appendix H — e.g. acetic acid 1.8×10⁻⁵, formic 1.8×10⁻⁴, hypochlorous 2.9×10⁻⁸, hydrocyanic 4.9×10⁻¹⁰, and phosphoric acid's staged Kₐ₁ 7.5×10⁻³ / Kₐ₂ 6.2×10⁻⁸ / Kₐ₃ 4.2×10⁻¹³ (the `[polyprotic]` table); base ionization constants K_b from Appendix I — e.g. ammonia 1.8×10⁻⁵; the water ion-product K_w = 1.0×10⁻¹⁴ at 25 °C from §14.1; solubility products Kₛₚ from Appendix J — e.g. CaF₂ 4.0×10⁻¹¹, Mg(OH)₂ 8.9×10⁻¹²; rate constants k + reaction order from §12.4 (Integrated Rate Laws) — first order: the decomposition of hydrogen peroxide, k = 3.21×10⁻⁵ s⁻¹, Example 12.11, half-life ln2/k = 2.16×10⁴ s ≈ 6.0 h; second order: the dimerization of butadiene 2 C₄H₆ → C₈H₁₂, k = 5.76×10⁻² L mol⁻¹ min⁻¹; zero order: the decomposition of ammonia on a hot tungsten surface 2 NH₃ → N₂ + 3 H₂, k = 1.3×10⁻⁶ mol L⁻¹ s⁻¹; standard reduction potentials E° from Appendix L (Standard Electrode/Half-Cell Potentials) — e.g. Ag⁺/Ag +0.7996, Cu²⁺/Cu +0.337, 2H⁺/H₂ 0.00 (the SHE reference), Pb²⁺/Pb −0.1262, Ni²⁺/Ni −0.257, Fe²⁺/Fe −0.447, Zn²⁺/Zn −0.7618, Al³⁺/Al −1.662 V) | common ion charges + solubility rules + the bond-class guideline + the acid/base + gas-evolution reaction conventions + specific heat capacities + standard formation enthalpies + VSEPR geometry naming (universal teaching facts); ion + acid/base *composition* is separately machine-checked in `data.py`/`reactivity.py`; the ΔEN caveat ships as data with the rule; a specific heat and a ΔH_f° are rounded measured values under the data-sourced badge (an element in its standard state is ΔH_f° = 0 by definition); the VSEPR geometry *names* are the sourced convention while the electron-domain count that keys them is machine-derived from the verified Lewis structure (ADR-0044); a boiling point is a rounded measured value under the data-sourced badge — evidence for the IMF strength ordering, while the *presence* of each intermolecular force is machine-derived from the structure + polarity (the H-on-N/O/F test, ADR-0046); an acid ionization constant Kₐ (Appendix H), a base ionization constant K_b (Appendix I), the water ion-product K_w (§14.1), and a solubility product Kₛₚ (Appendix J) are rounded measured values under the data-sourced badge — the empirical input the equilibrium ICE ledger solves against, while the extent that satisfies mass action is machine-found + its residual re-checked (ADR-0048; for Kₛₚ the salt's ion composition, and for a weak base its proton accounting base + H⁺ = the conjugate-acid cation, are separately machine-checked in `data.py`); accessed 2026-07-05, Fig 7.8 verified 2026-07-08, §4.2 reaction classes + Table 5.1 specific heats + Appendix G formation enthalpies + §7.6 VSEPR geometries + §10.1 IMF boiling-point trend 2026-07-08, Appendix H ionization constants + Appendix J solubility products + Appendix I base ionization constants + §14.1 K_w 2026-07-08; a rate constant k of order 0/1/2 (§12.4) is a rounded measured value under the data-sourced badge — kept in its native units (the unit encodes the order + time base) — the empirical input a kinetics lesson's order-specific integrated rate law follows exactly, while the reaction *order* is a disclosed model (the order is measured, not read off the coefficients) and the reactant/equation composition is machine-checked in `data.py` + the balancer (ADR-0049); §12.4 kinetics (first-order 2026-07-08; second-/zero-order 2026-07-08); a standard reduction potential E° (Appendix L) is a rounded measured value under the data-sourced badge — the empirical input from which a cell's E°cell = E°(cathode) − E°(anode) and ΔG° = −nFE° follow exactly (model-exact); it is measured against the standard hydrogen electrode (defined as exactly 0 V, so only differences are physical), while the couple's composition (a cation, its neutral same-element reduced form, electrons = the ion charge) is machine-checked in `data.py` (ADR-0050); Appendix L reduction potentials 2026-07-09 |
| `bipm-si-2019` | `data/constants.toml` Avogadro constant + gas constant + Faraday constant | `cited` | BIPM *SI Brochure* 9th ed. (2019); 26th CGPM (2018) redefinition, in force 20 May 2019 | N_A = 6.02214076×10²³ mol⁻¹, **exact by definition**; and R = N_A·k_B = 8.314462618 J/(mol·K), exact in SI (both factors defined) — the L·atm/(mol·K) teaching value 0.0820573660809596 is that exact value unit-converted (1 L·atm = 101.325 J exactly), used by the ideal-gas formula-sheet entry (ADR-0039); the **Faraday constant** F = N_A·e = 96485.33212 C/mol is likewise **exact** (both factors defined: e = 1.602176634×10⁻¹⁹ C), the charge of one mole of electrons, used by the electrochemistry tier's ΔG° = −nFE° (ADR-0050); accessed 2026-07-05, Faraday added 2026-07-09 · https://www.bipm.org/en/publications/si-brochure |
| `nist-ionization-energies` | `data/elements.toml` first ionization energy | `cited` | NIST Atomic Spectra Database / *Ground Levels and Ionization Energies for the Neutral Atoms* — US-government data, **public domain**. Values (kJ/mol) are scientific facts (Feist) | first ionization energies; NIST reports in eV, converted to kJ/mol (1 eV = 96.485 kJ/mol); cross-checked against the `mendeleev` oracle within 2.5 kJ/mol. Extended 2026-07-09 (ADR-0052) from Z 1–20 + Fe/Cu/Zn to **Z 1–103** (103 elements — measured/recommended values through lawrencium); the transactinides (Z ≥ 104) are omitted, their ionization energies being theoretical estimates, not confidently measured. accessed 2026-07-05, extended 2026-07-09 · https://physics.nist.gov/PhysRefData/ASD/ionEnergy.html |
| `cordero-2008-covalent-radii` | `data/elements.toml` covalent radius | `cited` | Cordero, Gómez, Platero-Prats, Revés, Echeverría, Cremades, Barragán, Alvarez, "Covalent radii revisited," *Dalton Trans.* 2008, 2832–2838 (doi:10.1039/b801115j). Radii (pm) are scientific facts (Feist) | single-bond covalent radii, main-group Z ≤ 20 (the modern standard compilation); transition-metal radii are spin-state-dependent and deferred; cross-checked against the `mendeleev` oracle; accessed 2026-07-05 |
| `nist-webbook-boiling-points` | `data/boiling-points.toml` normal boiling / sublimation points | `cited` | NIST Chemistry WebBook, SRD 69 (US-government data, **public domain**); phase-change temperatures (°C at 1 atm) are scientific facts (Feist) | normal boiling points H₂O 100.0, NH₃ −33.3, CH₄ −161.5 °C and CO₂ sublimation −78.5 °C — the intermolecular-forces evidence; OpenStax §10.1 (Fig 10.12) teaches the trend but prints no precise values, so the values are sourced here while §10.1 remains the trend's teaching source (cited by the `intermolecular-forces` concept); accessed 2026-07-09 · https://webbook.nist.gov/chemistry/ |
| `allred-1961-electronegativity` | `data/elements.toml` electronegativity | `cited` | Allred, A. L., "Electronegativity values from thermochemical data," *J. Inorg. Nucl. Chem.* 1961, 17 (3–4), 215–221 (doi:10.1016/0022-1902(61)80142-5) — the **revised-Pauling** scale. Values (dimensionless) are scientific facts (Feist) | the two-decimal revised-Pauling electronegativities universally tabulated in the CRC Handbook and cross-checked against the `mendeleev` oracle (F 3.98, O 3.44, N 3.04, Cl 3.16, C 2.55, H 2.20, …); replaces the earlier OpenStax Fig 7.6 attribution, which prints the single-decimal *classic*-Pauling table (F 4.0) and so does not carry these values (the register previously flagged this internally by noting "the Allred-revised Pauling scale"); undefined for the noble gases (omitted, never 0). Extended 2026-07-09 (ADR-0052) to **71 elements** — every element where the Allred-revised value and the independent `mendeleev` cross-check concur within 0.06. OMITTED where they diverge > 0.06 (ten heavy metals — Tc, Lu, W, Pt, Au, Hg, Tl, Pb, Bi, U — whose Pauling EN is compilation-dependent), where mendeleev cannot confirm (Pm, Eu, Tb, Yb, Np), and for Z ≥ 95 + the noble gases (undefined): a gap is honest, a disputed value never shipped; accessed 2026-07-09 |

## Element-dataset decision (RESOLVED — ADR-0012)

Atomic weights come from **IUPAC/CIAAW** (abridged standard values, using the abridged single value rather
than the published interval), positions from the **IUPAC periodic table**, ion charges from **OpenStax
Chemistry 2e** (CC BY 4.0). All three registered above. Physical properties landed with the Valence-Table
flagship data session (ADR-0031, Phase 1 item 5a):

- ~~**NIST** — physical properties (ionization energies, radii, densities, electronegativity)~~ — landing:
  first **ionization energies** from NIST (`nist-ionization-energies`, public domain); **covalent radii** from
  Cordero et al. 2008 (`cordero-2008-covalent-radii`) — a cleaner single-source compilation than NIST for
  radii; **electronegativity** from the revised-Pauling scale (Allred 1961, `allred-1961-electronegativity` —
  originally mis-attributed to OpenStax Fig 7.6, which prints the classic single-decimal table; corrected
  2026-07-09). Densities and further NIST properties remain deferred until a lesson needs them.
- ~~The **solubility ruleset**~~ — done (ADR-0017): `data/solubility.toml` from OpenStax Table 4.1,
  registered above.

## Conventions for adding an entry

0. Test **oracles** (ADR-0026: `chempy`, `periodictable` as dev-dependencies cross-checking the engine) are
   *not* register entries — the register records shipped values; oracles verify and never supply one.
1. Register the source here (id, version, license, conditions) in the same session that lands the data.
2. Put the data under `data/` with the source id embedded in the file (provenance travels with the data).
3. Values used in lessons/Atlas entries reference the source id; the gate enforces resolution.
4. Exceptions and validity conditions (temperature, concentration regime) are recorded with the rule, not
   in prose only.
