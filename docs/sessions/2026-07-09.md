# Session 2026-07-09 — v1.0.0 QC sweep (Fable 5 planning session)

Owner: "Code/doc review session. Examine the current state, do quality control from a technical, chemical,
and human-navigability POV. Point us toward a solid v1.0.0." **Review only — nothing was fixed this
session.** This log is the work order for the fix sessions that follow. Method: baseline verification runs
plus a 15-dimension multi-agent review (140 agents; every high/medium finding adversarially verified by two
independent re-derivers where budget allowed — 37 findings confirmed, 25 flagged-unverified when the
verification budget ran out, of which the highest-stakes were then hand-verified, 60 lows recorded).

## Baseline (all verified this session)

- **derived/ is fresh and deterministic**: `npm run produce` reproduces the committed output byte-for-byte.
- **7 gates + `astro build` green**, 45 pages — matches the counters.
- **427/427 producer tests pass from the repo root.** From `producer/`, 5 fail (test_gym reaction-families):
  `gym.py:1137 _load_reactivity` hard-codes `Path.cwd()` and ignores its parameter (which carries the spec id,
  not a root — fixing needs a root threaded through `generate_gym`). cwd-as-default-root is producer-wide
  (`data.py:193`, `solubility.py:40`, `reactivity.py:44,100`, `build.py:663/714/749`) but only gym.py's is
  non-overridable.

## Work package A — chemistry corrections (learner-facing; do first)

- **A1 [HIGH]** `src/components/KineticsLesson.astro:187` + `problems/kinetics/hydrogen-peroxide-decomposition.kinetics.toml:33`
  (comment) + DECISIONS.md ADR-0049 (~line 1777): the first-order misconception refutation ends "a shrinking
  half-life would signal a **higher-order** reaction" — inverted. Shrinking successive t½ signals order **< 1**
  (the repo's own zero-order lesson); higher order (second) **grows**. Wrong chemistry shipped on a rendered page.
- **A2 [HIGH]** `reference/concepts/reaction-rate.toml:8` vs all three kinetics lessons: the Atlas concept defines
  rate = −(1/a)·d[A]/dt; the lessons (all a=2) use rate = −d[A]/dt with the sourced k. Cross-linked surfaces
  contradict by exactly 2×. No single page is numerically wrong (OpenStax §12.4 uses the per-species convention);
  fix = state the per-species convention in lessons/engine docstring and/or note k_A = a·k_rxn in the concept.
- **A3 [HIGH]** `data/solubility-products.toml`: both Kₛₚ values cited to OpenStax Appendix J, but Appendix J
  (fetched live this session) lists CaF₂ = 4.0×10⁻¹¹ and Mg(OH)₂ = 8.9×10⁻¹² vs shipped 3.45×10⁻¹¹ / 5.61×10⁻¹²
  (modern CRC values). Provenance the named source contradicts — a data-sourced badge honesty defect. Verdicts/
  trends don't flip (prediction margins 4170× and 44.9×) but lesson numerics change if values change. **Owner
  decision O1 below.**
- **A4 [MED]** `data/elements.toml` electronegativities are the revised-Pauling set (F 3.98, O 3.44, Cl 3.16)
  cited to OpenStax Fig 7.6, which uses classic Pauling (F 4.0 stated in the text; its Example 7.3 ΔEN values —
  O–H 1.4, C–H 0.4 — are arithmetically incompatible with the shipped numbers, which give 1.24, 0.35). **Owner
  decision O2 below.** Related low: `data/elements.toml` header's interval-uncertainty policy misstates CIAAW.
- **A5 [MED]** `reference/concepts/polyprotic-acid.toml:10`: "the amphiprotic middle species sits at ~Ka2" is
  wrong for the diprotic case — the species at ≈Ka2 is the **second-ionization product**. Align README.md:19 and
  `equilibrium.py:778` docstring phrasing.
- **A6 [MED]** `reference/concepts/oxidation-number.toml:7`: badged `ledger-exact` while the rule hierarchy is
  regime-3 by the project's own regime map, and the rules are unsourced repo-wide (hard-coded in `redox.py
  _rule_ox`). Fix: ADR-0044-style split (exact accounting vs rule-sourced rules) or badge + register a source.
- **A7 [MED]** `data/rate-constants.toml`: no entry carries a temperature; the cited source states 40 °C for the
  H₂O₂ k. `data/reduction-potentials.toml` likewise never states 25 °C (the Ka/Kb/Kw/Ksp files all do).
- **A8 [MED, flagged-unverified]** `data/specific-heats.toml` silver 0.233 attributed to OpenStax Table 5.1 which
  has no silver row (accepted 25 °C value 0.235); `data/boiling-points.toml` cites §10.1 which prints none of the
  four values. Chemistry fine, provenance wrong — re-cite or re-source.
- **A9 [MED]** neutralization lesson: misconception claim ("uses up both completely") is refuted by the
  `moles_and_extent_limits` branch in SolutionPlayer, which argues against the volume trap — a claim the learner
  wasn't shown. Also `derived/equilibrium/*.prediction.json` regimes facet says "ICE ledger" though the kind has
  no ICE table.
- **A10 [MED]** titration half-equivalence card asserts "pH = pKa" while displaying 4.75 vs 4.74 (exact solve
  straddles the rounding boundary; regime-map/ADR-0048 say "4.74 = 4.74"); the gate only checks ≈ within 0.05.

## Work package B — verification/honesty hardening (the product is the verification system)

- **B1** `scripts/validate/validate-gyms.mjs:22,436`: exits 0 when `derived/gyms/` is missing or empty — deleting
  all 13 gyms (130 problems) deploys green. One-line fix, matching validate-solutions. Same pattern: only
  `*.solution.json` has a non-empty requirement; the other six lesson shapes pass at zero files.
- **B2** `scripts/validate/electrochemistrycheck.mjs:83`: the `ox_display` check is dead code (an `if` whose body
  is a comment) — needs a fraction-aware label check. Adjacent: `oxidation_states.*.from/.to` rendered but only
  monotonicity-checked.
- **B3** `scripts/validate/check-katex.mjs`: live rendered-but-ungated LaTeX — titration `neutralizationHtml`
  (EquilibriumLesson.astro:148), valence-table `other_ions[].latex` (ValenceTable.svelte:207), formula-sheet
  assumption `claimHtml` (formulas.astro:104).
- **B4** `scripts/validate/check-parity.mjs:230`: interactive-practice loop has no else-fail for unrecognized
  question kinds (still counts them as checked).
- **B5** `balancecheck.mjs` is imported by the gyms and reference gates but **not** by validate-solutions/
  check-ledger — the six reaction lessons' equations (text/latex/coefficients vs ledger ν, atom/charge balance)
  are asserted booleans (schema-pinned `const:true`), not re-derived. Wire `verifyBalance` in.
- **B6** kinetics/electrochem `*_display` twins that are rendered but not gate-checked: `result.half_life_*_display`,
  `half_life.hours_display` (drives SVG gridlines), curve `t_hours_display` (polyline x-coords), landmark
  `concentration_display`; electrochem `e_cell_display`, `delta_g_display`, both `e_standard_display`.
- **B7 [stack contract]** the full KaTeX runtime ships client-side in the SolutionPlayer chunk
  (`dist/_astro/SolutionPlayer.*.js`, 309 KB, contains katex) via the view.js→katex.js import chain — violates
  ADR-0001 "KaTeX at build time (no client KaTeX)". **Owner decision O3 below.** Add a bundle-scan gate once fixed.
- **B8** producer guards: extent.py:95 product rows skip the nonnegative (and nonpositive-ν) checks the reactant
  rows get (emit-time contract; CI's check-ledger backstops committed output); reaction.py:54 `dissociate()`
  silently resolves ambiguous ion decompositions by table order (H₂O(aq), if ever authored, → H⁺ + O²⁻ and passes
  every gate — add ambiguity refusal like balance()'s); the ADR-0013 never-float contract is unenforced at the
  spec boundary (`units._to_decimal` launders floats; `build.py` `Decimal(float)` admits binary garbage; only
  `extent._frac` refuses — reject non-string quantities at load); build.py:750 dispatch-by-glob silently skips a
  misnamed lesson file (add an unmatched-file error); structure.py:121 no duplicate-bond guard (parallel single
  bonds inflate the VSEPR domain count; structurecheck.mjs shares the convention so it can't catch it);
  equilibrium.py:963 titration equivalence dispatch uses exact Decimal equality on a rounded division — a
  non-terminating v_eq emits garbage pH (~32/−15) (gate catches it loud, but producer should dispatch on tolerance
  + reconcile the gate's re-derived region from the 6-sig-fig volume).
- **B9** schema tightening: `solution.schema.json` regimes needs `minItems:1` (only lesson schema without it; an
  empty regimes array ships unlabeled honesty data); `reference.schema.json` concept `regime` should be required
  (producer doesn't default it either — end-to-end hole); forbid `k_times_thalf` off order 1 (and consider
  requiring it on order 1); gym numeric fields lack the decimalString pattern (a Decimal sci-notation repr has
  already leaked into a committed instance); kinetics schema descriptions still first-order-only; equilibrium
  schema says "Four subtypes" (six); gym schema description still Phase-1.
- **B10** `scan-text.mjs` blocklist is only 4 exam-board-family patterns (the sibling-seeded set) while the
  ADR-0004 policy is any course/exam/board — extend (IB, A-level, GCSE, NGSS, AQA, OCR, …) before any
  public-facing release. (Note: this gate scans committed docs including session logs — avoid the bare
  banned acronym even when describing the finding.)
- **B11** cwd-coupling (baseline): thread a root through `generate_gym`; consider anchoring the producer-wide
  cwd default to the repo root so pytest passes from anywhere.

## Work package C — navigability + pedagogy

- **C1** No learning order anywhere: lessons and gym indexes sort alphabetically by slug; a learner's sole CTA
  lands on Bonding first, buffer before weak-acid, the foundational precipitation lessons at positions 20–21/22.
  Nothing marks tiers/prerequisites; the landing page never mentions the Gym (where the entry skills live) or the
  Valence Table flagship. Add a tier/sequence ordering + a start-here path. **Zero lesson↔gym cross-links**
  site-wide (the one reference→gym link is valence-table.astro:21); cross-link both ways.
- **C2** 9 new Atlas entries (kinetics + electrochemistry concepts) ship literal `[[wiki-link]]` markup to
  learners (grep `[[` in derived/reference — 9 files). Render or strip at build; add a gate assertion.
- **C3** "Used in / Lessons:" labels degrade to raw slugs for 4 of 7 lesson kinds on all five reference pages
  (lessonTitle maps glob only solution/structure/comparison). Factor a shared seven-kind helper (pattern already
  in `lessons/[slug].astro:21`).
- **C4 [flagged-unverified]** 32/43 Atlas concept chips display raw regime slugs ("ledger-exact") instead of the
  three-badge learner vocabulary; jargon leaks in learner prose — "bisection"/"the gate re-solved it"
  (EquilibriumLesson.astro:137,402), "(ADR-0002)" cited in the chemical-equilibrium Atlas definition, "null space
  of the conservation matrix" (balancing-equations), "[Decimal compare]", "The producer assembled every pair".
- **C5** verification page still describes only the Phase-1 precipitation pipeline — undersells the checks behind
  16 of 22 lessons (ICE re-solve, curve re-derivation, electron ledger, Lewis ledger, dimensional homogeneity).
- **C6** mobile: Atlas tables (`formulas.astro` .vars 22rem, `molecules.astro`/StructureLesson .atoms 24rem) force
  page overflow at 375 px (lesson components all use .tablewrap — copy the pattern); ValenceTable Explore grid
  renders 18 fluid columns → ~16 px cells at phone width (WCAG 24 px target floor); dark-mode primary buttons
  ~2.7:1 contrast; reactions.astro hardcodes light-theme colors.
- **C7** display-policy drift: `build.py:332 mass_g_display` uses `to_decimal(m, 3)` = 3 **decimal places**, not
  the ADR-0025 3-significant-figure policy (verified: `to_decimal` quantizes places); ionic equations ship with
  **no phase labels** (molecular has them; complete/net ionic don't — house conventions say phases always explicit).
- **C8** practice/gym polish: kinetics gym q0's two diagnostics (0.009975, 0.01) sit 0.25% apart — inside the 1%
  tolerance, so the second-order mistake can trigger the first-order message (space diagnostics or tighten match);
  lesson practice choices render in emission order and the correct answer is never last in all 12 committed choice
  questions (seeded shuffle); kinetics SVG "n×½" landmark labels vs first-half-life gridlines mislead for orders ≠ 1;
  weak-acid gym prompts write "HCN ⇌ H⁺ + A⁻" (name the actual anion); ElectrochemistryLesson hardcodes the cathode
  ion as "²⁺" (an Ag⁺ lesson would render Ag²⁺ — wrong chemistry), ignores the JSON `spontaneous` flag, and
  hardcodes F=96485 twice; EquilibriumLesson misconception branches hardcode exemplar-specific prose (the Ksp
  branch's "about 35×" is wrong for the already-curated Mg(OH)₂ — 47.26×); energy lesson pairs signed q with
  "released" ("heat released −44.5 kJ") [flagged-unverified]; favicon + 404 page missing; breadcrumbs show raw
  topic slugs.

## Work package D — docs/protocol sweep

- AGENTS.md `## Current state` is **78 lines vs its 25-line ceiling** (3.1×); ROADMAP `## Status` ~203 lines vs
  the one-line-per-landed-item budget. Compress both (history → CHANGELOG/session logs).
- Counters: **"21 lessons" → 22** (breakdown already sums to 22); "9 rule-sourced" → 10; two stale ADR ranges
  (lines 194, 273: …-0049/-0048 → -0050). regime-map.md Phase column corrupted with lesson counts in 4 rows
  (kinetics "3", acids-bases "3", Ksp "4", electrochemistry "1" — all Phase **2**); duplicate IMF row (lines 30–31);
  regime-map + README still say "156-salt" (now 182). `docs/authoring-gyms.md` missing kinetics_v1 (12/13).
  architecture.md module-map "proofs" row still says "SymPy dims.py / partly built".
- Docstring drift: build.py "Five lesson shapes" (seven) + build_equilibrium "Four subtypes" (six); gym.py
  `_numeric_response` "0.5%" (code: 3.5%); equilibrium.py residual-threshold claim (no threshold exists);
  extent.py "Q7 still open" (closed, ADR-0025); view.js "first-order" comment (orders 0/1/2).

## Owner decisions — RESOLVED same session (owner chose all four recommendations)

- **O1 (Ksp source): adopt OpenStax Appendix J values** (CaF₂ 4.0e-11, Mg(OH)₂ 8.9e-12). Lesson numerics shift;
  re-produce + gates; no prediction verdict flips (margins 4170× / 44.9×).
- **O2 (EN source): keep the revised-Pauling values, fix the citation** to a source that actually carries them;
  downstream ΔEN/polarity classifications stay as shipped.
- **O3 (client KaTeX): move SolutionPlayer's runtime KaTeX to build-time rendering** (restores ADR-0001), then
  add a bundle-scan gate so it can't regress.
- **O4 (v1.0.0 scope): close the tiers first** — Arrhenius (kinetics) + Nernst/ΔG-formula-entry + redox-balancing
  gym (electrochemistry) land before the Phase-2 DoD review and the v1.0.0 tag.

## Path to v1.0.0 (decisions applied)

1. Session: work package A (chemistry corrections) — small diffs, re-produce, gates.
2. Session: work package B (gate/producer/schema hardening).
3. Session: work package C (navigability/pedagogy; learning order is the big rock).
4. Session: work package D (orientation-doc compression + counter fixes) — may fold into 1–3's close-outs.
5. Sessions: Arrhenius; Nernst + redox gym (if O4 = close the tiers).
6. Phase-2 DoD review + release mechanics: package.json 0.0.0 → 1.0.0, CHANGELOG v1.0.0 entry, git tag,
   README refresh (public face; 156→182 etc.), scan-text broadened (B10), **public attribution/credits page for
   OpenStax CC BY 4.0 + CIAAW/NIST data** (not reviewed this sweep — licensing compliance for a public site
   needs a pass; LICENSE-content.md exists but the rendered site shows only source ids).

Not reviewed this sweep (residual risk, the completeness critic died at budget): licensing/attribution
end-to-end, README as public face, interactive slider runtime behavior beyond parity, print/copy behavior, SEO.

### State at session end — QC sweep complete; v1.0.0 work order written

**Nothing shipped or fixed — review only.** Baseline: derived/ deterministic, 7 gates + build green (45 pages),
427/427 tests from repo root (5 cwd-coupled failures from producer/). Findings: 3 high-severity chemistry/honesty
defects (A1 inverted half-life claim, A2 rate-convention contradiction, A3 Ksp provenance) + ~30 confirmed
mediums across four work packages (A chemistry, B verification hardening, C navigability, D docs), all with
file:line above. The four owner decisions were resolved in-session: O1 adopt OpenStax Ksp values, O2 keep
revised-Pauling EN + fix citation, O3 build-time KaTeX + bundle gate, O4 close the tiers before tagging.
**Next up:** execute work package A (chemistry corrections) — start with A1/A2 (the two rendered chemistry
errors), then A3 (OpenStax Ksp values + re-produce) and A4–A10; then B, C, D, Arrhenius, Nernst, DoD, tag.

---

## Chapter 2 — work package A SHIPPED (Opus 4.8 implementing, ADR-0051)

Model switched to Opus 4.8; owner: "Continue autonomously." Executed all of work package A end to end.

**A1 (HIGH, inverted half-life):** fixed the first-order misconception refutation in
[KineticsLesson.astro](src/components/KineticsLesson.astro) (shrinking t½ ⇒ order **< 1**, growing ⇒ higher order),
plus the H₂O₂ TOML comment and the ADR-0049 parenthetical. **A2 (HIGH, rate convention):** the
[reaction-rate concept](reference/concepts/reaction-rate.toml) and the [kinetics engine docstring](producer/src/chemkernel/kinetics.py)
now disclose the two conventions and state the lessons use the per-species rate −d[A]/dt = k[A]ⁿ (k = A's disappearance
constant) — no shipped number changed. **A3 (HIGH, Ksp):** OpenStax App-J values adopted (CaF₂ 4.0×10⁻¹¹,
Mg(OH)₂ 8.9×10⁻¹²); solubility 2.15×10⁻⁴ M, suppression 53 900×, prediction margins 3600×/71× — both verdicts hold.
**A4 (EN):** registered `allred-1961-electronegativity`, repointed elements.toml + fixed the interval-uncertainty
header claim. **A5:** polyprotic "amphiprotic middle" → "second-ionization product" (concept + README + docstring).
**A6:** oxidation-number concept rebadged `ledger-exact` → `rule-sourced` + OpenStax §4.1 source (regime-map now
consistent). **A7:** temperatures recorded (H₂O₂ k 40 °C; E° table's 25 °C) + honest disclosure where the source
gives none; aluminium → aluminum. **A8:** silver (absent from Table 5.1) → silicon; boiling points re-sourced to
new `nist-webbook-boiling-points` (§10.1 keeps the trend). **A9:** new `neutralization_leaves_excess` refutation
branch in SolutionPlayer (ledger shows limiting → 0, excess keeps 0.5 mmol) + prediction regimes facet "ICE ledger"
→ "dilution + reaction quotient Q". **A10:** titration "pH = pKₐ" → "pH ≈ pKₐ" with an honest exact-solve-offset note.

**Verification:** `npm run produce` → 23 derived files regenerated (deterministic; re-run leaves exactly those 23).
All **7 gates green**, **astro build 45 pages**, **427/427 producer tests** (8 value/source assertions updated to match
the intentional changes). Built HTML spot-checked for A1/A9/A10 rendered text. Two new SOURCES.md rows; the OpenStax
row shed EN + boiling-point values, gained the §4.1 oxidation-number rules. ADR-0051 records the honesty-touching calls.
**Also fixed:** the earlier docs-only pushes (7dc0107, 94d964f) had failed CI on the scan gate — my QC log contained the
bare banned acronym while quoting finding B10; reworded (the scan gate scans committed session logs too). This push is green.

**Counters that move (for the eventual doc sweep, package D):** none of the totals change (22 lessons, 85 Atlas
objects, 13 gyms/130, 45 pages, 427 tests all hold). But the **rule-sourced concept count rises by 2** since the
review's baseline: standard-reduction-potential (already noted stale) **and** oxidation-number (this session) — the
AGENTS.md "9 rule-sourced" sub-count should become **11** when D runs, not 10.

### State at session end — work package A COMPLETE; B/C/D + tier-depth remain

**Shipped:** all of work package A (3 HIGH + honesty/provenance corrections), ADR-0051, committed + pushed CI-green.
Everything verified: 7 gates, 45 pages, 427 tests, deterministic derived/. Owner decisions O1–O4 all applied.
**Next up:** work package **B** (verification/gate hardening — B1 validate-gyms empty-dir exit-0, B2 electrochem
ox_display dead check, B3 ungated KaTeX fields, B5 wire balancecheck into lesson gates, B7/O3 build-time KaTeX +
bundle gate, B8 producer guards, B9 schema tightening, B10 scan-text breadth), then **C** (navigability — learning
order is the big rock), **D** (doc compression + counter fixes incl. the "9→11 rule-sourced" above), then Phase-2
depth (Arrhenius; Nernst) and the v1.0.0 tag. The work order with file:line for every item is in Chapter 1 above.
