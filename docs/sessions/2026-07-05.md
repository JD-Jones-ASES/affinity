# Session 2026-07-05 — bootstrap: docs-first founding, private repo

Fresh project; nothing to resume. The owner supplied the founding brief and asked for the initial
documentation and infrastructure buildout — no heavy coding — structured so that subsequent
implementation sessions can drive from the docs alone, with AGENTS.md routing which documents each kind
of session reads and every session ending in a doc sweep + git actions. Private repo approved. Terse log
of what shipped, what's deferred, and next-up. (Doc-sweep SOP per AGENTS.md §Session close-out.)

## Decisions (full rationale in DECISIONS.md)

- Eleven founding ADRs recorded (ADR-0001…0011): mirror the Quadrature stack; ChemKernel + species
  ledger over extent as the computational object; four-regime/three-badge honesty model; frozen
  gitignored brief + provider-agnostic committed text; TOML authoring; versioned sourced datasets with
  the element-dataset choice deferred to Phase 0; hard safety scope (no wet-lab procedure); two-layer
  verification split; docs-first bootstrap with explicit session routing + close-out protocol; MIT +
  CC BY-SA 4.0 and private-until-published; direct instruction + sliders-must-earn-their-place.

## Shipped — the documentation contract

- **Reconnaissance:** four parallel readers mapped the sibling Quadrature repo (entry-point structure,
  docs/ inventory and session-log format, ADR/ROADMAP skeletons, full pipeline/infra) so Affinity mirrors
  proven conventions rather than inventing.
- **Root docs:** AGENTS.md (identity + session-routing table + mandatory close-out checklist + factory
  invariant + planned repo map + honesty model + escalation rules + inherited traps), README.md (public
  pitch, honest pre-release status), ROADMAP.md (Phase 0 vertical slice scoped from brief §16 with
  definition of done; Phase 1 map; Atlas parallel track; out-of-scope), DECISIONS.md, CHANGELOG.md.
- **docs/:** architecture.md (pipeline, ChemKernel module map, solution-object plan, ported-machinery
  inventory from the sibling, planned gate table, seven open questions each ending in an ADR),
  house-conventions.md (units/notation/chemistry defaults/sig-figs/naming; open items marked),
  regime-map.md (all v1 topics regime-classified — the coverage dashboard), SOURCES.md (three-tier badge
  taxonomy, empty register, element-dataset candidates).
- **Hygiene:** .gitignore (brief + JD.md private; Drive temp dirs; derived/-is-committed note),
  .gitattributes (LF-pinned), LICENSE (MIT), LICENSE-content.md (CC BY-SA 4.0). Founding brief renamed
  `Project_Brief.md` → `PROJECT_BRIEF.md` to match the gitignore, kept frozen (ADR-0004).

## Verification

- Docs-only session: no producer, no gates, no tests yet (counters all zero by design).
- **Adversarial review pass, four independent lenses** (cross-reference integrity; contradiction sweep
  against the brief and across docs; cold-start executability — "could a fresh session run Phase 0 from
  these docs alone?"; leak/hygiene scan): **15 findings, all fixed before first commit.** Notable:
  SOURCES.md's sibling-ported method tags now map explicitly onto the ADR-0003 badges; routing-table
  section refs aligned to real architecture.md headings; house-conventions rows added to three routing
  rows; species-id pattern exemption and topic-slug mapping made explicit; ROADMAP restored two dropped
  brief-§16 items (carbonate charge-balance click; all three misconception targets) and now owns its
  Phase 1/2+ split of brief §17; percent yield moved to Phase 1 in the regime map. Clean areas confirmed:
  all ADR ids/links/brief §refs resolve, sibling paths verified on disk, zero banned-term hits in
  committed text, licenses/dates consistent.
- Staging verified with `git ls-files --cached` at close-out before the founding commit (Drive temp-dir
  gotcha): brief, `JD.md`, `.claude/` absent from the index.

## Deferred / open

- Element-dataset decision (ADR-0006) — first Phase 0 data session; candidates listed in SOURCES.md.
- Architecture open questions Q1–Q7 (architecture.md) — resolve during Phase 0, each → ADR.
- `docs/authoring-problems.md` — written in Phase 0 once the spec format stabilizes (writing it now would
  be speculation).
- CI workflow + `.claude/launch.json` — wired when there is a build to run.

## State at session end

**0 lessons, 0 Atlas entries, 0 gates, 0 tests** — documentation contract complete: 5 root docs, 4
evergreen docs + this log, licenses, hygiene files. Private repo `JD-Jones-ASES/affinity` created and
pushed at close-out of this session.

## Next up (owner's call)

Open directions: (a) start Phase 0 with the element-dataset research session (ADR-0006) — ROADMAP's
recommendation; (b) start Phase 0 with ChemKernel's formula parser + balancer — but note molar-mass
emission depends on the ADR-0006 dataset (no hard-coded constants), so either land (a) in the same
session or defer molar mass; (c) a planning session to pin the solution schema first (architecture
Q2/Q5).

## Autonomous session (cont.) — ChemKernel foundation (Opus 4.8 driving)

Owner switched the model to Opus 4.8 mid-session ("continue the buildout autonomously, according to
plan") — the handoff the bootstrap was built for. Took direction (a)+(b) together: land the dataset first
(so molar mass has a legal source), then the parser and balancer.

### Decisions (full rationale in DECISIONS.md)
- ADR-0012 — element/ion datasets: CIAAW atomic weights + IUPAC positions + OpenStax ion charges; TOML
  under `data/`; strings→Decimal; load-time self-check. Resolves architecture Q1.
- ADR-0013 — exact numeric representation: Decimal for masses, rational for balancing, never float.
  Resolves architecture Q2.
- ADR-0014 — parser grammar v0 + balancer via rational null space (1-D required, re-verified). Resolves
  architecture Q3.

### Shipped — the bottom of the ChemKernel stack
- **`data/elements.toml`** (9 elements) + **`data/ions.toml`** (13 ions); three sources registered in
  SOURCES.md. Atomic weights verified against CIAAW's abridged table (web-fetched, cited with access date).
- **`producer/` uv package `chemkernel`**: `__init__` (BuildError + version), `formula.py` (pure parser),
  `data.py` (loader + molar mass + dataset self-validation), `balance.py` (conservation-matrix balancer).
  Mirrors the sibling's package/orchestration conventions (`_write_json` shape, BuildError-with-context,
  `Path.cwd()` root); entry points deferred until `build.py`.
- **`producer/tests/`**: `test_data.py`, `test_formula.py`, `test_balance.py` — independent hand-checks,
  sibling convention (no conftest, one file per module).

### Verification
- **23 producer tests green** (`uv --project producer run pytest`, 6.0s). Coverage: parser (counts,
  nested parentheses, charge, phase, LaTeX, error cases), dataset (Decimal weights, molar mass vs hand
  values, unknown-element failure, ion self-consistency), balancer (Phase-0 molecular `[1,1,1,2]`,
  methane combustion `[1,2,1,2]`, net-ionic charge row `[1,1,1]`, smallest-integer reduction,
  unbalanceable + charge-mismatch raise).
- **End-to-end sanity** (live, not from cached output): M(CaCO₃)=100.086 → 100.09 g/mol (matches brief),
  M(CaCl₂)=110.978, M(Na₂CO₃)=105.988; both target balances correct; SO₄²⁻ LaTeX → `SO_{4}^{2-}`.
- Staging re-checked with `git ls-files --cached` before commit: `.venv`, `__pycache__`, `.pytest_cache`
  absent; `uv.lock` present (committed for reproducibility, sibling pattern).

### Deferred / open
- Architecture Q4–Q7 still open (regime-4 badge, schema granularity, solubility-rule encoding, display
  sig-figs). Q6 (solubility encoding) blocks the net-ionic transformer.
- Next module: units/dimensional-analysis engine — adapt the sibling's `dims.py` (SI 7-vector), add `L`
  and `M ≡ mol/L` to the unit namespace.

### State at session end
**0 lessons, 0 Atlas entries, 0 gates, 23 producer tests green.** ChemKernel foundation (data + parser +
balancer) landed and pushed. Phase 0 continues from the units engine / dissociation / extent solver.

### Increment 2 — units engine + the species ledger (the ADR-0002 pivot)

Owner: "Continue." Built the two modules that turn the foundation into the pivot object.

- **ADR-0015** — `chemkernel.units`: `Quantity` = exact Decimal over an amount/mass/volume `Dim` basis;
  units cancel through ×/÷, mismatch raises. Symbolic homogeneity of reference formulas (`dims.py`)
  deferred to the Atlas.
- **ADR-0016** — `chemkernel.extent`: `solve_extent` → `Ledger` (species, phase, charge, signed ν,
  initial/final mol, role). ξ = min over reactants of n₀/coeff; ties → both limiting; exact `Fraction`;
  refuses negative amounts. Species-agnostic (molecular / net-ionic / complete-ionic).
- **Verification: 37 producer tests green** (+14). End-to-end, live: moles from volume×molarity through
  the units engine → balancer → ledger reproduces the brief exactly — ξ = 0.00250 mol, CaCl₂ limiting,
  0.00050 mol CO₃²⁻ leftover, **0.250 g CaCO₃** — from both the molecular and net-ionic forms. The
  limiting-reagent switch (the interactive's core, and the volume/mass misconception) is tested.
- **State:** 0 lessons, 0 Atlas entries, 0 gates, **37 producer tests green.** Compute core complete
  (data → parser → balancer → units → ledger). Next front: dissociation + net-ionic transformer, which
  opens with a solubility-ruleset sourcing decision (architecture Q6) → then schema + `build.py` + gates.

### Increment 3 — ionic transforms + sourced solubility

Owner: "Continue." Built the reaction-transformation layer, opening (as flagged) with the solubility
sourcing decision so the precipitate is machine-classified, not asserted.

- **ADR-0017** — solubility ruleset: `data/solubility.toml` faithful to OpenStax Chemistry 2e Table 4.1
  (web-fetched + cited), precedence-ordered; `chemkernel.solubility.classify` returns the governing rule
  id for citation; `verify_phase` build check. Resolves architecture Q6.
- **ADR-0018** — reaction transforms: `chemkernel.reaction.dissociate` (salt → ions via the ion table by
  charge balance + composition match, nothing hard-coded), `complete_ionic`, `net_ionic` (spectator
  cancellation + atom/charge conservation re-check).
- **Verification: 47 producer tests green** (+10). End-to-end, live: the Phase-0 reaction transforms
  molecular → complete ionic (`Ca²⁺ + 2Cl⁻ + 2Na⁺ + CO₃²⁻ → CaCO₃(s) + 2Na⁺ + 2Cl⁻`) → net ionic
  (`Ca²⁺ + CO₃²⁻ → CaCO₃(s)`, spectators Na⁺/Cl⁻), and CaCO₃'s precipitation is classified insoluble and
  cited to the carbonate rule; CaSO₄ correctly hits the sulfate exception; a mislabeled `CaCO3(aq)` fails
  `verify_phase`.
- **State:** 0 lessons, 0 Atlas entries, 0 gates, **47 producer tests green.** The complete ChemKernel
  compute + chemistry engine is done — every piece of the Phase 0 lesson's chemistry computes and verifies
  in the library. Next front: the **emit/verify/present layer** — solution schema → `build.py` (authored
  spec → verified JSON) → Node gates + CI → player. This is the natural next session and a good review
  point (the engine is complete; the delivery layer is a fresh sub-area).

### Increment 4 — the emit + verify pipeline (spec → verified JSON → gate)

Owner: "Continue autonomously. Go with your recommendations." Took the recommended path: single solution
schema (Q5), wired the whole spec → JSON → gate loop.

- **ADR-0019** — `chemkernel.build`: `build-problems` entry point reads `problems/**/*.problem.toml`,
  runs the engine, writes `derived/<topic>/<slug>.solution.json`. Exact decimal strings (non-terminating
  → build failure). Emitted species ids are phase-stripped cores.
- **ADR-0020** — one `schemas/solution.schema.json` (draft 2020-12, `additionalProperties:false`, optional
  blocks); `scripts/validate/validate-solutions.mjs` = Ajv + honesty cross-checks; wired via `package.json`
  (`produce` / `validate` / `prepare:data`), Ajv in devDependencies.
- **Authored** `problems/precipitation/calcium-carbonate-limiting.problem.toml` (the brief §16 slice,
  human layer only) → **committed** `derived/precipitation/calcium-carbonate-limiting.solution.json`.
- **Verification: 48 producer tests + the Node gate green.** The emitted JSON reproduces the brief
  (0.250 g CaCO₃, ledger, three equations, spectators Na⁺/Cl⁻, carbonate rule cited). The schema gate was
  proven **non-vacuous** — a throwaway script confirmed Ajv rejects tampered `checks`, extra keys, bad
  enums, and a missing ledger. `npm install` added Ajv (node_modules gitignored, `package-lock.json`
  committed). Staging re-checked: no `node_modules`/`.venv`/caches; `derived/` committed as designed.
- **Determinism fix (caught by the end-of-session readiness check):** `net_ionic` ordered its terms from
  a `set` union, whose iteration varies with `PYTHONHASHSEED` across processes — so a rebuild could reorder
  `Ca^2+ + CO3^2-` and make committed `derived/` non-byte-stable (ADR-0008). Fixed to preserve left-to-right
  insertion order; added a cross-hash-seed determinism test (builds under seeds 0/1/42/12345, requires
  identical output). The committed JSON was unchanged (its order was already stable). +1 test.
- **State:** **1 built + validated lesson (not yet rendered), 0 Atlas entries, 1 Node gate, 49 producer
  tests.** The engine + emit + verify layers are complete; the pipeline runs authored spec → verified JSON
  → pure-Node gate. Next: the **player** (Astro + Svelte) rendering the committed JSON + the two
  interactives, then the rest of the gate suite + CI. The player is the natural next session and a clean
  review point — the whole verified-data spine now exists; what's left is presentation.

### Increment 5 — the player + honest interactives (Opus 4.8 driving)

Owner: "Read AGENTS.md and the roadmap. Continue an autonomous buildout session until my input is required."
Took the flagged next front — the player — and carried it through to the working limiting-reagent switch.
Read the sibling's player patterns (Base/SolutionPlayer/EnergyBars/lib/pages, `check-parity`/`scan-text`)
and ported them chemistry-native.

#### Decisions (full rationale in DECISIONS.md)
- **ADR-0021 — the player:** Astro static + Svelte islands stepping the committed solution JSON; three-badge
  rendering of the honesty model; base `/affinity`; `client:load`; `css:"injected"` for nested islands.
- **ADR-0022 — honest interactives:** the producer emits an optional `interactive` block (JS closed forms +
  engine-computed sample points; multiplicities derived from the real chemistry); `check-parity.mjs`
  re-proves the browser's JS against the engine across the slider range, ties the default to the committed
  answer. Sliders that recompute chemistry stay honest without client-side Python.
- **ADR-0023 — gate suite rounded out:** `check-ledger` (re-derives n = n₀ + ν·ξ), `check-katex`,
  `scan-text` (seeded from the sibling per ADR-0004).

#### Shipped
- **Scaffold:** `astro.config.mjs`, `tsconfig.json`, `package.json` (astro 7 / svelte 5 / katex + scripts),
  `src/layouts/Base.astro`, `src/styles/portal.css` (Affinity tokens: chemistry-blue accent; blue/violet/amber
  badges), `src/lib/{withBase,katex,view}.js`. `.claude/launch.json` (local preview; gitignored).
- **Pages:** `index` (the ledger thesis + three-badge model), `lessons/index` (by topic), `lessons/[slug]`
  (one per committed `*.solution.json`), `verification`.
- **Islands:** `SolutionPlayer.svelte` (the dumb stepper — Equations / Dimensional analysis / Species ledger /
  Beaker / Extent tabs, badges, result cards, SHOWN checks, data-driven misconception refutation, disclosed
  assumptions), `ExtentBar.svelte` (two capacity bars + ξ line + the switch), `BeakerSpecies.svelte` (free
  ions before mixing → solid + spectators + leftover after).
- **Producer:** `chemkernel/interactive.py` (`build_interactive`) + wired into `build.py`; the committed
  `derived/…solution.json` now carries the `interactive` block. Schema grew the optional block (+ `$defs/ionrole`).
- **Gates:** `scripts/validate/{check-parity,check-ledger,check-katex,scan-text}.mjs`, wired into
  `npm run validate` (5 gates) + `npm run build` (validate → astro build).
- **Producer test:** `tests/test_interactive.py` (+4) — shape, closed-forms-reproduce-engine samples,
  default+switch samples, full-build inclusion.

#### Verification (exact counts)
- **53 producer tests green** (`uv --project producer run pytest`, 2.2 s; +4).
- **5 Node gates green:** validate-solutions (1 valid), check-ledger (4 rows satisfy n = n₀+ν·ξ + result
  match), check-parity (**80 closed-form points** = 10 samples × 8 forms match the engine), check-katex (7
  strings render), scan-text (no banned terms). **`astro build`** green (4 pages).
- **All four new gates proven non-vacuous** by tamper tests (wrong molar-mass literal → parity fails; wrong
  final_mol → ledger fails; both exit 1, then restored via rebuild).
- **Determinism** now covers the interactive block (the cross-hash-seed build test builds the full solution).
- **Visual confirmation in the browser** (dev preview): the lesson renders the full verified chain; the
  **Extent tab shows the limiting-reagent switch** — raising [CaCl₂] 0.100→0.150 M flips limiting from Ca²⁺
  to CO₃²⁻ (ξ 2.5→3 mmol, 0.250→0.300 g, matching parity sample 1); the **Beaker tab** shows free ions
  before mixing and CaCO₃(s) + surviving spectators + leftover after — all three brief-§16 misconception
  targets addressed. Both light and dark themes clean; no console errors; nested-island CSS renders (trap #2
  handled). Preview server stopped after verification.

#### Deferred / open
- Practice generator (item 6), Atlas entry + periodic lens (item 7), authoring guide (item 8).
- CI `deploy.yml` — **owner's Phase-0 publish call (ADR-0010); not wired autonomously** (no Pages, no
  visibility flip).
- The interactive emitter supports only single-precipitate double-displacement; other reaction shapes will
  render statically (block omitted) until the emitter generalizes.
- Atom/charge re-check in `check-ledger` (vs. the current extent-equation re-derivation) needs element
  counts in the emitted ledger or a JS parser — future.

#### State at session end
**1 built + rendered lesson, 0 Atlas entries, 5 Node gates + `astro build`, 53 producer tests green.** The
emit → verify → **present** pipeline is complete end to end for the Phase 0 slice: authored spec → verified
JSON → five pure-Node gates → static Astro/Svelte site with two honest, parity-verified interactives. This
is the clean Phase-0 review boundary the prior increment flagged.

#### Next up (owner's call)
The player is done and is a natural review point. Open fronts, each a fresh sub-area: (a) practice generator
(`precipitation_limiting_reagent_v1`); (b) the Chemical Atlas entry + Valence Table periodic lens (opens
`reference/`); (c) the authoring guide from the now-stable spec format; (d) — owner-gated — the Phase-0
publish (CI `deploy.yml`, Pages, visibility flip). Docs-only sessions always in season.

### Increment 6 — practice + authoring guide + CI/Pages (Opus 4.8 driving)

Owner picked up the review point with a queue: **authoring guide → practice generator → Chemical Atlas +
lens**, and unblocked Pages — "my GitHub account is Educator, so you can make the GitHub pages without making
the repo public yet." Did the first two + CI/Pages this increment; the Atlas is the remaining item.

#### Decisions
- No new ADRs — all three items execute inside settled contracts. Practice + the practice `check-parity`
  extension fall under ADR-0022/0023; the authoring guide is docs; CI/Pages is ADR-0001 + ADR-0010 (the
  owner's explicit Pages authorization is the publish call ADR-0010 reserved).

#### Shipped
- **Authoring guide** `docs/authoring-problems.md` — the `*.problem.toml` spec from the stabilized format
  (fields, the bare-keys gotcha, derived-vs-authored, refuse-to-emit, `[practice]`, build/verify, worked ex).
- **CI + Pages** — `.github/workflows/deploy.yml` (npm install → 5 gates + astro build → Pages, base
  `/affinity`, no Python in CI). Pages **enabled on the private repo** via `gh api … build_type=workflow`
  (Educator plan) and **live**: https://jd-jones-ases.github.io/affinity/ (home + lesson 200). First CI run
  succeeded.
- **Practice generator** — `chemkernel.practice.generate_practice` (`precipitation_limiting_reagent_v1`):
  deterministic seeded variants, limiting/mass/leftover asks, misconception distractors, reject-list;
  multiplicities reused from the interactive block. Spec `[practice]` family/seed/count → optional schema
  `practice` block. `check-parity.mjs` re-derives every answer in Node from the closed forms. Player
  `PracticeQuestion.svelte` + a Practice tab. Guide updated with the `[practice]` section.

#### Verification (exact counts)
- **57 producer tests green** (+4 practice: shape, closed-forms-reproduce-engine, default+switch samples,
  full-build inclusion). **5 Node gates green** — check-parity now re-derives **6 practice answers** (80
  closed-form points + 6 practice). **`astro build`** (4 pages) + the **live CI run** green.
- Practice determinism confirmed byte-identical across `PYTHONHASHSEED` 0/1/42/12345 (same full-solution md5);
  the determinism test was updated to force `PYTHONIOENCODING=utf-8` on its subprocess (Windows cp1252 stdout
  can't encode the `ξ` now in practice explanations — the real `_write_json` already writes utf-8).
- **In-browser verification** (production `astro preview`): the Practice tab renders 6 questions; clicking a
  wrong choice reveals it red with its misconception, the correct answer green, and the worked "Why"; tabs
  switch cleanly (one selected); layout wraps on mobile. Both interactives + all static tabs still good.
- **Bug found & fixed via forcing the panel open:** `PracticeQuestion` named a helper `state`, colliding
  with Svelte 5's internal `state` import (used to compile `$state`) → "Cannot access 'state' before
  initialization" at render, which silently corrupted the island's tab reactivity (the panel wouldn't switch
  to Practice; two tabs read selected). Renamed to `choiceClass`. Root-caused with a temporary `data-tab`
  debug attribute after eval/dispatch clicks proved unreliable against Svelte-5 event delegation (only real
  `preview_click` fires delegated handlers; and the 6th tab overflowed the 800px preview viewport).

#### Deferred / open
- **Item 7 — the Chemical Atlas entry + the Valence Table periodic lens** is the sole remaining Phase-0 build
  (opens `reference/` + `derived/reference/` + `build-reference` + `reference.schema.json` +
  `validate-reference` + player pages). After it, Phase 0 is complete → owner review.
- **Pages visibility caveat:** on the Educator (non-Enterprise) plan the deployed site is world-readable at
  its URL even though the repo is private — flagged for the owner to confirm intent
  ([[affinity-publish-pages]] memory).

#### State at session end
**1 built + rendered lesson (with 6 practice questions), 0 Atlas entries, 5 Node gates + CI + live Pages, 57
producer tests green.** Three commits since the player: authoring guide + CI (`647ae06`), practice
(`916ecc1`), and this doc sweep. Site deployed. The only Phase-0 gap is the Atlas/periodic-lens.
