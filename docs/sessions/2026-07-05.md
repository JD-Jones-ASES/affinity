# Session 2026-07-05 — bootstrap: docs-first founding, private repo

Fresh project; nothing to resume. The owner supplied the founding brief and asked for the initial
documentation and infrastructure buildout — no heavy coding — structured so that subsequent
implementation sessions can drive from the docs alone, with AGENTS.md routing which documents each kind
of session reads and every session ending in a doc sweep + git actions. Private repo approved. Terse log
of what shipped, what's deferred, and next-up. (Doc-sweep SOP per AGENTS.md §Session close-out.)

## Decisions (full rationale in DECISIONS.md)

- Eleven founding ADRs recorded (ADR-0001…0011): mirror the Quadrature stack; ChemKernel + species
  ledger over extent as the computational object; four-regime/three-badge honesty model; frozen
  gitignored brief + provider-agnostic committed text; TOML authoring; versioned sourced datasets with
  the element-dataset choice deferred to Phase 0; hard safety scope (no wet-lab procedure); two-layer
  verification split; docs-first bootstrap with explicit session routing + close-out protocol; MIT +
  CC BY-SA 4.0 and private-until-published; direct instruction + sliders-must-earn-their-place.

## Shipped — the documentation contract

- **Reconnaissance:** four parallel readers mapped the sibling Quadrature repo (entry-point structure,
  docs/ inventory and session-log format, ADR/ROADMAP skeletons, full pipeline/infra) so Affinity mirrors
  proven conventions rather than inventing.
- **Root docs:** AGENTS.md (identity + session-routing table + mandatory close-out checklist + factory
  invariant + planned repo map + honesty model + escalation rules + inherited traps), README.md (public
  pitch, honest pre-release status), ROADMAP.md (Phase 0 vertical slice scoped from brief §16 with
  definition of done; Phase 1 map; Atlas parallel track; out-of-scope), DECISIONS.md, CHANGELOG.md.
- **docs/:** architecture.md (pipeline, ChemKernel module map, solution-object plan, ported-machinery
  inventory from the sibling, planned gate table, seven open questions each ending in an ADR),
  house-conventions.md (units/notation/chemistry defaults/sig-figs/naming; open items marked),
  regime-map.md (all v1 topics regime-classified — the coverage dashboard), SOURCES.md (three-tier badge
  taxonomy, empty register, element-dataset candidates).
- **Hygiene:** .gitignore (brief + JD.md private; Drive temp dirs; derived/-is-committed note),
  .gitattributes (LF-pinned), LICENSE (MIT), LICENSE-content.md (CC BY-SA 4.0). Founding brief renamed
  `Project_Brief.md` → `PROJECT_BRIEF.md` to match the gitignore, kept frozen (ADR-0004).

## Verification

- Docs-only session: no producer, no gates, no tests yet (counters all zero by design).
- **Adversarial review pass, four independent lenses** (cross-reference integrity; contradiction sweep
  against the brief and across docs; cold-start executability — "could a fresh session run Phase 0 from
  these docs alone?"; leak/hygiene scan): **15 findings, all fixed before first commit.** Notable:
  SOURCES.md's sibling-ported method tags now map explicitly onto the ADR-0003 badges; routing-table
  section refs aligned to real architecture.md headings; house-conventions rows added to three routing
  rows; species-id pattern exemption and topic-slug mapping made explicit; ROADMAP restored two dropped
  brief-§16 items (carbonate charge-balance click; all three misconception targets) and now owns its
  Phase 1/2+ split of brief §17; percent yield moved to Phase 1 in the regime map. Clean areas confirmed:
  all ADR ids/links/brief §refs resolve, sibling paths verified on disk, zero banned-term hits in
  committed text, licenses/dates consistent.
- Staging verified with `git ls-files --cached` at close-out before the founding commit (Drive temp-dir
  gotcha): brief, `JD.md`, `.claude/` absent from the index.

## Deferred / open

- Element-dataset decision (ADR-0006) — first Phase 0 data session; candidates listed in SOURCES.md.
- Architecture open questions Q1–Q7 (architecture.md) — resolve during Phase 0, each → ADR.
- `docs/authoring-problems.md` — written in Phase 0 once the spec format stabilizes (writing it now would
  be speculation).
- CI workflow + `.claude/launch.json` — wired when there is a build to run.

## State at session end

**0 lessons, 0 Atlas entries, 0 gates, 0 tests** — documentation contract complete: 5 root docs, 4
evergreen docs + this log, licenses, hygiene files. Private repo `JD-Jones-ASES/affinity` created and
pushed at close-out of this session.

## Next up (owner's call)

Open directions: (a) start Phase 0 with the element-dataset research session (ADR-0006) — ROADMAP's
recommendation; (b) start Phase 0 with ChemKernel's formula parser + balancer — but note molar-mass
emission depends on the ADR-0006 dataset (no hard-coded constants), so either land (a) in the same
session or defer molar mass; (c) a planning session to pin the solution schema first (architecture
Q2/Q5).

## Autonomous session (cont.) — ChemKernel foundation (Opus 4.8 driving)

Owner switched the model to Opus 4.8 mid-session ("continue the buildout autonomously, according to
plan") — the handoff the bootstrap was built for. Took direction (a)+(b) together: land the dataset first
(so molar mass has a legal source), then the parser and balancer.

### Decisions (full rationale in DECISIONS.md)
- ADR-0012 — element/ion datasets: CIAAW atomic weights + IUPAC positions + OpenStax ion charges; TOML
  under `data/`; strings→Decimal; load-time self-check. Resolves architecture Q1.
- ADR-0013 — exact numeric representation: Decimal for masses, rational for balancing, never float.
  Resolves architecture Q2.
- ADR-0014 — parser grammar v0 + balancer via rational null space (1-D required, re-verified). Resolves
  architecture Q3.

### Shipped — the bottom of the ChemKernel stack
- **`data/elements.toml`** (9 elements) + **`data/ions.toml`** (13 ions); three sources registered in
  SOURCES.md. Atomic weights verified against CIAAW's abridged table (web-fetched, cited with access date).
- **`producer/` uv package `chemkernel`**: `__init__` (BuildError + version), `formula.py` (pure parser),
  `data.py` (loader + molar mass + dataset self-validation), `balance.py` (conservation-matrix balancer).
  Mirrors the sibling's package/orchestration conventions (`_write_json` shape, BuildError-with-context,
  `Path.cwd()` root); entry points deferred until `build.py`.
- **`producer/tests/`**: `test_data.py`, `test_formula.py`, `test_balance.py` — independent hand-checks,
  sibling convention (no conftest, one file per module).

### Verification
- **23 producer tests green** (`uv --project producer run pytest`, 6.0s). Coverage: parser (counts,
  nested parentheses, charge, phase, LaTeX, error cases), dataset (Decimal weights, molar mass vs hand
  values, unknown-element failure, ion self-consistency), balancer (Phase-0 molecular `[1,1,1,2]`,
  methane combustion `[1,2,1,2]`, net-ionic charge row `[1,1,1]`, smallest-integer reduction,
  unbalanceable + charge-mismatch raise).
- **End-to-end sanity** (live, not from cached output): M(CaCO₃)=100.086 → 100.09 g/mol (matches brief),
  M(CaCl₂)=110.978, M(Na₂CO₃)=105.988; both target balances correct; SO₄²⁻ LaTeX → `SO_{4}^{2-}`.
- Staging re-checked with `git ls-files --cached` before commit: `.venv`, `__pycache__`, `.pytest_cache`
  absent; `uv.lock` present (committed for reproducibility, sibling pattern).

### Deferred / open
- Architecture Q4–Q7 still open (regime-4 badge, schema granularity, solubility-rule encoding, display
  sig-figs). Q6 (solubility encoding) blocks the net-ionic transformer.
- Next module: units/dimensional-analysis engine — adapt the sibling's `dims.py` (SI 7-vector), add `L`
  and `M ≡ mol/L` to the unit namespace.

### State at session end
**0 lessons, 0 Atlas entries, 0 gates, 23 producer tests green.** ChemKernel foundation (data + parser +
balancer) landed and pushed. Phase 0 continues from the units engine / dissociation / extent solver.

### Increment 2 — units engine + the species ledger (the ADR-0002 pivot)

Owner: "Continue." Built the two modules that turn the foundation into the pivot object.

- **ADR-0015** — `chemkernel.units`: `Quantity` = exact Decimal over an amount/mass/volume `Dim` basis;
  units cancel through ×/÷, mismatch raises. Symbolic homogeneity of reference formulas (`dims.py`)
  deferred to the Atlas.
- **ADR-0016** — `chemkernel.extent`: `solve_extent` → `Ledger` (species, phase, charge, signed ν,
  initial/final mol, role). ξ = min over reactants of n₀/coeff; ties → both limiting; exact `Fraction`;
  refuses negative amounts. Species-agnostic (molecular / net-ionic / complete-ionic).
- **Verification: 37 producer tests green** (+14). End-to-end, live: moles from volume×molarity through
  the units engine → balancer → ledger reproduces the brief exactly — ξ = 0.00250 mol, CaCl₂ limiting,
  0.00050 mol CO₃²⁻ leftover, **0.250 g CaCO₃** — from both the molecular and net-ionic forms. The
  limiting-reagent switch (the interactive's core, and the volume/mass misconception) is tested.
- **State:** 0 lessons, 0 Atlas entries, 0 gates, **37 producer tests green.** Compute core complete
  (data → parser → balancer → units → ledger). Next front: dissociation + net-ionic transformer, which
  opens with a solubility-ruleset sourcing decision (architecture Q6) → then schema + `build.py` + gates.

### Increment 3 — ionic transforms + sourced solubility

Owner: "Continue." Built the reaction-transformation layer, opening (as flagged) with the solubility
sourcing decision so the precipitate is machine-classified, not asserted.

- **ADR-0017** — solubility ruleset: `data/solubility.toml` faithful to OpenStax Chemistry 2e Table 4.1
  (web-fetched + cited), precedence-ordered; `chemkernel.solubility.classify` returns the governing rule
  id for citation; `verify_phase` build check. Resolves architecture Q6.
- **ADR-0018** — reaction transforms: `chemkernel.reaction.dissociate` (salt → ions via the ion table by
  charge balance + composition match, nothing hard-coded), `complete_ionic`, `net_ionic` (spectator
  cancellation + atom/charge conservation re-check).
- **Verification: 47 producer tests green** (+10). End-to-end, live: the Phase-0 reaction transforms
  molecular → complete ionic (`Ca²⁺ + 2Cl⁻ + 2Na⁺ + CO₃²⁻ → CaCO₃(s) + 2Na⁺ + 2Cl⁻`) → net ionic
  (`Ca²⁺ + CO₃²⁻ → CaCO₃(s)`, spectators Na⁺/Cl⁻), and CaCO₃'s precipitation is classified insoluble and
  cited to the carbonate rule; CaSO₄ correctly hits the sulfate exception; a mislabeled `CaCO3(aq)` fails
  `verify_phase`.
- **State:** 0 lessons, 0 Atlas entries, 0 gates, **47 producer tests green.** The complete ChemKernel
  compute + chemistry engine is done — every piece of the Phase 0 lesson's chemistry computes and verifies
  in the library. Next front: the **emit/verify/present layer** — solution schema → `build.py` (authored
  spec → verified JSON) → Node gates + CI → player. This is the natural next session and a good review
  point (the engine is complete; the delivery layer is a fresh sub-area).

### Increment 4 — the emit + verify pipeline (spec → verified JSON → gate)

Owner: "Continue autonomously. Go with your recommendations." Took the recommended path: single solution
schema (Q5), wired the whole spec → JSON → gate loop.

- **ADR-0019** — `chemkernel.build`: `build-problems` entry point reads `problems/**/*.problem.toml`,
  runs the engine, writes `derived/<topic>/<slug>.solution.json`. Exact decimal strings (non-terminating
  → build failure). Emitted species ids are phase-stripped cores.
- **ADR-0020** — one `schemas/solution.schema.json` (draft 2020-12, `additionalProperties:false`, optional
  blocks); `scripts/validate/validate-solutions.mjs` = Ajv + honesty cross-checks; wired via `package.json`
  (`produce` / `validate` / `prepare:data`), Ajv in devDependencies.
- **Authored** `problems/precipitation/calcium-carbonate-limiting.problem.toml` (the brief §16 slice,
  human layer only) → **committed** `derived/precipitation/calcium-carbonate-limiting.solution.json`.
- **Verification: 48 producer tests + the Node gate green.** The emitted JSON reproduces the brief
  (0.250 g CaCO₃, ledger, three equations, spectators Na⁺/Cl⁻, carbonate rule cited). The schema gate was
  proven **non-vacuous** — a throwaway script confirmed Ajv rejects tampered `checks`, extra keys, bad
  enums, and a missing ledger. `npm install` added Ajv (node_modules gitignored, `package-lock.json`
  committed). Staging re-checked: no `node_modules`/`.venv`/caches; `derived/` committed as designed.
- **Determinism fix (caught by the end-of-session readiness check):** `net_ionic` ordered its terms from
  a `set` union, whose iteration varies with `PYTHONHASHSEED` across processes — so a rebuild could reorder
  `Ca^2+ + CO3^2-` and make committed `derived/` non-byte-stable (ADR-0008). Fixed to preserve left-to-right
  insertion order; added a cross-hash-seed determinism test (builds under seeds 0/1/42/12345, requires
  identical output). The committed JSON was unchanged (its order was already stable). +1 test.
- **State:** **1 built + validated lesson (not yet rendered), 0 Atlas entries, 1 Node gate, 49 producer
  tests.** The engine + emit + verify layers are complete; the pipeline runs authored spec → verified JSON
  → pure-Node gate. Next: the **player** (Astro + Svelte) rendering the committed JSON + the two
  interactives, then the rest of the gate suite + CI. The player is the natural next session and a clean
  review point — the whole verified-data spine now exists; what's left is presentation.

### Increment 5 — the player + honest interactives (Opus 4.8 driving)

Owner: "Read AGENTS.md and the roadmap. Continue an autonomous buildout session until my input is required."
Took the flagged next front — the player — and carried it through to the working limiting-reagent switch.
Read the sibling's player patterns (Base/SolutionPlayer/EnergyBars/lib/pages, `check-parity`/`scan-text`)
and ported them chemistry-native.

#### Decisions (full rationale in DECISIONS.md)
- **ADR-0021 — the player:** Astro static + Svelte islands stepping the committed solution JSON; three-badge
  rendering of the honesty model; base `/affinity`; `client:load`; `css:"injected"` for nested islands.
- **ADR-0022 — honest interactives:** the producer emits an optional `interactive` block (JS closed forms +
  engine-computed sample points; multiplicities derived from the real chemistry); `check-parity.mjs`
  re-proves the browser's JS against the engine across the slider range, ties the default to the committed
  answer. Sliders that recompute chemistry stay honest without client-side Python.
- **ADR-0023 — gate suite rounded out:** `check-ledger` (re-derives n = n₀ + ν·ξ), `check-katex`,
  `scan-text` (seeded from the sibling per ADR-0004).

#### Shipped
- **Scaffold:** `astro.config.mjs`, `tsconfig.json`, `package.json` (astro 7 / svelte 5 / katex + scripts),
  `src/layouts/Base.astro`, `src/styles/portal.css` (Affinity tokens: chemistry-blue accent; blue/violet/amber
  badges), `src/lib/{withBase,katex,view}.js`. `.claude/launch.json` (local preview; gitignored).
- **Pages:** `index` (the ledger thesis + three-badge model), `lessons/index` (by topic), `lessons/[slug]`
  (one per committed `*.solution.json`), `verification`.
- **Islands:** `SolutionPlayer.svelte` (the dumb stepper — Equations / Dimensional analysis / Species ledger /
  Beaker / Extent tabs, badges, result cards, SHOWN checks, data-driven misconception refutation, disclosed
  assumptions), `ExtentBar.svelte` (two capacity bars + ξ line + the switch), `BeakerSpecies.svelte` (free
  ions before mixing → solid + spectators + leftover after).
- **Producer:** `chemkernel/interactive.py` (`build_interactive`) + wired into `build.py`; the committed
  `derived/…solution.json` now carries the `interactive` block. Schema grew the optional block (+ `$defs/ionrole`).
- **Gates:** `scripts/validate/{check-parity,check-ledger,check-katex,scan-text}.mjs`, wired into
  `npm run validate` (5 gates) + `npm run build` (validate → astro build).
- **Producer test:** `tests/test_interactive.py` (+4) — shape, closed-forms-reproduce-engine samples,
  default+switch samples, full-build inclusion.

#### Verification (exact counts)
- **53 producer tests green** (`uv --project producer run pytest`, 2.2 s; +4).
- **5 Node gates green:** validate-solutions (1 valid), check-ledger (4 rows satisfy n = n₀+ν·ξ + result
  match), check-parity (**80 closed-form points** = 10 samples × 8 forms match the engine), check-katex (7
  strings render), scan-text (no banned terms). **`astro build`** green (4 pages).
- **All four new gates proven non-vacuous** by tamper tests (wrong molar-mass literal → parity fails; wrong
  final_mol → ledger fails; both exit 1, then restored via rebuild).
- **Determinism** now covers the interactive block (the cross-hash-seed build test builds the full solution).
- **Visual confirmation in the browser** (dev preview): the lesson renders the full verified chain; the
  **Extent tab shows the limiting-reagent switch** — raising [CaCl₂] 0.100→0.150 M flips limiting from Ca²⁺
  to CO₃²⁻ (ξ 2.5→3 mmol, 0.250→0.300 g, matching parity sample 1); the **Beaker tab** shows free ions
  before mixing and CaCO₃(s) + surviving spectators + leftover after — all three brief-§16 misconception
  targets addressed. Both light and dark themes clean; no console errors; nested-island CSS renders (trap #2
  handled). Preview server stopped after verification.

#### Deferred / open
- Practice generator (item 6), Atlas entry + periodic lens (item 7), authoring guide (item 8).
- CI `deploy.yml` — **owner's Phase-0 publish call (ADR-0010); not wired autonomously** (no Pages, no
  visibility flip).
- The interactive emitter supports only single-precipitate double-displacement; other reaction shapes will
  render statically (block omitted) until the emitter generalizes.
- Atom/charge re-check in `check-ledger` (vs. the current extent-equation re-derivation) needs element
  counts in the emitted ledger or a JS parser — future.

#### State at session end
**1 built + rendered lesson, 0 Atlas entries, 5 Node gates + `astro build`, 53 producer tests green.** The
emit → verify → **present** pipeline is complete end to end for the Phase 0 slice: authored spec → verified
JSON → five pure-Node gates → static Astro/Svelte site with two honest, parity-verified interactives. This
is the clean Phase-0 review boundary the prior increment flagged.

#### Next up (owner's call)
The player is done and is a natural review point. Open fronts, each a fresh sub-area: (a) practice generator
(`precipitation_limiting_reagent_v1`); (b) the Chemical Atlas entry + Valence Table periodic lens (opens
`reference/`); (c) the authoring guide from the now-stable spec format; (d) — owner-gated — the Phase-0
publish (CI `deploy.yml`, Pages, visibility flip). Docs-only sessions always in season.

### Increment 6 — practice + authoring guide + CI/Pages (Opus 4.8 driving)

Owner picked up the review point with a queue: **authoring guide → practice generator → Chemical Atlas +
lens**, and unblocked Pages — "my GitHub account is Educator, so you can make the GitHub pages without making
the repo public yet." Did the first two + CI/Pages this increment; the Atlas is the remaining item.

#### Decisions
- No new ADRs — all three items execute inside settled contracts. Practice + the practice `check-parity`
  extension fall under ADR-0022/0023; the authoring guide is docs; CI/Pages is ADR-0001 + ADR-0010 (the
  owner's explicit Pages authorization is the publish call ADR-0010 reserved).

#### Shipped
- **Authoring guide** `docs/authoring-problems.md` — the `*.problem.toml` spec from the stabilized format
  (fields, the bare-keys gotcha, derived-vs-authored, refuse-to-emit, `[practice]`, build/verify, worked ex).
- **CI + Pages** — `.github/workflows/deploy.yml` (npm install → 5 gates + astro build → Pages, base
  `/affinity`, no Python in CI). Pages **enabled on the private repo** via `gh api … build_type=workflow`
  (Educator plan) and **live**: https://jd-jones-ases.github.io/affinity/ (home + lesson 200). First CI run
  succeeded.
- **Practice generator** — `chemkernel.practice.generate_practice` (`precipitation_limiting_reagent_v1`):
  deterministic seeded variants, limiting/mass/leftover asks, misconception distractors, reject-list;
  multiplicities reused from the interactive block. Spec `[practice]` family/seed/count → optional schema
  `practice` block. `check-parity.mjs` re-derives every answer in Node from the closed forms. Player
  `PracticeQuestion.svelte` + a Practice tab. Guide updated with the `[practice]` section.

#### Verification (exact counts)
- **57 producer tests green** (+4 practice: shape, closed-forms-reproduce-engine, default+switch samples,
  full-build inclusion). **5 Node gates green** — check-parity now re-derives **6 practice answers** (80
  closed-form points + 6 practice). **`astro build`** (4 pages) + the **live CI run** green.
- Practice determinism confirmed byte-identical across `PYTHONHASHSEED` 0/1/42/12345 (same full-solution md5);
  the determinism test was updated to force `PYTHONIOENCODING=utf-8` on its subprocess (Windows cp1252 stdout
  can't encode the `ξ` now in practice explanations — the real `_write_json` already writes utf-8).
- **In-browser verification** (production `astro preview`): the Practice tab renders 6 questions; clicking a
  wrong choice reveals it red with its misconception, the correct answer green, and the worked "Why"; tabs
  switch cleanly (one selected); layout wraps on mobile. Both interactives + all static tabs still good.
- **Bug found & fixed via forcing the panel open:** `PracticeQuestion` named a helper `state`, colliding
  with Svelte 5's internal `state` import (used to compile `$state`) → "Cannot access 'state' before
  initialization" at render, which silently corrupted the island's tab reactivity (the panel wouldn't switch
  to Practice; two tabs read selected). Renamed to `choiceClass`. Root-caused with a temporary `data-tab`
  debug attribute after eval/dispatch clicks proved unreliable against Svelte-5 event delegation (only real
  `preview_click` fires delegated handlers; and the 6th tab overflowed the 800px preview viewport).

#### Deferred / open
- **Item 7 — the Chemical Atlas entry + the Valence Table periodic lens** is the sole remaining Phase-0 build
  (opens `reference/` + `derived/reference/` + `build-reference` + `reference.schema.json` +
  `validate-reference` + player pages). After it, Phase 0 is complete → owner review.
- **Pages visibility caveat:** on the Educator (non-Enterprise) plan the deployed site is world-readable at
  its URL even though the repo is private — flagged for the owner to confirm intent
  ([[affinity-publish-pages]] memory).

#### State at session end
**1 built + rendered lesson (with 6 practice questions), 0 Atlas entries, 5 Node gates + CI + live Pages, 57
producer tests green.** Three commits since the player: authoring guide + CI (`647ae06`), practice
(`916ecc1`), and this doc sweep. Site deployed. The only Phase-0 gap is the Atlas/periodic-lens.

### Increment 7 — the Chemical Atlas + Valence Table (Phase 0 complete — Opus 4.8 driving)

Owner: "Yes, continue" (to the queued Atlas). Built the reference layer — the last Phase-0 item — end to end.

#### Decisions
- No new ADRs. The reference layer executes inside settled contracts: authored TOML (ADR-0005), data-only
  empirical values (ADR-0006/0012/0017), refuse-to-emit (ADR-0008), two-layer verify (ADR-0008). Charge-
  crossover salt assembly is regime-1 (the parser verifies neutrality + composition) over regime-3 charges.

#### Shipped
- **Producer:** `chemkernel/reference.py` — `build_valence_table` (projects `data/` to elements-in-position +
  sourced ion charges + polyatomic ions, and assembles the four lesson salts by charge crossover, each
  re-checked neutral + composition), `assemble_formula` (crossover with parenthesised polyatomics),
  `build_reference_entry` (concept entries). New `build-reference` entry point (`pyproject.toml`); `npm
  produce` now runs both builders.
- **Content:** `reference/concepts/{limiting-reagent,extent-of-reaction}.toml` — cross-linked (typed
  `related` edges) + tied to the lesson.
- **Contracts:** `schemas/valence-table.schema.json` + `schemas/reference.schema.json`; **`validate-reference.mjs`**
  (Ajv by `kind`; edges + lesson slugs resolve; charge-balance ions ∈ table); `check-katex` extended to
  reference LaTeX + inline definition math. Wired into `npm run validate` (now 6 gates).
- **Player:** `ValenceTable.svelte` (periodic grid by group×period; click element → common ion + sourced
  charge + why; click polyatomic → neutral formulas by charge balance; Ca/Na highlighted),
  `src/pages/reference/{index,valence-table}.astro`, **Reference** in the nav, lesson concept-chips linked
  into the Atlas. `view.js` grew `renderValenceTable`/`renderConcept` (build-time KaTeX).

#### Verification (exact counts)
- **62 producer tests green** (+5: valence-table shape, the four salts, crossover incl. parenthesised
  `Ca(NO3)2` + `Na3PO4`, concept build, missing-field rejection). **6 Node gates green**
  (validate-solutions, **validate-reference** = 3 objects, check-ledger, check-parity, check-katex = **32**
  strings, scan-text). **`astro build` = 6 pages.** `derived/reference/` byte-stable.
- **In-browser (production preview):** both brief-§16 reference targets confirmed — clicking **Ca** shows
  Ca²⁺ + the sourced-charge reasoning; clicking **carbonate** shows CaCO₃ (`1×(+2)+1×(−2)=0`) and Na₂CO₃
  (`2×(+1)+1×(−2)=0`) derived by crossover with the "verified" badge. The Atlas index renders both concept
  cards with inline+display KaTeX, typed related-edges, and lesson links. No console errors.

#### State at session end — PHASE 0 COMPLETE
**1 built + rendered lesson (with 6 practice questions), 1 Valence Table + 2 concept entries, 6 Node gates +
CI + live Pages, 62 producer tests green.** Every brief-§16 definition-of-done item is met end to end and
deployed (https://jd-jones-ases.github.io/affinity/). Commit `0978631` (Atlas) + this doc sweep. **Phase 0
stops here for owner review.**

#### Next up (owner's call)
Phase 0 is done and deployed — the review boundary. After review, per ROADMAP: **Phase 1 (the procedural
core)** and/or the **Atlas breadth-fill** — both are phase boundaries the owner opens, not autonomous. A
second Phase-0 lesson or more Atlas entries would extend within the settled contracts. The
`interactive`/`practice` emitters currently handle only single-precipitate double-displacement; other
reaction shapes render statically until generalised.

### Increment 8 — Atlas breadth-fill + polish (Opus 4.8 driving)

Owner reviewed the deployed site ("looks clean"), flagged one spacing bug, and asked to fill in more Atlas
entries before winding down. Small, self-contained increment inside the settled contracts (Phase 0 stays
complete; this deepens the Atlas).

#### Decisions
- No ADRs. One contract-consistent extension: concept entries gained an optional `source`, and a
  `rule-sourced` concept must cite it — the honesty model (ADR-0003) applied to the Atlas, mirroring the
  solution gate's rule-sourced check.

#### Shipped
- **Fix:** the Verification page rendered "in action in **thelessons**" — a line-ending word met an inline
  `<a>` on the next line and Astro collapsed the whitespace to nothing. Fixed with `{" "}`; a full scan of
  all 6 built HTML pages (`[a-z]<(a|strong|em|code)` / closing-tag-then-letter) confirms **no other collapse
  artifacts** anywhere.
- **Five concept entries** — `molarity`, `molar-mass`, `net-ionic-equation`, `precipitation`,
  `solubility-rules` — now **7 concepts**, cross-linked into a coherent graph. Every reference_link in the
  Phase-0 lesson resolves, so all 9 concept chips link (7 → concepts, calcium/carbonate → the Valence Table).
- **Source field:** `schemas/reference.schema.json` + `build_reference_entry` + `validate-reference` +
  the index badge. `precipitation` and `solubility-rules` are rule-sourced, cited to `openstax-chemistry-2e`.

#### Verification (exact counts)
- **64 producer tests green** (+2: rule-sourced-needs-source, all authored concepts build). **6 gates**:
  validate-reference = **8 objects**, check-katex = **40 strings**; all green. `astro build` = 6 pages.
- In-browser (production preview): the 7 concept cards render with inline+display KaTeX, cross-linked
  related-edges, and lesson links; the two rule-sourced cards show the violet source badge; the lesson's 9
  chips all resolve (verified via fetch+parse); the Verification page reads "in action in the lessons" with
  the space. No console errors.

#### State at session end — PHASE 0 COMPLETE (+ Atlas breadth growing)
**1 lesson (with practice), 1 Valence Table + 7 concept entries (2 rule-sourced, cited), 6 gates + CI + live
Pages, 64 producer tests green.** Commit `ef641d8` (Atlas breadth + fix) + this doc sweep. Deployed. Winding
down per the owner — AGENTS.md routes the next session. Phase 0 remains at the owner-review boundary; Phase 1
and further breadth-fill are the owner's to open.

### Increment 9 — a second lesson (non-unit stoichiometry) + Atlas breadth (Opus 4.8 driving)

Owner: "Read AGENTS.md. This is an autonomous buildout session. Follow the roadmap as you see fit (new
lessons, atlas, etc.). Work until you need my input." Took the sanctioned Phase-0 extension track — a second
lesson + Atlas breadth, all inside the settled contracts (Phase 0 stays complete; no phase boundary opened).
Chose the calcium-phosphate reaction specifically because its **3:2 net-ionic stoichiometry** is the case the
1:1 carbonate lesson structurally can't teach: the limiting reagent is set by moles ÷ coefficient, not raw
moles.

#### Decisions
- **No new ADRs — all changes are contract-consistent extensions** (precedent: increment 8). The second
  lesson executes inside the authored-spec + refuse-to-emit contracts; the coefficient-aware misconception
  refutation and the capacity-based practice explanation are rendering/text choices under ADR-0011/ADR-0022
  (no schema, honesty-model, house-convention, stack, or scope change); the Valence Table phosphate salts and
  the six concepts execute inside the reference builder (ADR-0005). The publish is the already-settled
  push→CI→Pages flow (ADR-0010), not a new publish decision.
- **Design choice (recorded here, not ADR-level):** the practice `limiting`-question explanation was rewritten
  to reason by capacity (moles ÷ net-ionic coefficient) rather than "supplies fewer moles." The old text
  restated the exact misconception the new lesson breaks and is *false in general* for non-1:1 stoichiometry —
  only coincidentally true for the sampled variants. A new producer test locks the capacity behaviour in.

#### Shipped
- **Second lesson** `problems/precipitation/calcium-phosphate-limiting.problem.toml` →
  `derived/…/calcium-phosphate-limiting.solution.json`: 30.0 mL 0.100 M CaCl₂ + 25.0 mL 0.100 M Na₃PO₄ →
  **Ca₃(PO₄)₂(s)** (0.310 g), net ionic `3 Ca²⁺ + 2 PO₄³⁻ → Ca₃(PO₄)₂`, spectators Na⁺/Cl⁻, phosphate rule
  (`insol-phosphate`) cited, CaCl₂ limiting with **more** Ca²⁺ moles (3.00 mmol) than PO₄³⁻ (2.50 mmol).
  Full `interactive` block + 6 `practice` variants — the emitters generalised to coefficient > 1 with **no
  code changes** (multiplicities were already engine-derived). Seed `424242`.
- **Player (`SolutionPlayer.svelte`):** added a data-driven **coefficient-aware refutation** — when the two
  reactant coefficients differ, it reads the ledger and shows each reactant's capacity (initial ÷ |ν|), names
  the smaller, and flags that the limiting reagent starts with more moles. The 1:1 (carbonate) lesson keeps
  the volume story. **Fixed a latent bug:** `smallerVolLimits` compared phased given ids
  (`Na2CO3(aq)`) against phase-stripped ledger ids (`Na2CO3`), so it was always false — right only by
  coincidence; now phase-stripped and correct.
- **Practice (`chemkernel/practice.py`):** `limiting` explanation → capacity reasoning with a
  "fewer moles doesn't always mean limiting" caveat when coefficients differ; mass explanation's molar mass no
  longer padded with trailing zeros. Both lessons' practice regenerated.
- **Atlas: six concepts** — `stoichiometry`, `dissociation`, `spectator-ion`, `polyatomic-ion`,
  `conservation-of-mass`, `balancing-equations` (now **13**), cross-linked (new forward edges + back-edges
  from `limiting-reagent`/`extent-of-reaction`/`net-ionic-equation`); every existing concept's `lessons` now
  lists both lessons; both lesson specs' `reference_links` grew to 15 resolving chips. `polyatomic-ion` is
  rule-sourced (openstax).
- **Valence Table (`reference.py`):** added the phosphate pairs — clicking phosphate shows **Ca₃(PO₄)₂** and
  **Na₃PO₄** by charge crossover, verified neutral (`3×(+2)+2×(−3)=0`, `3×(+1)+1×(−3)=0`).
- **Docs:** CHANGELOG entry, regime-map rows (limiting-reagent/precipitation/molarity → 2 lessons;
  balancing/stoichiometry/reaction-classes/ions gained atlas concepts), ROADMAP + AGENTS current-state
  counters, authoring-problems misconception note, this log.

#### Verification (exact counts)
- **65 producer tests green** (`uv --project producer run pytest`, 2.2 s; +1: non-unit-stoichiometry practice
  asserts capacity-based answers and no "supplies fewer" text). **6 Node gates green:** validate-solutions = 2,
  validate-reference = **14 objects** (1 table + 13 concepts), check-ledger = **8 rows**, check-parity = **160
  closed-form points + 12 practice answers**, check-katex = **71 strings**, scan-text clean. **`astro build`
  = 7 pages.**
- **In-browser (production `astro preview`, base `/affinity`):** the phosphate lesson renders the full chain;
  the **coefficient misconception refutation** reads "CaCl₂: 3 mmol ÷ 3 = 1 mmol; Na₃PO₄: 2.5 mmol ÷ 2 = 1.25
  mmol. CaCl₂ is smaller, so it limits — even though it starts with more moles"; the **Extent switch** flips
  to PO₄³⁻ at [CaCl₂]=0.15 M (ξ 1→1.25 mmol, 0.310→0.388 g, Ca²⁺ 0.75 mmol leftover — matches parity sample 2);
  the **Practice** tab's limiting explanation is capacity-based. **Regression:** the carbonate lesson is byte-
  identical in result (0.250 g / CaCl₂ / Na₂CO₃ 0.5 mmol) and still shows the **volume** story (coefficient
  branch correctly does not fire at 1:1); its chips grew 9→15. The **Atlas index** shows 13 concepts with
  correct badges/edges/lessons; the **Valence Table** phosphate click shows both crossover salts. No console
  errors on any page.

#### Deferred / open
- The `interactive`/`practice` emitters are now proven at arbitrary integer stoichiometry but still only
  handle single-precipitate double-displacement; acid-base / gas-evolution / redox shapes render statically
  until the emitters generalise (a Phase-1+ item).
- The extent-bar/beaker islands style the anion with the `--ion-co3` token regardless of the actual anion
  (cosmetic; the cation is Ca²⁺ in both lessons so `--ion-ca` stays right). A per-anion token is a future
  polish, not touched to avoid shared-CSS churn.

#### State at session end — PHASE 0 COMPLETE (+ Atlas breadth growing)
**2 lessons (with practice), 1 Valence Table + 13 concept entries (2 rule-sourced, cited), 6 gates + CI + live
Pages, 65 producer tests green.** Phase 0 stays complete and at the owner-review boundary; Phase 1 remains the
owner's to open. A third lesson or further Atlas breadth would continue inside these contracts; generalising
the emitters to a new reaction family is a Phase-1 boundary.

### Increment 10 — Phase 1 opens: the dimensional-analysis gym (Opus 4.8 driving)

Owner: "Context window is solid. Start building in Phase=1. Rationale: the more problems we solve, the easier
filling in more granular lessons will be later. Continue autonomously until you need my input." The owner
opened Phase 1 — the phase boundary the prior increments held at. Per the ROADMAP philosophy (open a phase
with its stress scenario so the granular fill inherits a solved instrument) and the owner's build-the-
infrastructure rationale, opened Phase 1 with **item 1 (the dimensional-analysis gym)** and built the reusable
**generated-problem instrument** end to end.

#### Decisions
- **ADR-0024 — procedural gyms:** a first-class generated/verified/drillable content type, mirroring the
  `reference` track's shape (authored `gyms/*.gym.toml` → `build-gyms` → `derived/gyms/*.gym.json` +
  `gym.schema.json` + `validate-gyms` + `/gym/` player). Deterministic in the seed; exact `Fraction` values
  (non-terminating rejected); the **units engine re-checks each conversion's dimensions**; wrong choices are
  named cancellation mistakes; the Node gate re-derives every answer from raw inputs. Chosen over bolting
  gyms onto the lesson pipeline so items 2–6 add a `family`, not new plumbing.

#### Shipped
- **Producer:** `chemkernel/gym.py` — `generate_gym` for family `solution_conversions_v1` (five kinds:
  volume·molarity→moles, moles·molarity→volume, mass↔moles, volume·molarity·molar-mass→grams) over five
  sourced-molar-mass salts; `build-gyms` entry point (`pyproject.toml`); `npm run produce` now runs all three
  builders.
- **Content:** `gyms/dimensional-analysis/solution-conversions.gym.toml` (seed 4242043, 10 problems) + a new
  `reference/concepts/dimensional-analysis.toml` Atlas concept (the gym's chips resolve into it).
- **Contracts:** `schemas/gym.schema.json` (draft 2020-12, `additionalProperties:false`) +
  `scripts/validate/validate-gyms.mjs` — the **7th gate** — Ajv + per-kind Node re-derivation + choice
  invariants (one correct, distinct displays, chain ends at the answer, molar-mass consistency). Wired into
  `npm run validate` and `produce`.
- **Player:** `src/pages/gym/{index,[slug]}.astro` + `src/islands/DimensionalGym.svelte` (a drill stepper —
  pick → reveal the cancellation chain + explanation + named mistake, score, restart); **Gym** added to the
  nav; concept chips into the Atlas.
- **Docs:** ADR-0024; ROADMAP (Phase 1 OPEN + item-1 scope block + status); `docs/authoring-gyms.md`;
  CHANGELOG; regime-map (dimensional-analysis row → gym); AGENTS (factory diagram + repo map + current-state +
  where-next); this log.

#### Verification (exact counts)
- **70 producer tests green** (+5 gym: shape/kind-coverage, exact re-derivation from inputs, terminating
  answers, determinism, unknown-family refusal). **7 Node gates green** — validate-solutions = 2,
  validate-reference = **15 objects** (1 table + 14 concepts), **validate-gyms = 10 problems re-derived**,
  check-ledger = 8, check-parity = 160 + 12, check-katex = **75**, scan. **`astro build` = 9 pages** (+2 gym).
- **Independent answer check:** the generated JSON was inspected — e.g. 50 mL 0.2 M CaCl₂ → 0.01 mol; 16.6467 g
  CaCl₂ (M 110.978) → 0.15 mol; the two-step 40 mL 0.25 M Na₂CO₃ → 1.05988 g — with distractors that are real
  cancellation errors (skipped mL→L → ×1000; inverted the molar-mass factor; stopped at moles).
- **In-browser (production preview):** the `/gym/solution-conversions/` page renders the drill (Problem 1/10,
  score, prompt, three choices), the two badges (10 machine-verified problems; molar masses sourced), and the
  three Atlas chips (Dimensional Analysis/Molarity/Molar Mass, all resolving). **Preview-click limitation:**
  this server session did not dispatch Svelte-5 delegated clicks — the *lesson tabs* (proven working in
  increment 9) were equally unresponsive to `preview_click`, confirming it environmental, not a gym defect.
  `preview_fill` (input events) works; click events are the flaky ones. The `DimensionalGym` click logic is
  byte-for-byte the `PracticeQuestion` pattern verified live in increment 9, and compiles cleanly, so the
  drill interaction is confirmed by pattern parity + the data gates rather than a fresh click.

#### Deferred / open
- Gym families for Phase-1 items 2–6 (nomenclature, balancing, stoichiometry, …) — each a new `family` in
  `generate_gym`. Particle/Avogadro conversions await registering the Avogadro constant as a sourced datum.
- The dimensional-analysis gym answers are shown exact (e.g. 1.05988 g); sig-fig display treatment
  (house-conventions) is a separate, deferred concern.

#### State at session end — PHASE 1 OPEN (item 1 landed)
**2 lessons (with practice), 1 dimensional-analysis gym (10 verified conversions), 1 Valence Table + 14 concept
entries (2 rule-sourced, cited), 7 gates + CI + live Pages, 70 producer tests + astro build (9 pages).** Phase 0
stays complete; Phase 1 is open and filling depth-first — items 2–6 inherit the gym instrument. Next-up
candidates: a nomenclature or balancing gym (item 2/3), a stoichiometry gym (item 4), a third lesson, or Atlas
breadth. Opening Phase 2 remains the owner's call.

### Increment 11 — typography, oracles, doc sweep, roadmap overhaul (Fable 5 driving)

Owner reviewed the deployed practice tab ("Can the practice problems be rendered more nicely, with
subscripts? … Are there Python libraries for doing chemistry? … Enter plan mode for a document sweep and
planning overhaul. Our goal over the next several sessions is a relatively complete chemistry course.")
Planned in plan mode (3 explore agents: doc drift, rendering pipeline, brief scope; 4 owner decisions via
Q&A), then executed the approved plan.

#### Decisions
- **ADR-0025 — formula display conventions.** Three layers, one look: ASCII in data/schemas/gates; upright
  `\mathrm` in producer LaTeX (IUPAC; was math italic); Unicode sub/superscripts in prose via view-side
  `prettyText` (longest-first replaceAll of exactly the producer's tokens, `$…$`-math-safe — measurement
  numbers untouchable by construction). Also **settles architecture Q7** (display sig-figs: ledger exact /
  derived 3 sig figs / givens echoed). Owner picked Unicode-in-prose + upright over KaTeX-everywhere.
- **ADR-0026 — external chemistry libraries are verification oracles only.** chempy + periodictable as
  dev-deps; never runtime; not SOURCES register entries. Owner picked test-oracles-only over
  no-libraries / library-as-data-source.
- **Scope decision (owner):** roadmap targets **Phase 1 + Atlas breadth**; Phase 2 opens only on owner
  review.

#### Shipped
- **Rendering:** `formula._to_latex` wraps bodies in `\mathrm` (tests updated); `view.js` gained
  `prettyText` + `renderGym`, applied to practice prompts/choices/explains/misconceptions, gym
  prompts/explains, lesson scenarios (lesson page + lessons index), assumption + misconception claims;
  precipitate symbol now reuses the ledger row's LaTeX (was ad-hoc italic); slider labels + beaker
  captions/annotation prettified in `ExtentBar`/`BeakerSpecies`. All `derived/` LaTeX regenerated.
- **Oracles:** `producer/tests/test_oracle.py` (+4): every curated atomic weight vs periodictable; every
  corpus molar mass vs chempy; both lesson balances vs `balance_stoichiometry` (1:1:1:2 and 3:2:1:6 both
  reproduced). `pyproject` dev-group + uv.lock updated; importorskip degradation.
- **Doc sweep:** architecture.md to as-built (gyms in pipeline/module-map/gates table; **7 gates**; real
  counters; Q4 given a decision trigger; Q7 resolved); README status modernized + Gym listed;
  house-conventions typography rule + sig-figs settled; SOURCES oracle note.
- **ROADMAP overhaul:** full scope blocks for Phase-1 items 2–6 (nomenclature/balancing/stoichiometry/
  Valence-Table-flagship/reaction-families, from brief §5–§13 detail), an 8-session map, and the Phase-1
  definition of done — "relatively complete procedural course" = every Phase-0/1 regime-map row covered
  (lesson/gym/atlas), all instruments landed, 4+ lessons, review gate before Phase 2.

#### Verification (exact counts)
- **74 producer tests green** (+4 oracle; latex assertions updated). **7 gates green** (unchanged counts:
  2 solutions / 15 reference / 10 gym problems / 8 ledger rows / 160+12 parity / 75 KaTeX / scan clean —
  derived/ ASCII by design, so the string-comparing gates never saw the typography change). **astro build =
  9 pages.**
- **In-browser (owner's dev server):** phosphate scenario reads "…0.100 M CaCl₂(aq) … What mass of
  Ca₃(PO₄)₂ forms…"; zero raw ASCII formulas in the rendered lesson body; practice prompts/choices Unicode
  (via island props); gym prompt "How many moles of CaCl₂ are in 50 mL of 0.2 M CaCl₂(aq)?" — numbers
  untouched; **31 `\mathrm` KaTeX nodes / 0 italic `mathnormal`** on the carbonate lesson; regressions
  intact (0.250 g / 0.310 g, volume-vs-coefficient misconception stories unchanged).

#### Deferred / open
- Architecture Q4 (regime-4 badge) stays open with an explicit trigger: decide when the first
  mechanistic-regime entry ships.
- chempy emits pyparsing deprecation warnings (harmless, dev-only); CI Node-20 actions deprecation still
  pending a version bump.

#### State at session end — PHASE 1 OPEN (item 1 + conventions landed; sessions 2–8 mapped)
**2 lessons + 1 gym, 1 Valence Table + 14 concepts, 7 gates + CI + live Pages, 74 producer tests, 9 pages.**
Next per the ROADMAP session map: **item 2 — the formula & nomenclature engine** (names dataset + rules
module + both-direction gym families + the data-driven name gate).

#### Post-push addendum (increment 11)
First deploy of `13274be` failed **transiently in `actions/deploy-pages`** ("Deployment failed, try again
later") even though every gate + astro build passed in CI — and the failure was briefly masked because
`gh run watch --exit-status | tail` reports tail's exit status, not the watch's. Caught it by curling the
live HTML for the *new content* (still served old), re-ran with `gh run rerun --failed` → success. Live
verified: gym serves "How many moles of CaCl₂ …" (Unicode) and the lesson HTML carries 169 `mathrm` nodes.
Recorded as **known trap #5** in AGENTS.md.

### Increment 12 — Phase-1 item 2: the formula & nomenclature engine (ionic) (Opus 4.8 driving)

Owner: "Go ahead" (to item 2, per the session map). Built the ionic nomenclature engine + gym end to end;
covalent/acid naming deferred within the item.

#### Decisions
- **ADR-0027 — ionic nomenclature.** Sourced `compound_name` on every ion (the name it takes in a compound;
  Stock Roman numerals for variable metals). `chemkernel.nomenclature` = name (cation+anion compound_name) +
  formula (verified crossover). The gym emits structured ion parts so `validate-gyms.mjs` re-derives the
  name (concatenation) and formula (gcd crossover) **in pure Node** — no JS chemistry engine, yet CI
  re-proves the name↔formula pairing. Names regime-3 sourced; composition regime-1 machine-verified.

#### Shipped
- **Data:** `data/elements.toml` += K, Mg, Al, Fe, Cu, Zn (CIAAW weights, each cross-checked against the
  `periodictable` oracle *before* authoring); `data/ions.toml` += Fe²⁺/Fe³⁺, Cu⁺/Cu²⁺, Zn²⁺, K⁺, Mg²⁺,
  Al³⁺, sulfide, nitride, and `compound_name` on all 23 ions; `data.py` Ion.compound_name field.
- **Engine:** `chemkernel/nomenclature.py` (name_ionic, formula_ionic, roman/greek/is_variable/
  other_charge_names/base_cation_name/assemble_with) + `test_nomenclature.py` (6).
- **Gym:** `gym.py` dispatches by family (`_FAMILIES`); `_generate_nomenclature` + `_nomenclature_problem`
  (both directions, reject-on-collision distractors); `gyms/nomenclature/ionic-compounds.gym.toml` (seed
  8175003, 10 problems). Schema generalized (optional chain/unit, `subscript_tokens`, ionic `derivation`
  shape via `$defs/ionpart`). `validate-gyms.mjs` nomenclature branch.
- **Player:** `renderGym` now subscripts prompt/explain/**choices** via `subscript_tokens`; `DimensionalGym`
  renders the chain only when present (nomenclature has none). New `reference/concepts/nomenclature.toml`.
  Valence Table: deterministic lowest-charge pick for variable metals (test updated 9→15 elements).

#### Verification (exact counts)
- **83 producer tests green** (+9: 6 nomenclature + 3 gym-nomenclature). **7 gates:** validate-gyms = **2
  gyms / 20 problems** re-derived (10 conversions numerically, 10 nomenclature by crossover+concat),
  validate-reference = 16, check-katex = 88, others unchanged; scan clean. **astro build = 10 pages.**
- **Independent spot-check:** the 10 generated problems are chemically correct — Cu₂O → copper(I) oxide,
  FeO → iron(II) oxide, iron(III) sulfate → Fe₂(SO₄)₃, calcium phosphate → Ca₃(PO₄)₂ — with distractors that
  are real named mistakes (wrong Stock numeral, own-charge subscripts Fe₃(SO₄)₂, covalent prefixes
  "monosodium mononitrate").
- **In-browser (dev server):** the nomenclature drill renders both directions; formula prompts/choices show
  Unicode subscripts (NaNO₃, Ca₃(PO₄)₂, FeCl₃) while name choices stay plain; props keep ASCII values
  (Ca3(PO4)2) alongside subscripted displays (Ca₃(PO₄)₂) — gate stability preserved. Conversion gym still
  shows its chain; the Valence Table now shows Fe/Cu/Zn. No console errors.

#### Deferred / open
- Covalent-prefix naming (`covalent_nomenclature_v1`, needs a binary-molecular dataset) and acid naming
  (`acid_nomenclature_v1`) — a nomenclature follow-up.
- Full variable-charge display on the Valence Table lens + attaching naming to formula mode — item 5.

#### State at session end — PHASE 1: items 1–2 landed
**2 lessons + 2 gyms (20 verified problems), 1 Valence Table (15 elements) + 15 concepts, 7 gates + CI + live
Pages, 83 producer tests, 10 pages.** Next per the session map: **item 3 — the balancing engine** (a
balancing gym + conservation-matrix view; the emitted matrix lets the island tally per element without a
runtime chemistry engine).
