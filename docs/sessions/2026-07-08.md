# Session 2026-07-08 — Phase-1 item 5b: the Valence-Table flagship modes (Fable 5 driving)

Owner: "Read AGENTS.md. Continue the buildout autonomously. Work until you need my input." Resumed from the
2026-07-05 log's flagged next item — **item 5b, the Valence-Table lenses + trend/bonding/practice modes** —
and landed it end to end, completing item 5. Two new ADRs; one architecture open question (Q4) resolved by
applying ADR-0003's documented default (no honesty-model change; no escalation trigger hit).

## Decisions (full rationale in DECISIONS.md)

- **ADR-0033 — the Valence-Table flagship modes.** (1) **Q4 resolved: no fourth badge** — regime-4
  mechanistic/interpretive content renders under the model-assumed (amber) badge with an explicit
  "interpretive — story, not proof" marker; the lens pattern panels are the project's first regime-4 content
  and ship that way, as does the first `mechanistic` concept. (2) Lenses are emitted data (five, each with
  the brief-§8.1 pattern panel and a source key the gate resolves); **valence electrons** derived from the
  IUPAC group (He = 2; d-block honestly omitted). (3) **Formula mode** = the full cation×anion crossover
  product (156 pairs, H⁺ excluded pending acid naming), verified + **named** via `chemkernel.nomenclature`
  (closing the item-2 deferral), with the own-charge mistake emitted per pair and proven wrong (non-neutral
  vs unreduced); variable-charge metals surface all their ions (`other_ions`). (4) **Bonding mode** = a new
  curated ruleset `data/bonding.toml` (OpenStax Fig 7.8, verified online this session: <0.4 / 0.4–1.8 /
  >1.8, "a general guide with many exceptions" shipped as data); the island computes ΔEN by integer
  arithmetic over build-time ×100 values. (5) **Trend mode** plots committed values only; gaps labeled.
- **ADR-0034 — `periodic_trends_v1`.** The practice mode is a seventh gym family generated from the same
  data: `trend_compare` / `predict_ion` / `order_ionization`, all categorical menus (ADR-0032). Trend
  exceptions are answered from data (the B/Be and O/N first-IE dips), and where the naive left-to-right
  order disagrees with the data it becomes a *named* trap. The gate re-derives every answer in pure Node and
  **cross-checks every embedded value/ion/symbol against the committed `valence-table.json`** (molar-mass
  consistency extended to reference data).

## Shipped

- **Data:** `data/bonding.toml` (sourced ΔEN classes + caution); `data.py` loads + shape-checks it
  (`ChemData.bonding`, a `bonding` sources key). SOURCES.md `openstax-chemistry-2e` coverage extended to
  Fig 7.8 (verified 2026-07-08).
- **Producer** (`chemkernel/reference.py`): `valence_electrons` (group rule), `common_monatomic_ions`
  (factored for reuse by the gym), `_LENSES` (five pattern panels, `regime: "mechanistic"`),
  `_crossover_mistake` (own-charge assembly proven non-neutral or unreduced), the full named
  `charge_balance` product (nomenclature imported lazily — reference↔nomenclature cycle), `other_ions`,
  the `bonding` block. (`chemkernel/gym.py`): the `periodic_trends_v1` family — `_trend_series` (main-group,
  H excluded), `_trend_compare_problem`, `_predict_ion_problem` (fixed-charge main group; sign-flip +
  miscounted-charge distractors), `_order_ionization_problem` (naive order as the named trap when the data
  dips), family-aware provenance sources.
- **Contracts:** `schemas/valence-table.schema.json` grew `lenses`/`bonding`/`valence_electrons`/
  `other_ions`/`charge_balance{name, cation_name, anion_name, mistake}` (+ a shared `$defs/ion`);
  `schemas/gym.schema.json` grew the three kinds + `property`/`direction`/`series`/`candidates`/`element`/
  `ion` and the provenance property-source keys.
- **Gates:** `validate-reference.mjs` re-derives valence electrons (group rule), salt names (concatenation),
  subscripts (gcd crossover + formula reconstruction), mistake presence/formula/kind, ion-id caret suffixes
  vs charges, lens source keys, bonding boundary tiling. `validate-gyms.mjs` gained
  `verifyTrendCompare`/`verifyOrderIonization`/`verifyPredictIon` + the lazy `valence-table.json`
  cross-check.
- **Content:** `gyms/periodic-trends/periodic-trends.gym.toml` (seed 1618033, 10 problems); three concepts —
  `periodic-trends` (**the first mechanistic-regime entry**), `electronegativity` (rule-sourced, openstax),
  `ionization-energy` (rule-sourced, nist) → **19 concepts**.
- **Player:** `ValenceTable.svelte` rebuilt into four modes (Explore + lens selector + color scale + pattern
  panel; Trends SVG with honest gaps + partial-period note; Formula builder with two pickers, verified name,
  crossover note, mistake card; Bonding with exact ΔEN, sourced classification, polarity spectrum, caution).
  `view.js renderValenceTable` renders the new ion/mistake fields and precomputes exact `enCents`/threshold
  cents. `reference/index.astro` renders `mechanistic` as the interpretive badge; `gym/[slug].astro` badge is
  family-aware ("properties sourced (…)" for trends); `valence-table.astro` intro rewritten (and two
  whitespace-collapse artifacts fixed — trap from increment 8; a full-page scan confirms zero artifacts
  across all 16 pages).
- **Tests:** `test_reference.py` +6 (full named product, proven mistakes, VE rule, other_ions,
  lenses/bonding sourcing); `test_gym.py` +5 (shape/rotation/provenance, compare-from-data,
  order-with-named-exceptions, predict-ion-vs-table, determinism) and the categorical-menu parametrization
  now covers the trends gym.
- **Docs:** ADR-0033/0034; ROADMAP (item-5 COMPLETE block + status entry + session map); CHANGELOG; AGENTS
  current state; architecture (Q4 resolved, counters, reference/gym module rows, both gate rows); regime-map
  (periodic-trends row → gym+atlas; bonding row seeded); authoring-gyms (`periodic_trends_v1` +
  response-mode note); SOURCES (Fig 7.8); README status; this log.

## Verification (exact counts)

- **167 producer tests green** (`uv --project producer run pytest`, ~14 s; +11).
- **7 Node gates green:** validate-solutions = 3, **validate-reference = 20 objects** (1 table + 19
  concepts, now re-deriving VE/names/crossover/mistakes/bonding), **validate-gyms = 7 gyms / 70 problems**
  (trends cross-checked against the emitted table), check-ledger = 3/12, check-parity = 240 + 18,
  **check-katex = 253** (the 156 salt formulas), scan clean. **`astro build` = 16 pages** (+1:
  `/gym/periodic-trends/`). `derived/` **byte-stable** across two full rebuilds (30 files, md5-compared).
- **Gates proven non-vacuous — 8 tamper tests, each caught then restored:** corrupted valence_electrons
  (`C: 5 != re-derived 4`), salt name (`re-derived 'iron(III) oxide' != emitted 'iron(II) oxide'`), swapped
  crossover subscripts, dropped CaCO₃ mistake, broken bonding boundary, corrupted trends candidate value
  (`Li electronegativity '9999' != table '0.98'`), reversed order answer, flipped predicted-ion charge.
- **In-browser (production `astro preview`, base `/affinity`):** **Explore** — VE lens shows He 2 / C 4 /
  Ar 8 / Fe —, color scale applied, panel shows all four rows + both badges (`data-sourced
  (iupac-periodic-table)` + `interpretive — story, not proof`). **Trends** — Period-2 first-IE graph plots
  Li 520.2 … Ne 2080.7 with both dips visible (B 800.6 < Be 899.5; O 1313.9 < N 1402.3); the EN series shows
  Ne as a labeled gap ("undefined for the noble gases — never interpolated"). **Formula builder** — default
  Ca²⁺+CO₃²⁻ → CaCO₃ *calcium carbonate* with the unreduced Ca₂(CO₃)₂ mistake; Fe³⁺+O²⁻ → Fe₂O₃ *iron(III)
  oxide* with the own-charge Fe₃O₂ mistake (3×(+3) + 2×(−2) = +5). **Bonding** — H/O → ΔEN 1.24 polar
  covalent; Na/Cl → 2.23 ionic; caution + interpretive badge render. **Gym** — scored a correct group-1 EN
  comparison (explain: "K (0.82) < Na (0.93) < Li (0.98)"), then a wrong Ca²⁻ pick names "Calcium is a metal
  — it LOSES electrons…"; choices render Unicode (Ca²⁺/Ca⁺/Ca²⁻); badge reads "properties sourced
  (openstax-chemistry-2e, cordero-2008-covalent-radii, nist-ionization-energies)"; the three Atlas chips
  resolve. **Reference index** — 19 concepts; `periodic-trends` carries the interpretive badge,
  `electronegativity`/`ionization-energy` the violet rule-sourced badges. Dark theme clean; **no console
  errors or warnings**; the whitespace-collapse scan over all 16 built pages is clean.

## Deferred / open

- **Item 5 leftovers (recorded in ROADMAP):** transition-metal covalent radii (spin-state pass); ionic radii
  (per-ion, coordination-dependent); the further brief-§8.1 lenses (oxidation states, electron affinity,
  metallic character, density, abundance — each needs a data-curation session); a metal/nonmetal field
  (data-driven bonding caution + "which compound is most likely ionic" drills); acid naming in the formula
  builder (item-2 follow-up).
- **Item 6 — reaction families** is the last Phase-1 item (atlas `reaction-family` kind + classifier +
  `reaction_families_v1` gym + the acid-base neutralization lesson, which generalises the
  interactive/practice emitters past precipitation) — then the session-8 Atlas breadth audit and the
  Phase-1 definition-of-done check → owner review before Phase 2.

## State at session end — PHASE 1: items 1–5 DONE

**3 lessons + 7 gyms (70 verified problems) + 1 Valence Table (23 elements; four modes, five lenses,
156 named+verified salts) + 19 concepts (5 rule-sourced, 1 mechanistic/interpretive), 7 gates + CI + live
Pages, 167 producer tests, 16 pages.** Architecture Q4 is closed (interpretive marker, no fourth badge).
**Next up:** item 6 (reaction families), then the Atlas breadth audit and the Phase-1 definition-of-done
check before the Phase-2 review gate.

## Increment 2 — doc-hygiene: anti-bloat budgets + orientation-doc compression (Fable 5 driving)

Owner (after reviewing item 5b): confirm/strengthen the end-of-session doc protocols against bloat/drift,
advise on the session-start workflow, then end the session. Docs-only increment.

### Decisions

- No ADRs — AGENTS.md §Session close-out already mandates the sweep (ADR-0009); this increment adds
  **quantitative anti-bloat budgets** to it and executes the compression they require. Protocol wording is
  AGENTS-level maintenance, not a contract change.

### Shipped

- **Protocol:** close-out step 2 rewritten with explicit budgets — `AGENTS.md ## Current state` = counters +
  standing facts (25-line ceiling, states *what is*, never *how it got there*); `## Where this might go
  next` = one paragraph; `ROADMAP.md ## Status` = a "Now" line + one compressed line per landed item;
  landed scope blocks keep deferred/open lists only; counters live *only* in AGENTS.md; a budget-scoped
  smell test. Routing step 3 now reads the newest log's closing block first; close-out step 3 mandates that
  block ("State at session end" ending in a "Next up" pointer).
- **Compression:** `AGENTS.md ## Current state` + `## Where this might go next` cut from ~110 lines of
  accumulated narrative to ~35 (counters + standing facts + the item-6 pointer); `ROADMAP.md ## Status`
  cut from ~13 long bullets (~14 KB) to a "Now" line + 6 one-liners (~2 KB). All removed detail already
  lived in CHANGELOG.md (one entry per increment, verified) and docs/sessions/.
- **Audit (4-lens adversarial workflow: information-loss, cold-start, consistency, protocol clarity — 17
  findings, all triaged, 4 must-fix + 9 worthwhile fixed):** confirmed **no load-bearing fact lost** and the
  cold-start trace passes; fixed the stale "16 concept entries" in ROADMAP's parallel-track section (→ 19),
  the stale "five Node gates" in AGENTS §Deploy and deploy.yml's comment (→ seven), a smell-test wording
  that would have fired on every correct sweep, the budget unit (per landed *item*, not per increment), and
  three architecture.md routing gaps — the check-parity row + solution-object practice block now state the
  ADR-0032 mode/diagnostics contract, §Rendering gained the ADR-0028 seeded-shuffle rule (hydration-
  mismatch guard), and the as-built header is dated 2026-07-08.

### Verification

- Docs-only: `scan-text` gate green over the edited docs; no producer/gate/site changes (the earlier
  increment's 167 tests + 7 gates + 16 pages stand). Orientation-doc sizes after sweep: AGENTS.md ~220
  lines (was 254 pre-budget with far more in §Current state), ROADMAP.md ~220 lines (was 334).

### State at session end — item 5b shipped + doc protocol hardened

**Counters unchanged from increment 1** (3 lessons · 7 gyms / 70 problems · Valence Table 23 elements/four
modes · 19 concepts · 7 gates · 167 tests · 16 pages · CI/Pages live). Orientation docs are at budget; the
close-out protocol now enforces staying there.
**Next up:** item 6 (reaction families — ROADMAP scope block), then the Atlas breadth audit and the Phase-1
definition-of-done check before the Phase-2 review gate.

## Increment 3 — Phase-1 item 6: reaction families, end to end (Opus 4.8 driving)

Owner: "Read AGENTS.md. Continue the buildout autonomously. Work until you need my input. Multiple increments
are fine." Resumed from the flagged next item — **item 6, reaction families** — and landed it in four
committed sub-increments (6a classifier, 6b Atlas kind, 6c gym, 6d neutralization lesson). One escalation
point hit: the item-6 scope's "generalise the interactive/practice emitters" for the neutralization lesson —
put to the owner (static vs full lesson vs stop); owner chose the **full lesson (sliders + practice)**.
Three new ADRs; no honesty-model / house-convention / scope change (all scoped, additive schema growth).

### Decisions (full rationale in DECISIONS.md)

- **ADR-0035 — reaction classifier + reaction-family Atlas kind.** `classify_reaction` over the balanced
  phased formulas + injected sourced datasets (solubility / acid-base / decomposition), most-specific-first;
  **redox by the free-element signature** (no oxidation numbers — Phase 2). Two curated datasets
  (`data/acids-bases.toml`, `data/decomposition.toml`, `chemkernel.reactivity`, composition-checked). The
  `reaction-family` Atlas kind: 7 families / 21 examples, each engine-balanced + engine-classified (the
  producer refuses a mis-filed example), with net-ionic views. `verifyBalance` extracted to shared
  `balancecheck.mjs`; `validate-reference` re-proves balance + redox + label consistency in Node.
- **ADR-0036 — `reaction_families_v1` gym.** classify-the-family + name-the-spectators, both categorical;
  reactions engine-classified at generation; definitional family distractors, over-inclusion/reacting-ion
  spectator distractors. `validate-gyms` re-proves molecular + net balance + spectator-absence.
- **ADR-0037 — acid-base neutralization lesson (emitter generalisation).** The reported product is the
  net-ionic product of any phase — `result.precipitate` (solid) or general `result.product` + `result.salt`
  (precipitation lessons byte-identical); `interactive.py` drops its must-be-solid guard (the switch was
  always net-ionic-level); `practice.py` parametrised. No solubility claim. Player: water/salt card, Beaker
  tab gated to solids, ExtentBar generalised; the three solution gates read `precipitate ?? product`.

### Shipped

- **Data/producer:** `data/acids-bases.toml`, `data/decomposition.toml`; `chemkernel/reactivity.py`
  (AcidBase/Decomposition, self-checked); `reaction.py` (+`classify_reaction`, `redox_free_elements`);
  `reference.py` (+`build_reaction_family`); `gym.py` (+`reaction_families_v1`, two kinds); `build.py`
  (result generalised — precipitate ?? product + salt; provenance/percent-yield guarded; kind-dispatch for
  reaction-family Atlas entries); `interactive.py` (product any phase); `practice.py` (family + product_noun).
- **Contracts:** `schemas/reaction-family.schema.json` (new); `gym.schema.json` (+2 kinds + family/net_species/
  net_coefficients/spectators + `reaction_classes` provenance); `solution.schema.json` (result.precipitate +
  provenance.solubility now optional; shared `reportedProduct` $def; result.product/salt).
- **Gates:** shared `scripts/validate/balancecheck.mjs` (`verifyBalance` + `redoxFreeElements`, imported by
  validate-gyms + validate-reference); `validate-reference` (reaction-family dispatch + re-derivation);
  `validate-gyms` (`verifyReactionFamily`); `validate-solutions` / `check-ledger` / `check-parity`
  (precipitate ?? product); `check-katex` (reaction-family LaTeX).
- **Content:** `reference/reactions/{combustion,synthesis,decomposition,single-replacement,precipitation,
  acid-base,gas-evolution}.toml` (7 families); `gyms/reaction-families/reaction-families.gym.toml`;
  `problems/neutralization/hydrochloric-sodium-hydroxide.problem.toml`.
- **Player:** `reference/reactions.astro` (new) + Atlas-index card; `view.js` (`renderReactionFamily` +
  general product in `renderSolution`); `SolutionPlayer.svelte` (water/salt card, Beaker gated, ExtentBar
  prop); `ExtentBar.svelte` (product label); `gym/[slug].astro` (reaction-family badge + chips).
- **Tests:** `test_reactivity.py` (new, +6); `test_reaction.py` (+classifier corpus/redox); `test_reference.py`
  (+5 reaction-family); `test_gym.py` (+5 reaction-families); `test_build.py` (+2 neutralization);
  `test_interactive.py` (+1 water product); `test_oracle.py` (+17 chempy corpus balances).
- **Docs:** ADR-0035/0036/0037; CHANGELOG (item-6 entry); ROADMAP (status + item-6 LANDED + session-map #7 +
  parallel-track count); AGENTS current-state (counters + standing facts) + where-next; architecture
  (as-built counters, reaction/reactivity/gym/reference module rows, six gate rows, solution-object result);
  regime-map (reaction-classes row goes lesson+gym+atlas); SOURCES (acids-bases + decomposition);
  authoring-gyms (`reaction_families_v1`); authoring-problems (non-precipitation lesson shape); README; log.

### Verification (exact counts)

- **236 producer tests green** (`uv --project producer run pytest`, ~14 s; +69 from 167).
- **7 Node gates green:** validate-solutions = 4, **validate-reference = 27 objects** (1 table + 19 concepts
  + 7 reaction families), **validate-gyms = 8 gyms / 80 problems**, check-ledger = 4/16, **check-parity =
  320 closed-form points + 24 practice answers**, **check-katex = 381** (+121 reaction-family LaTeX), scan
  clean. **`astro build` = 19 pages** (+3: `/reference/reactions/`, `/gym/reaction-families/`,
  `/lessons/hydrochloric-sodium-hydroxide/`). `derived/` **byte-stable** across two full rebuilds (39 files).
- **Gates proven non-vacuous — tamper tests, each caught then restored:** flipped example redox flag
  (`redox false != re-derived true [H,Zn]`), broken example coefficient (`Cl not conserved`), mis-filed
  family (`family 'acid-base' != entry family 'precipitation'`), spectator that reacts (`Ca^2+ … appears in
  the net ionic equation`), broken gym coefficient (`Zn not conserved`), corrupted neutralization water mass
  (`H2O mass 9.999 != moles × molar mass`; parity `default mass 0.027 != 9.999`).
- **Oracle (ADR-0026):** chempy independently balances all 17 reaction-families-corpus reactions.
- **In-browser (dev preview):** **Reactions Atlas** — all 7 families in teaching order, correct redox badges
  (combustion/synthesis/single-rep = redox; decomposition = "varies"; precip/acid-base/gas = not redox),
  net-ionic views on the 4 replacement families only, H₂CO₃ subscripted in the gas-evolution misconception.
  **Reaction-families gym** — scored a correct classify (Zn + 2 HCl → single replacement) with the
  free-element redox reason; badge "reaction classes sourced (openstax-chemistry-2e)"; concept chips resolve.
  **Neutralization lesson** — cards "Mass of H₂O formed 0.027 g / Salt formed (NaCl) 0.088 g / Limiting NaOH
  / Left HCl 0.5 mmol"; **no Beaker tab**; only machine-checked + model-assumed badges; the Extent slider
  flips the limiting reagent OH⁻ → H⁺ as NaOH rises (0.027 → 0.036 g water, 7 mmol OH⁻ excess). No console
  errors.

### Deferred / open (recorded in ROADMAP item-6 block)

Full oxidation-number assignment (Phase-2 redox); predict-products gym drills; a generic double-replacement
Atlas entry (no driving force = not a real reaction); a gas-evolution *lesson* and the diprotic
coefficient-story neutralization (machinery handles both — second lessons, not new code); further gas-forming
intermediates (sulfurous acid needs the sulfite ion curated).

### State at session end — PHASE 1: items 1–6 all DONE

**4 lessons (2 precipitation + 1 percent-yield + 1 acid-base neutralization) + 8 gyms (80 verified problems)
+ 1 Valence Table (23 elements; four modes) + 19 concepts + 7 reaction families (21 engine-classified example
reactions), 7 gates + CI + live Pages, 236 producer tests, 19 pages.** The lesson emitters now handle any
two-solution reaction whose net-ionic product is a single species (solid precipitate or water). Reaction
classification is the shared instrument behind the Atlas, gym, and lesson; redox is the free-element
signature only.
**Next up:** the **Atlas breadth audit** (session-map #8 — species-atlas + formula-sheet entry kinds, fill
every phase-0/1 regime-map row), then the **Phase-1 definition-of-done check → owner review** before the
Phase-2 gate.
