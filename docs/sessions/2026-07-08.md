# Session 2026-07-08 — Phase-1 item 5b: the Valence-Table flagship modes (Fable 5 driving)

Owner: "Read AGENTS.md. Continue the buildout autonomously. Work until you need my input." Resumed from the
2026-07-05 log's flagged next item — **item 5b, the Valence-Table lenses + trend/bonding/practice modes** —
and landed it end to end, completing item 5. Two new ADRs; one architecture open question (Q4) resolved by
applying ADR-0003's documented default (no honesty-model change; no escalation trigger hit).

## Decisions (full rationale in DECISIONS.md)

- **ADR-0033 — the Valence-Table flagship modes.** (1) **Q4 resolved: no fourth badge** — regime-4
  mechanistic/interpretive content renders under the model-assumed (amber) badge with an explicit
  "interpretive — story, not proof" marker; the lens pattern panels are the project's first regime-4 content
  and ship that way, as does the first `mechanistic` concept. (2) Lenses are emitted data (five, each with
  the brief-§8.1 pattern panel and a source key the gate resolves); **valence electrons** derived from the
  IUPAC group (He = 2; d-block honestly omitted). (3) **Formula mode** = the full cation×anion crossover
  product (156 pairs, H⁺ excluded pending acid naming), verified + **named** via `chemkernel.nomenclature`
  (closing the item-2 deferral), with the own-charge mistake emitted per pair and proven wrong (non-neutral
  vs unreduced); variable-charge metals surface all their ions (`other_ions`). (4) **Bonding mode** = a new
  curated ruleset `data/bonding.toml` (OpenStax Fig 7.8, verified online this session: <0.4 / 0.4–1.8 /
  >1.8, "a general guide with many exceptions" shipped as data); the island computes ΔEN by integer
  arithmetic over build-time ×100 values. (5) **Trend mode** plots committed values only; gaps labeled.
- **ADR-0034 — `periodic_trends_v1`.** The practice mode is a seventh gym family generated from the same
  data: `trend_compare` / `predict_ion` / `order_ionization`, all categorical menus (ADR-0032). Trend
  exceptions are answered from data (the B/Be and O/N first-IE dips), and where the naive left-to-right
  order disagrees with the data it becomes a *named* trap. The gate re-derives every answer in pure Node and
  **cross-checks every embedded value/ion/symbol against the committed `valence-table.json`** (molar-mass
  consistency extended to reference data).

## Shipped

- **Data:** `data/bonding.toml` (sourced ΔEN classes + caution); `data.py` loads + shape-checks it
  (`ChemData.bonding`, a `bonding` sources key). SOURCES.md `openstax-chemistry-2e` coverage extended to
  Fig 7.8 (verified 2026-07-08).
- **Producer** (`chemkernel/reference.py`): `valence_electrons` (group rule), `common_monatomic_ions`
  (factored for reuse by the gym), `_LENSES` (five pattern panels, `regime: "mechanistic"`),
  `_crossover_mistake` (own-charge assembly proven non-neutral or unreduced), the full named
  `charge_balance` product (nomenclature imported lazily — reference↔nomenclature cycle), `other_ions`,
  the `bonding` block. (`chemkernel/gym.py`): the `periodic_trends_v1` family — `_trend_series` (main-group,
  H excluded), `_trend_compare_problem`, `_predict_ion_problem` (fixed-charge main group; sign-flip +
  miscounted-charge distractors), `_order_ionization_problem` (naive order as the named trap when the data
  dips), family-aware provenance sources.
- **Contracts:** `schemas/valence-table.schema.json` grew `lenses`/`bonding`/`valence_electrons`/
  `other_ions`/`charge_balance{name, cation_name, anion_name, mistake}` (+ a shared `$defs/ion`);
  `schemas/gym.schema.json` grew the three kinds + `property`/`direction`/`series`/`candidates`/`element`/
  `ion` and the provenance property-source keys.
- **Gates:** `validate-reference.mjs` re-derives valence electrons (group rule), salt names (concatenation),
  subscripts (gcd crossover + formula reconstruction), mistake presence/formula/kind, ion-id caret suffixes
  vs charges, lens source keys, bonding boundary tiling. `validate-gyms.mjs` gained
  `verifyTrendCompare`/`verifyOrderIonization`/`verifyPredictIon` + the lazy `valence-table.json`
  cross-check.
- **Content:** `gyms/periodic-trends/periodic-trends.gym.toml` (seed 1618033, 10 problems); three concepts —
  `periodic-trends` (**the first mechanistic-regime entry**), `electronegativity` (rule-sourced, openstax),
  `ionization-energy` (rule-sourced, nist) → **19 concepts**.
- **Player:** `ValenceTable.svelte` rebuilt into four modes (Explore + lens selector + color scale + pattern
  panel; Trends SVG with honest gaps + partial-period note; Formula builder with two pickers, verified name,
  crossover note, mistake card; Bonding with exact ΔEN, sourced classification, polarity spectrum, caution).
  `view.js renderValenceTable` renders the new ion/mistake fields and precomputes exact `enCents`/threshold
  cents. `reference/index.astro` renders `mechanistic` as the interpretive badge; `gym/[slug].astro` badge is
  family-aware ("properties sourced (…)" for trends); `valence-table.astro` intro rewritten (and two
  whitespace-collapse artifacts fixed — trap from increment 8; a full-page scan confirms zero artifacts
  across all 16 pages).
- **Tests:** `test_reference.py` +6 (full named product, proven mistakes, VE rule, other_ions,
  lenses/bonding sourcing); `test_gym.py` +5 (shape/rotation/provenance, compare-from-data,
  order-with-named-exceptions, predict-ion-vs-table, determinism) and the categorical-menu parametrization
  now covers the trends gym.
- **Docs:** ADR-0033/0034; ROADMAP (item-5 COMPLETE block + status entry + session map); CHANGELOG; AGENTS
  current state; architecture (Q4 resolved, counters, reference/gym module rows, both gate rows); regime-map
  (periodic-trends row → gym+atlas; bonding row seeded); authoring-gyms (`periodic_trends_v1` +
  response-mode note); SOURCES (Fig 7.8); README status; this log.

## Verification (exact counts)

- **167 producer tests green** (`uv --project producer run pytest`, ~14 s; +11).
- **7 Node gates green:** validate-solutions = 3, **validate-reference = 20 objects** (1 table + 19
  concepts, now re-deriving VE/names/crossover/mistakes/bonding), **validate-gyms = 7 gyms / 70 problems**
  (trends cross-checked against the emitted table), check-ledger = 3/12, check-parity = 240 + 18,
  **check-katex = 253** (the 156 salt formulas), scan clean. **`astro build` = 16 pages** (+1:
  `/gym/periodic-trends/`). `derived/` **byte-stable** across two full rebuilds (30 files, md5-compared).
- **Gates proven non-vacuous — 8 tamper tests, each caught then restored:** corrupted valence_electrons
  (`C: 5 != re-derived 4`), salt name (`re-derived 'iron(III) oxide' != emitted 'iron(II) oxide'`), swapped
  crossover subscripts, dropped CaCO₃ mistake, broken bonding boundary, corrupted trends candidate value
  (`Li electronegativity '9999' != table '0.98'`), reversed order answer, flipped predicted-ion charge.
- **In-browser (production `astro preview`, base `/affinity`):** **Explore** — VE lens shows He 2 / C 4 /
  Ar 8 / Fe —, color scale applied, panel shows all four rows + both badges (`data-sourced
  (iupac-periodic-table)` + `interpretive — story, not proof`). **Trends** — Period-2 first-IE graph plots
  Li 520.2 … Ne 2080.7 with both dips visible (B 800.6 < Be 899.5; O 1313.9 < N 1402.3); the EN series shows
  Ne as a labeled gap ("undefined for the noble gases — never interpolated"). **Formula builder** — default
  Ca²⁺+CO₃²⁻ → CaCO₃ *calcium carbonate* with the unreduced Ca₂(CO₃)₂ mistake; Fe³⁺+O²⁻ → Fe₂O₃ *iron(III)
  oxide* with the own-charge Fe₃O₂ mistake (3×(+3) + 2×(−2) = +5). **Bonding** — H/O → ΔEN 1.24 polar
  covalent; Na/Cl → 2.23 ionic; caution + interpretive badge render. **Gym** — scored a correct group-1 EN
  comparison (explain: "K (0.82) < Na (0.93) < Li (0.98)"), then a wrong Ca²⁻ pick names "Calcium is a metal
  — it LOSES electrons…"; choices render Unicode (Ca²⁺/Ca⁺/Ca²⁻); badge reads "properties sourced
  (openstax-chemistry-2e, cordero-2008-covalent-radii, nist-ionization-energies)"; the three Atlas chips
  resolve. **Reference index** — 19 concepts; `periodic-trends` carries the interpretive badge,
  `electronegativity`/`ionization-energy` the violet rule-sourced badges. Dark theme clean; **no console
  errors or warnings**; the whitespace-collapse scan over all 16 built pages is clean.

## Deferred / open

- **Item 5 leftovers (recorded in ROADMAP):** transition-metal covalent radii (spin-state pass); ionic radii
  (per-ion, coordination-dependent); the further brief-§8.1 lenses (oxidation states, electron affinity,
  metallic character, density, abundance — each needs a data-curation session); a metal/nonmetal field
  (data-driven bonding caution + "which compound is most likely ionic" drills); acid naming in the formula
  builder (item-2 follow-up).
- **Item 6 — reaction families** is the last Phase-1 item (atlas `reaction-family` kind + classifier +
  `reaction_families_v1` gym + the acid-base neutralization lesson, which generalises the
  interactive/practice emitters past precipitation) — then the session-8 Atlas breadth audit and the
  Phase-1 definition-of-done check → owner review before Phase 2.

## State at session end — PHASE 1: items 1–5 DONE

**3 lessons + 7 gyms (70 verified problems) + 1 Valence Table (23 elements; four modes, five lenses,
156 named+verified salts) + 19 concepts (5 rule-sourced, 1 mechanistic/interpretive), 7 gates + CI + live
Pages, 167 producer tests, 16 pages.** Architecture Q4 is closed (interpretive marker, no fourth badge).
**Next up:** item 6 (reaction families), then the Atlas breadth audit and the Phase-1 definition-of-done
check before the Phase-2 review gate.

## Increment 2 — doc-hygiene: anti-bloat budgets + orientation-doc compression (Fable 5 driving)

Owner (after reviewing item 5b): confirm/strengthen the end-of-session doc protocols against bloat/drift,
advise on the session-start workflow, then end the session. Docs-only increment.

### Decisions

- No ADRs — AGENTS.md §Session close-out already mandates the sweep (ADR-0009); this increment adds
  **quantitative anti-bloat budgets** to it and executes the compression they require. Protocol wording is
  AGENTS-level maintenance, not a contract change.

### Shipped

- **Protocol:** close-out step 2 rewritten with explicit budgets — `AGENTS.md ## Current state` = counters +
  standing facts (25-line ceiling, states *what is*, never *how it got there*); `## Where this might go
  next` = one paragraph; `ROADMAP.md ## Status` = a "Now" line + one compressed line per landed item;
  landed scope blocks keep deferred/open lists only; counters live *only* in AGENTS.md; a budget-scoped
  smell test. Routing step 3 now reads the newest log's closing block first; close-out step 3 mandates that
  block ("State at session end" ending in a "Next up" pointer).
- **Compression:** `AGENTS.md ## Current state` + `## Where this might go next` cut from ~110 lines of
  accumulated narrative to ~35 (counters + standing facts + the item-6 pointer); `ROADMAP.md ## Status`
  cut from ~13 long bullets (~14 KB) to a "Now" line + 6 one-liners (~2 KB). All removed detail already
  lived in CHANGELOG.md (one entry per increment, verified) and docs/sessions/.
- **Audit (4-lens adversarial workflow: information-loss, cold-start, consistency, protocol clarity — 17
  findings, all triaged, 4 must-fix + 9 worthwhile fixed):** confirmed **no load-bearing fact lost** and the
  cold-start trace passes; fixed the stale "16 concept entries" in ROADMAP's parallel-track section (→ 19),
  the stale "five Node gates" in AGENTS §Deploy and deploy.yml's comment (→ seven), a smell-test wording
  that would have fired on every correct sweep, the budget unit (per landed *item*, not per increment), and
  three architecture.md routing gaps — the check-parity row + solution-object practice block now state the
  ADR-0032 mode/diagnostics contract, §Rendering gained the ADR-0028 seeded-shuffle rule (hydration-
  mismatch guard), and the as-built header is dated 2026-07-08.

### Verification

- Docs-only: `scan-text` gate green over the edited docs; no producer/gate/site changes (the earlier
  increment's 167 tests + 7 gates + 16 pages stand). Orientation-doc sizes after sweep: AGENTS.md ~220
  lines (was 254 pre-budget with far more in §Current state), ROADMAP.md ~220 lines (was 334).

### State at session end — item 5b shipped + doc protocol hardened

**Counters unchanged from increment 1** (3 lessons · 7 gyms / 70 problems · Valence Table 23 elements/four
modes · 19 concepts · 7 gates · 167 tests · 16 pages · CI/Pages live). Orientation docs are at budget; the
close-out protocol now enforces staying there.
**Next up:** item 6 (reaction families — ROADMAP scope block), then the Atlas breadth audit and the Phase-1
definition-of-done check before the Phase-2 review gate.

## Increment 3 — Phase-1 item 6: reaction families, end to end (Opus 4.8 driving)

Owner: "Read AGENTS.md. Continue the buildout autonomously. Work until you need my input. Multiple increments
are fine." Resumed from the flagged next item — **item 6, reaction families** — and landed it in four
committed sub-increments (6a classifier, 6b Atlas kind, 6c gym, 6d neutralization lesson). One escalation
point hit: the item-6 scope's "generalise the interactive/practice emitters" for the neutralization lesson —
put to the owner (static vs full lesson vs stop); owner chose the **full lesson (sliders + practice)**.
Three new ADRs; no honesty-model / house-convention / scope change (all scoped, additive schema growth).

### Decisions (full rationale in DECISIONS.md)

- **ADR-0035 — reaction classifier + reaction-family Atlas kind.** `classify_reaction` over the balanced
  phased formulas + injected sourced datasets (solubility / acid-base / decomposition), most-specific-first;
  **redox by the free-element signature** (no oxidation numbers — Phase 2). Two curated datasets
  (`data/acids-bases.toml`, `data/decomposition.toml`, `chemkernel.reactivity`, composition-checked). The
  `reaction-family` Atlas kind: 7 families / 21 examples, each engine-balanced + engine-classified (the
  producer refuses a mis-filed example), with net-ionic views. `verifyBalance` extracted to shared
  `balancecheck.mjs`; `validate-reference` re-proves balance + redox + label consistency in Node.
- **ADR-0036 — `reaction_families_v1` gym.** classify-the-family + name-the-spectators, both categorical;
  reactions engine-classified at generation; definitional family distractors, over-inclusion/reacting-ion
  spectator distractors. `validate-gyms` re-proves molecular + net balance + spectator-absence.
- **ADR-0037 — acid-base neutralization lesson (emitter generalisation).** The reported product is the
  net-ionic product of any phase — `result.precipitate` (solid) or general `result.product` + `result.salt`
  (precipitation lessons byte-identical); `interactive.py` drops its must-be-solid guard (the switch was
  always net-ionic-level); `practice.py` parametrised. No solubility claim. Player: water/salt card, Beaker
  tab gated to solids, ExtentBar generalised; the three solution gates read `precipitate ?? product`.

### Shipped

- **Data/producer:** `data/acids-bases.toml`, `data/decomposition.toml`; `chemkernel/reactivity.py`
  (AcidBase/Decomposition, self-checked); `reaction.py` (+`classify_reaction`, `redox_free_elements`);
  `reference.py` (+`build_reaction_family`); `gym.py` (+`reaction_families_v1`, two kinds); `build.py`
  (result generalised — precipitate ?? product + salt; provenance/percent-yield guarded; kind-dispatch for
  reaction-family Atlas entries); `interactive.py` (product any phase); `practice.py` (family + product_noun).
- **Contracts:** `schemas/reaction-family.schema.json` (new); `gym.schema.json` (+2 kinds + family/net_species/
  net_coefficients/spectators + `reaction_classes` provenance); `solution.schema.json` (result.precipitate +
  provenance.solubility now optional; shared `reportedProduct` $def; result.product/salt).
- **Gates:** shared `scripts/validate/balancecheck.mjs` (`verifyBalance` + `redoxFreeElements`, imported by
  validate-gyms + validate-reference); `validate-reference` (reaction-family dispatch + re-derivation);
  `validate-gyms` (`verifyReactionFamily`); `validate-solutions` / `check-ledger` / `check-parity`
  (precipitate ?? product); `check-katex` (reaction-family LaTeX).
- **Content:** `reference/reactions/{combustion,synthesis,decomposition,single-replacement,precipitation,
  acid-base,gas-evolution}.toml` (7 families); `gyms/reaction-families/reaction-families.gym.toml`;
  `problems/neutralization/hydrochloric-sodium-hydroxide.problem.toml`.
- **Player:** `reference/reactions.astro` (new) + Atlas-index card; `view.js` (`renderReactionFamily` +
  general product in `renderSolution`); `SolutionPlayer.svelte` (water/salt card, Beaker gated, ExtentBar
  prop); `ExtentBar.svelte` (product label); `gym/[slug].astro` (reaction-family badge + chips).
- **Tests:** `test_reactivity.py` (new, +6); `test_reaction.py` (+classifier corpus/redox); `test_reference.py`
  (+5 reaction-family); `test_gym.py` (+5 reaction-families); `test_build.py` (+2 neutralization);
  `test_interactive.py` (+1 water product); `test_oracle.py` (+17 chempy corpus balances).
- **Docs:** ADR-0035/0036/0037; CHANGELOG (item-6 entry); ROADMAP (status + item-6 LANDED + session-map #7 +
  parallel-track count); AGENTS current-state (counters + standing facts) + where-next; architecture
  (as-built counters, reaction/reactivity/gym/reference module rows, six gate rows, solution-object result);
  regime-map (reaction-classes row goes lesson+gym+atlas); SOURCES (acids-bases + decomposition);
  authoring-gyms (`reaction_families_v1`); authoring-problems (non-precipitation lesson shape); README; log.

### Verification (exact counts)

- **236 producer tests green** (`uv --project producer run pytest`, ~14 s; +69 from 167).
- **7 Node gates green:** validate-solutions = 4, **validate-reference = 27 objects** (1 table + 19 concepts
  + 7 reaction families), **validate-gyms = 8 gyms / 80 problems**, check-ledger = 4/16, **check-parity =
  320 closed-form points + 24 practice answers**, **check-katex = 381** (+121 reaction-family LaTeX), scan
  clean. **`astro build` = 19 pages** (+3: `/reference/reactions/`, `/gym/reaction-families/`,
  `/lessons/hydrochloric-sodium-hydroxide/`). `derived/` **byte-stable** across two full rebuilds (39 files).
- **Gates proven non-vacuous — tamper tests, each caught then restored:** flipped example redox flag
  (`redox false != re-derived true [H,Zn]`), broken example coefficient (`Cl not conserved`), mis-filed
  family (`family 'acid-base' != entry family 'precipitation'`), spectator that reacts (`Ca^2+ … appears in
  the net ionic equation`), broken gym coefficient (`Zn not conserved`), corrupted neutralization water mass
  (`H2O mass 9.999 != moles × molar mass`; parity `default mass 0.027 != 9.999`).
- **Oracle (ADR-0026):** chempy independently balances all 17 reaction-families-corpus reactions.
- **In-browser (dev preview):** **Reactions Atlas** — all 7 families in teaching order, correct redox badges
  (combustion/synthesis/single-rep = redox; decomposition = "varies"; precip/acid-base/gas = not redox),
  net-ionic views on the 4 replacement families only, H₂CO₃ subscripted in the gas-evolution misconception.
  **Reaction-families gym** — scored a correct classify (Zn + 2 HCl → single replacement) with the
  free-element redox reason; badge "reaction classes sourced (openstax-chemistry-2e)"; concept chips resolve.
  **Neutralization lesson** — cards "Mass of H₂O formed 0.027 g / Salt formed (NaCl) 0.088 g / Limiting NaOH
  / Left HCl 0.5 mmol"; **no Beaker tab**; only machine-checked + model-assumed badges; the Extent slider
  flips the limiting reagent OH⁻ → H⁺ as NaOH rises (0.027 → 0.036 g water, 7 mmol OH⁻ excess). No console
  errors.

### Deferred / open (recorded in ROADMAP item-6 block)

Full oxidation-number assignment (Phase-2 redox); predict-products gym drills; a generic double-replacement
Atlas entry (no driving force = not a real reaction); a gas-evolution *lesson* and the diprotic
coefficient-story neutralization (machinery handles both — second lessons, not new code); further gas-forming
intermediates (sulfurous acid needs the sulfite ion curated).

### State at session end — PHASE 1: items 1–6 all DONE

**4 lessons (2 precipitation + 1 percent-yield + 1 acid-base neutralization) + 8 gyms (80 verified problems)
+ 1 Valence Table (23 elements; four modes) + 19 concepts + 7 reaction families (21 engine-classified example
reactions), 7 gates + CI + live Pages, 236 producer tests, 19 pages.** The lesson emitters now handle any
two-solution reaction whose net-ionic product is a single species (solid precipitate or water). Reaction
classification is the shared instrument behind the Atlas, gym, and lesson; redox is the free-element
signature only.
**Next up:** the **Atlas breadth audit** (session-map #8 — species-atlas + formula-sheet entry kinds, fill
every phase-0/1 regime-map row), then the **Phase-1 definition-of-done check → owner review** before the
Phase-2 gate.

## Increment 4 — session-map #8: the Atlas breadth audit + Phase-1 DoD (Opus 4.8 driving)

Owner: "Read AGENTS.md. Continue the buildout autonomously. Work until you need my input. Multiple increments
are fine." Resumed from the flagged next item — the **Atlas breadth audit**. Landed the species Atlas kind,
filled the last empty regime-map row, fixed a cross-link integrity defect the audit surfaced, and confirmed the
**Phase-1 definition-of-done is met** — the review gate before Phase 2. One ADR; no honesty-model /
house-convention / scope change (additive schema growth; the DoD is a checklist, not a scope decision).

### Decisions (full rationale in DECISIONS.md)

- **ADR-0038 — species Atlas kind; reaction-family cross-links restored.** (1) The `species` kind
  (`schemas/species.schema.json`, `build_species_entry`): composition (per-element count + sourced weight +
  subtotal), signed charge, and molar mass all **derived from the authored phase-less formula** — never
  asserted; names/phase/prose authored + labeled. The producer refuses off-dataset elements, class↔charge
  mismatches, a multi-element monatomic-ion, or a phase baked into the formula. `validate-reference` re-parses
  the formula in Node (`formula.mjs`), re-sums the molar mass from the Valence Table's sourced weights, and
  re-checks class/charge + edge resolution. (2) An `atomic-mass` concept fills the last empty Phase-0/1
  regime-map row (*atoms, isotopes, average atomic mass*) — no new source. (3) **Trap-#4 fix:** all 7
  reaction families had shipped with empty `related`/`lessons` (bare keys absorbed into the last `[[examples]]`
  table); moved ahead of the array-of-tables headers + a producer guard that rejects an example/misconception
  carrying an unexpected key.

### Shipped

- **Contracts:** `schemas/species.schema.json` (new). **Producer:** `reference.py`
  (+`build_species_entry`; the reaction-family builder's trap-#4 guard); `build.py` (kind-dispatch for
  `species`). **Content:** `reference/species/*.toml` (14 — water, calcium/sodium/zinc carbonates, the two
  chlorides, HCl, NaOH, CO₂, Ca₃(PO₄)₂, O₂, H₂, carbonate + hydroxide ions); `reference/concepts/atomic-mass.toml`;
  the 7 `reference/reactions/*.toml` cross-links relocated + acid-base/precipitation lessons wired.
- **Gates:** `validate-reference.mjs` (species branch — re-parse + re-sum + class/charge; the Valence-Table
  atomic-weight map); `check-katex.mjs` (species symbol + inline math). **Player:** `src/pages/reference/species.astro`
  (new — species grouped by class, composition tables, machine-checked + data-sourced badges, cross-links
  routed to the right Atlas page by kind); `renderSpecies` in `view.js`; Species card + count on the Atlas
  index. `.claude/launch.json` gained `autoPort` (coexist with another chat's dev server).
- **Tests:** `test_reference.py` +7 (6 species — derived composition/molar mass, parenthesized group, ion
  charge, off-dataset reject, class/charge reject, phase-in-formula reject — plus the trap-#4 guard).
- **Docs:** ADR-0038; ROADMAP (status + session-map #8 done + DoD block MET + parallel-track counts);
  AGENTS current-state (counters + standing facts + where-next) ; CHANGELOG (session-8 entry); architecture
  (as-built counters, species builder row, validate-reference + check-katex rows); regime-map (atoms row
  filled + mole/ions rows); README; this log.

### Verification (exact counts)

- **243 producer tests green** (`uv --project producer run pytest`, ~14 s; +7 from 236).
- **7 Node gates green:** validate-solutions = 4, **validate-reference = 42 objects** (1 table + 20 concepts +
  7 reaction families + 14 species), validate-gyms = 8 gyms / 80, check-ledger = 4/16, check-parity = 320 +
  24, **check-katex = 419** (+species LaTeX), scan clean. **`astro build` = 20 pages** (+1: `/reference/species/`).
  `derived/` **byte-stable** across two rebuilds (54 files, md5-compared; +15 from 39).
- **Species gate proven non-vacuous — 8 tamper tests, each caught then restored:** corrupted molar mass
  (`'99.9' != Σ subtotals 18.015`), composition count (`H is 3 but the formula parses to 2`), atomic weight
  (`'9.9' != table '1.0080'`), flipped ion charge (`2 != re-parsed -2`), class↔charge mismatch, dangling
  `related` edge, dangling `reactions` link, plus the trap-#4 producer guard (pytest — an example carrying a
  `lessons` key raises "unexpected key").
- **In-browser (production `astro preview`, base `/affinity`):** `/reference/species/` renders all 14 cards in
  three groups (Compounds / Elements / Polyatomic ions); each shows the KaTeX symbol, the composition table
  (element × count × weight = subtotal) with the molar mass footer (H₂O 18.0150, Ca₃(PO₄)₂ 310.174, CO₃²⁻
  60.008 g/mol), the machine-checked + data-sourced badges, and cross-links; the carbonate-ion card shows the
  "charge -2" chip. The Atlas index shows the Species card ("14 compounds…"). The **reaction-family cross-links
  now render** (acid-base → its neutralization lesson; precipitation → both lessons) — the trap-#4 fix
  confirmed. Dark theme clean; **no console warnings or errors**.

### Deferred / open (recorded in ROADMAP + ADR-0038)

The formula/equation-sheet Atlas kind (mostly model-bearing → opens with Phase 2); monatomic-ion species
entries (the ion table + Valence Table already cover them; the schema permits the class); an
average-atomic-mass gym (needs per-isotope abundance data curated).

### State at session end — PHASE 1 COMPLETE (pending owner review)

**4 lessons + 8 gyms (80 verified problems) + 1 Valence Table (23 elements; four modes) + 20 concepts + 7
reaction families (21 examples) + 14 species entries, 7 gates + CI + live Pages, 243 producer tests, 20
pages.** The Chemical Atlas now carries all four brief-§10 entry kinds bar the Phase-2 formula sheet, and every
Phase-0/1 regime-map row shows coverage — the two structural Phase-1 definition-of-done criteria, both met.
**Next up:** **owner review of Phase 1** — the gate before Phase 2 (opening Phase 2, and the formula/equation
sheet that comes with it, is the owner's call). Always-in-season otherwise: Atlas breadth-fill, further lessons
(gas-evolution / diprotic neutralization), an average-atomic-mass gym (needs isotope data), docs-only work.

## Increment 5 — Phase 2 OPENED: the formula/equation-sheet Atlas kind (Opus 4.8 driving)

Owner (new session): "Read AGENTS.md. Continue the buildout autonomously. Work until you need my input. Multiple
increments are fine. **You are OK to open Phase 2**." The owner-review gate cleared — opened **Phase 2** (the
model-bearing tier) on gases + thermochemistry (brief §17 item 7) by landing the **formula/equation-sheet Atlas
kind** (the fourth brief-§10 kind, deferred through Phase 1). One ADR (ADR-0039, a phase-opening decision). No
honesty-model change (the three badges stand; dimensional homogeneity is a machine-check *marker*, the model-assumed
badge does the disclosure); additive schema growth; new `dimension.py` diverges from SymPy `dims.py` for
Node-parity (ADR-0001, recorded).

### Decision (full rationale in DECISIONS.md)

- **ADR-0039 — open Phase 2; the `formula` Atlas kind, verified by dimensional homogeneity.** (1) A reference
  relation's honesty = **machine-checked dimensional homogeneity**, not a proof of the relation: each term (side)
  is a monomial over the variables, and the producer refuses to emit unless every term reduces to one SI dimension
  vector. Model-exact relations (PV=nRT) carry the model-assumed badge + **must disclose an assumption**;
  ledger-exact ones (n=m/M) carry the machine-checked badge. (2) A **native integer-vector dimension engine**
  (`chemkernel.dimension`), NOT SymPy `dims.py` — because first-course relations are monomials, homogeneity is
  integer-vector arithmetic, and the payoff is **pure-Node re-derivation parity** (the producer emits per-variable
  dimension vectors + term factor powers; `validate-reference.mjs` via a shared `dimension.mjs` re-computes and
  re-checks). Kept separate from the Decimal `units.py` engine (ADR-0015). (3) Sourced constants (R, N_A) threaded
  from `data/constants.toml`, never hard-coded; the **gas constant R** registered under `bipm-si-2019`.

### Shipped

- **Producer:** `chemkernel/dimension.py` (new — SI 6-vector `[mass, length, time, amount, temperature, current]`,
  a definitional unit→dimension table, `unit_dimension`/`term_dimension`/`dimension_name`); `reference.py`
  (+`build_formula_entry` — computes each term's dimension, refuses non-homogeneous, threads constants, requires a
  model-exact assumption); `data.py` (constants now carry their unit label; `constant_unit(key)`); `build.py`
  (kind-dispatch for `formula`).
- **Data:** `data/constants.toml` gained the **gas constant R** (`0.0820573660809596 L*atm/(mol*K)`, note: exact
  SI R = 8.314462618 J/(mol·K), unit-converted).
- **Contracts:** `schemas/formula.schema.json` (new — variables/units/dimension vectors, terms/factors, assumptions,
  constants, dimension `$def` fixed length 6).
- **Gates:** `scripts/validate/dimension.mjs` (new — the pure-Node mirror of the unit table); `validate-reference.mjs`
  (formula branch: re-derive each variable's dimension from its unit, each term's from those + powers, require one
  shared dimension, model-exact discloses an assumption, resolve edges/lessons/sources); `check-katex.mjs` (formula
  statement + rearrangements + inline summary/domain math).
- **Content:** `reference/formulas/*.toml` (8 — mole–mass, molarity, dilution, Avogadro's-number, percent yield;
  ideal gas law, combined gas law, calorimetry).
- **Player:** `src/pages/reference/formulas.astro` (new — teaching-order groups, the dimensional-check line, the
  variable/unit table, disclosed model assumptions, rearrangements, cross-links); `view.renderFormula`;
  `reference/index.astro` gained the formula-sheet card.
- **Tests:** `test_dimension.py` (new, +6 — unit signatures, unknown-unit refuse, PV=nRT reduces to energy,
  homogeneity of mole-mass/calorimetry, name fallback); `test_reference.py` +6 (derived homogeneous dimension, R
  threaded from data, refuse non-homogeneous / unknown unit / model-exact-without-assumption, all 8 authored build).
- **Docs:** ADR-0039; ROADMAP (status + Phase-2 section opened with the opening-tier scope + DoD sketch +
  parallel-track all-four-kinds); AGENTS current-state (phase state + counters + standing facts) + where-next;
  CHANGELOG (Phase-2 entry); architecture (as-built header/counters, dimension.py + formula-builder module rows,
  validate-reference + check-katex gate rows, Phase-2 row); regime-map (gases + thermo rows → atlas; mole/solutions/
  percent-yield rows note the formula-sheet backfill); SOURCES (bipm-si-2019 now covers R); this log.

### Verification (exact counts)

- **255 producer tests green** (`uv --project producer run pytest`, ~15 s; +12 from 243).
- **7 Node gates green:** validate-solutions = 4, **validate-reference = 50 objects** (1 table + 20 concepts + 7
  reaction families + 14 species + **8 formulas**), validate-gyms = 8 gyms / 80, check-ledger = 4/16, check-parity =
  320 + 24, **check-katex = 461** (+42 formula LaTeX), scan clean. **`astro build` = 21 pages** (+1:
  `/reference/formulas/`). `derived/` **byte-stable** — only the 8 new formula files added, no existing derived changed.
- **Formula gate proven non-vacuous — 7 tamper tests, each caught then restored:** corrupted term dimension
  (`term 'PV' dimension … != re-derived`), corrupted variable dimension (`R … != re-derived`), forced
  non-homogeneity (`not homogeneous — term 'nRT' is [1,2,-2,…] but another term is [1,5,-2,…]`), corrupted total
  dimension (`emitted dimension … != re-derived common`), emptied model assumptions (`model-exact … discloses no
  assumption`), dangling related edge, unregistered source.
- **In-browser (dev preview, base root):** `/reference/formulas/` renders all 8 in two groups (Amount/mass/solutions;
  Gases & energy); ideal gas law shows the KaTeX `PV=nRT`, the **model-assumed** badge, the check line "✓
  dimensionally homogeneous — PV = nRT both reduce to **energy**, machine-verified", R = 0.0820573660809596
  L·atm/(mol·K) (bipm-si-2019), and both disclosed assumptions; mole-mass shows "n = m / M both reduce to **amount**".
  Dark theme clean; the Atlas index shows the 4th card ("Formula & equation sheet →"). **No console errors or warnings.**

### Deferred / open (recorded in ROADMAP + ADR-0039)

The numeric gas-law **computation** — the `units.py` pressure/temperature/energy extension (ADR-0015; °C→K as an
affine boundary conversion) + a `gas_laws_v1` gym (numeric free-entry, regime-2 model-exact-then-rounded, gate
re-derives numerically) — and a **gas-stoichiometry lesson** (the ledger drives a gas volume; needs `build.py`
generalised past two-solution double-displacement). Thermochemistry (energy ledger, specific-heat data, Hess);
further sheet entries (Hess, pH, K, ΔG, Nernst) land with their topics.

### State at session end — PHASE 2 OPEN (formula sheet landed)

**4 lessons + 8 gyms (80 problems) + 1 Valence Table (23 elements) + 20 concepts + 7 reaction families (21
examples) + 14 species + 8 formula-sheet entries, 7 gates + CI + live Pages, 255 producer tests, 21 pages.** The
Chemical Atlas now carries **all four brief-§10 entry kinds**. A reference relation's honesty is machine-checked
dimensional homogeneity (native `chemkernel.dimension` SI-vector engine, re-derived in pure Node) + the
model-assumed badge disclosing regime-2 assumptions.
**Next up:** the **numeric gas-law instrument** — extend `units.py` with pressure/temperature/energy dimensions
(ADR-0015 deferral) + a `gas_laws_v1` gym (PV=nRT + combined-gas-law), then a **gas-stoichiometry lesson**. The
formula sheet is the reference backbone both link to.

## Increment 6 — Phase-2 gas-laws gym; the units engine gains pressure/temperature (Opus 4.8 driving)

Continued the Phase-2 buildout from the flagged next item — the **numeric gas-law instrument**. Landed the
`gas_laws_v1` gym and the `units.py` extension it needed. One ADR (ADR-0040). No honesty-model change (the three
badges stand — a regime-2 gym discloses its model under the model-assumed badge, as a model-exact lesson does);
additive schema growth; the units.py `Dim` extension is the ADR-0015-anticipated "add basis components then".

### Decision (full rationale in DECISIONS.md)

- **ADR-0040 — gas-laws gym; units engine gains pressure/temperature; the first model-exact-then-rounded numeric
  answer.** (1) `units.py` `Dim` grows from 3 fields to 5 (pressure, temperature; ADR-0015's deferred extension);
  `atm`/`K`/`L*atm/(mol*K)` registered; every answer is computed **through** the units engine so the dimensions
  are certified (mol·L·atm·mol⁻¹·K⁻¹·K / atm → L). Temperature is absolute (K); °C is NOT a registry unit (affine,
  not ratio) — a °C given is converted at the boundary (K = °C + 273.15) and forgetting it is a named diagnostic.
  (2) The answer is **model-exact-then-rounded** (R is non-terminating): 3-sig-fig display / 4-sig-fig value, the
  gate re-derives PV=nRT numerically within 0.5% — not a weakening of ledger exactness (ADR-0013), a gas-law result
  is a rounded physical quantity. (3) Each state is generated **consistent** (n, P, T chosen; V computed) so no
  absurd values ship. (4) The gym **discloses the ideal-gas model** in an `assumptions` block under the
  model-assumed badge (the three-badge model applied to a gym for the first time).

### Shipped

- **Producer:** `units.py` (`Dim` +pressure/temperature; `atm`/`K`/`L*atm/(mol*K)` registry + canonical labels);
  `gym.py` (+`gas_laws_v1` — `_gas_ideal_problem`/`_gas_combined_problem` computing through `Quantity`, consistent
  states, `_sigdec`/`_round_sig`/`_gnum` rounding helpers, `_rounded_response` diagnostics, `_FAMILY_ASSUMPTIONS`,
  the `constants` provenance key). `data/constants.toml` already carried R (from increment 5).
- **Contracts:** `schemas/gym.schema.json` (+`gas_ideal`/`gas_combined` kinds, a `gas` derivation shape, the
  `constants` provenance key, an optional top-level `assumptions` block).
- **Gate:** `validate-gyms.mjs` (+`verifyGasLaw` — re-derives PV=nRT / the combined law numerically from the
  emitted state + R, 0.5% tolerance; the two kinds added to `NUMERIC_KINDS` + the dispatch).
- **Content:** `gyms/gas-laws/gas-laws.gym.toml` (seed 2718281, 10 problems).
- **Player:** `gym/[slug].astro` (a model-assumed badge + a disclosure line for a gym with `assumptions`; the
  `gas constant sourced (…)` source label; formula-kind reference_links resolve to `/reference/formulas/#id`;
  chip labels prefer the real Atlas title); `DimensionalGym.svelte` (a gas-aware chain caption — otherwise the
  free-entry numeric path is unchanged).
- **Tests:** `test_units.py` +2 (the gas-law product lands in volume; pressure/temperature are distinct dims);
  `test_gym.py` +5 (shape + model badge, the answer reproduces PV=nRT / the combined law, answers physical, the
  °C→kelvin diagnostic, determinism).
- **Docs:** ADR-0040; ROADMAP (status + opening-tier gas-gym LANDED + gas-lesson NEXT); AGENTS current-state +
  standing facts + where-next; CHANGELOG; architecture (as-built counters, units.py + gym.py rows, validate-gyms
  row); regime-map (gases row → gym+atlas); authoring-gyms (`gas_laws_v1`); README; this log.

### Verification (exact counts)

- **262 producer tests green** (`uv --project producer run pytest`, ~15 s; +7 from 255).
- **7 Node gates green:** validate-solutions = 4, validate-reference = 50, **validate-gyms = 9 gyms / 90 problems**
  (+1 gym / +10), check-ledger = 4/16, check-parity = 320 + 24, check-katex = 461, scan clean. **`astro build` =
  22 pages** (+1: `/gym/gas-laws/`). `derived/` **byte-stable** — only `gas-laws.gym.json` added; no existing
  derived changed (the `assumptions`/`constants` additions are gas-family-only).
- **Gas gate proven non-vacuous — 5 tamper tests, each caught then restored:** corrupted answer value
  (`answer 999 != re-derived 450.29…`), corrupted a state input (`!= re-derived 675.44…`), wrong R
  (`!= re-derived 4.44…`), a numeric problem given a choice menu (`a numeric answer must not be a
  multiple-choice menu`), a diagnostic within 3% of the answer (`within 3% … could mis-flag a correct entry`).
- **In-browser (dev preview):** `/gym/gas-laws/` shows the three badges (`10 machine-verified problems`,
  `gas constant sourced (bipm-si-2019)`, **model-assumed**) + the disclosure ("The gas behaves ideally… A
  disclosed model, not a law of algebra. The arithmetic is machine-checked and the answer is reported to 3
  significant figures."); the first prompt reads "An ideal gas sample has 0.5 atm, 7.39 L, 0.1 mol… what is the
  temperature (K)?"; entering **450** → "✓ Correct — 450 K" with the rearrange-and-substitute chain; entering
  the wrong-R value **4.44** → "Used R = 8.314 (the SI value in J/mol·K)…"; the concept chips resolve to the
  ideal-gas-law and combined-gas-law **formula-sheet** entries. No console errors.

### Deferred / open (recorded in ROADMAP + ADR-0040)

The **gas-stoichiometry lesson** (the extent ledger drives a gas volume via PV=nRT — needs `build.py` generalised
past two-solution double-displacement) is the next increment; then thermochemistry (the energy ledger — q=mcΔT
with specific-heat data curated, ΔH_rxn·ξ, Hess). `kPa`/`torr` units + °C display niceties; energy/charge
dimensions (thermo/electrochem); particle-count drills (now have both prerequisites — Avogadro datum + this
rounding pattern — but still want scientific-notation display).

### State at session end — PHASE 2 OPEN (formula sheet + gas-laws gym landed)

**4 lessons + 9 gyms (90 problems) + 1 Valence Table (23 elements) + 20 concepts + 7 reaction families (21
examples) + 14 species + 8 formula-sheet entries, 7 gates + CI + live Pages, 262 producer tests, 22 pages.** Phase
2 has both halves of its gas opener — the reference (formula sheet, dimensional homogeneity) and the practice
(gas-laws gym, model-exact-then-rounded). The units engine now does pressure/temperature; a regime-2 gym discloses
its model under the model-assumed badge.
**Next up:** the **gas-stoichiometry lesson** — the vertical slice where the ledger drives a gas volume (e.g. a
metal + acid → H₂ collected at stated P, T). Generalise `build.py`/`interactive.py`/`practice.py` past the
two-solution double-displacement shape (the ADR-0037 emitter generalisation is the template).

## Increment 7 — Phase-2 gas-stoichiometry lesson: the ledger drives a gas volume (Opus 4.8 driving)

Owner: "Read AGENTS.md. Continue the buildout autonomously. Work until you need my input. Multiple increments are
fine." Resumed from the flagged next item — the **gas-stoichiometry lesson** — and landed its **vertical slice**
(the ledger → a gas volume via PV=nRT), generalising `build.py` past the two-solution double-displacement shape.
One ADR (ADR-0041). No honesty-model / house-convention / scope change (additive schema growth; the volume is
model-exact-then-rounded, the ADR-0040 pattern applied to a lesson).

### Decision (full rationale in DECISIONS.md)

- **ADR-0041 — gas-stoichiometry lesson; `build.py`'s third reported-product shape.** (1) A **weighed-mass given**:
  `_moles_and_chain` gains a `mass_g` branch (grams ÷ molar mass → moles, dimension certified through the units
  engine, still must terminate — 3.269 g Zn = 0.0500 mol exactly). (2) A **`result.gas` block**: when a spec carries
  a `[conditions]` table (pressure + temperature + `gas_species`), the reported product is that gas and the block
  adds its **volume via `(n·R·T/P).to("L")`** through the units engine from the ledger's exact moles + the sourced R
  — **model-exact-then-rounded** (3 sig figs) under the model-assumed badge, with `molar_volume` (RT/P) emitted to
  name the 22.4-L-at-STP misconception. Honesty layered, not mixed: moles ledger-exact, volume model-exact.
  (3) **Free elements skip the solubility phase-check** (a metal has no solubility verdict). Deferred to a second
  increment: the lesson's interactive + generated practice (the `interactive`/`practice` generalisation past the
  cation/anion shape).

### Shipped

- **Producer** (`build.py`): `_moles_and_chain(given, data, ctx)` mass branch; `_gas_volume_block` (V=nRT/P through
  the units engine, °C→K boundary, `_sigfig` display helper); a gas branch in the result dispatch (`result.gas` +
  the gas as `result.product` + the dissolved `result.salt`); the gas-volume dimensional chain appended; the
  regime default **pinned to the original three facets** (`_DEFAULT_REGIMES`) so adding the `gas_behavior` facet
  shifts no existing lesson; the solubility phase-check skips free elements; `constants` added to provenance sources
  for a gas lesson.
- **Contracts:** `schemas/solution.schema.json` — the `result.gas` block (species/phase/moles/pressure/temperature/
  R/volume/molar-volume), `provenance.sources.constants`; `given.mass_g` was already reserved.
- **Gates:** `check-ledger.mjs` re-derives **V=nRT/P** numerically (0.5% tol) + the °C→K boundary + gas moles vs the
  reported product; `validate-solutions.mjs` ties `result.gas` to a model-exact regime + a disclosed model
  assumption + the sourced constant.
- **Content:** `problems/gas-stoichiometry/zinc-hydrochloric-hydrogen.problem.toml` (Zn + 2 HCl → ZnCl₂ + H₂;
  3.269 g Zn + 120.0 mL of 1.00 M HCl; collected at 1.00 atm, 25.00 °C; the 22.4-L misconception; 16 reference_links).
- **Player:** `SolutionPlayer.svelte` — the gas-volume headline card (model-assumed badge) + the gas card spelling
  out moles × RT/P = volume + the STP-trap note; the misconception now refutes via the authored `refuted_by` token
  with the verified gas numbers. `view.js` decorates `result.gas`. `lessons/[slug].astro` — concept chips **route by
  entry kind** (concept / reaction-family / species / formula) so all 16 links resolve (prior lessons benefit too).
- **Tests:** `test_build.py` +1 (`test_gas_stoichiometry_lesson` — the mass→mol chain, Zn limits, the gas block, V
  re-derived, the honesty layering, no interactive).
- **Docs:** ADR-0041; CHANGELOG; ROADMAP (status + opening-tier gas-lesson LANDED); AGENTS current-state + standing
  facts + where-next; architecture (as-built counters, build.py row, validate-solutions + check-ledger rows, result
  block); regime-map (gases row → lesson+gym+atlas); authoring-problems (mass givens + the gas-stoichiometry shape);
  README; this log.

### Verification (exact counts)

- **263 producer tests green** (`uv --project producer run pytest`, ~14 s; +1 from 262).
- **7 Node gates green:** validate-solutions = **5**, validate-reference = 50, validate-gyms = 9 gyms / 90,
  **check-ledger = 5 ledgers / 20 rows + 1 gas volume re-derived from PV=nRT**, check-parity = 320 + 24, **check-katex
  = 468** (+7 gas-lesson LaTeX), scan clean. **`astro build` = 23 pages** (+1: `/lessons/zinc-hydrochloric-hydrogen/`).
  `derived/` **byte-stable** across two rebuilds — only the new lesson added; **no existing solution changed** (git
  confirms only `derived/gas-stoichiometry/` is new).
- **Gates proven non-vacuous — 6 tamper tests, each caught then restored:** corrupt gas volume (`1.223 → 9.99`,
  `!= re-derived nRT/P = 1.223270`), break °C→K (`temperature_K 298.15 → 300`, `25.00 °C + 273.15 != 300 K`), corrupt
  gas moles (`!= reported product moles`), **bake in the 22.4 STP molar volume** (`22.4 != re-derived RT/P =
  24.465404`), drop the model-exact regime (`no model-exact regime — the ideal-gas volume is regime-2`), drop the
  constants source.
- **In-browser (dev preview):** the lesson renders — the **volume card** "Volume of H₂ gas [ideal gas] 1.22 L at
  1.00 atm, 25.00 °C" (amber/model), the gas card **0.05 mol × 24.5 L/mol = 1.22 L**, the three dimensional chains
  (**3.269 g → 0.05 mol**, 120.0 mL → 0.12 mol, **0.05 mol → 1.22 L via PV=nRT**), the misconception refuted "22.4
  L/mol is the molar volume only at STP … here RT/P = 24.5 L/mol, so H₂ occupies 1.22 L — not 1.12 L", and all **16
  concept chips resolve** (Single replacement → reactions, Ideal gas law / Moles from mass → formulas). No console
  errors; dark + light clean.

### Deferred / open (recorded in ROADMAP + ADR-0041)

The gas-stoichiometry **interactive** (mass/volume/molarity sliders → the gas volume, parity-verified) + **generated
practice** — the `interactive.py`/`practice.py` generalisation past the cation/anion double-displacement shape (the
next increment); collecting the gas **over water** (subtract the water-vapor pressure — needs a curated
vapor-pressure table); `kPa`/`torr` units. Then thermochemistry (the energy ledger).

### State at session end — PHASE 2 OPEN (gas-stoichiometry lesson landed, 7a)

**5 lessons (2 precipitation + 1 percent-yield + 1 acid-base neutralization + 1 gas-stoichiometry) + 9 gyms (90
problems) + 1 Valence Table (23 elements) + 20 concepts + 7 reaction families (21 examples) + 14 species + 8
formula-sheet entries, 7 gates + CI + live Pages, 263 producer tests, 23 pages.** `build.py` now emits three
reported-product shapes (precipitate / water / gas); the gas lesson's moles are ledger-exact and its volume is
model-exact-then-rounded (PV=nRT), re-derived by check-ledger. The lesson renders statically (no interactive/practice
yet — the double-displacement-only shape).
**Next up:** the gas lesson's **interactive + generated practice** (generalise `interactive.py`/`practice.py` past
the cation/anion shape — sliders for the metal mass + acid volume/molarity driving the gas volume; a gas practice
family). Then **thermochemistry** (q=mcΔT, ΔH_rxn·ξ, Hess).

## Increment 7b — gas-stoichiometry generated practice (Opus 4.8 driving)

Continued straight on from 7a: the gas lesson's **generated practice** (the lower-risk, higher-value half of the
deferred work — the slider interactive needs its own frontend component and stays deferred). Same ADR (ADR-0041 —
recorded as its "second increment"). No new decision; the interesting move is **decoupling practice from the
interactive block** for the first time.

### Shipped

- **Producer** (`practice.py`): `generate_gas_practice` — deterministic (metal-mole + stronger-acid grids so the
  limiting reagent genuinely switches) variants → free-entry **volume** (PV=nRT) + **leftover** (mmol), categorical
  **limiting reagent**. Volume distractors: the **22.4-L-at-STP** slip + sizing from the excess reactant. The set
  carries a `gas` block of reaction constants (metal molar mass + coefficients, R, T, P) so the answers re-derive in
  Node without an interactive. `build.py` wires it (metal = the free-element reactant; a gas lesson with `[practice]`
  and no interactive → gas practice). Seed 20260711 (scanned for a Zn-limiting AND HCl-limiting categorical).
- **Contracts:** `schemas/solution.schema.json` — a `practice.gas` constants block; a `volume` question kind;
  `practice.given` relaxed (species-only required, `mass_g` allowed).
- **Gate:** `check-parity.mjs` — a `checkGasPractice` branch re-derives volume (0.5%) / leftover (exact) / limiting
  from `practice.gas` + the per-question args; the response-mode validation (ADR-0032) extracted to a shared
  `validatePracticeMode` (now also covers the `volume` kind). A practice block with no interactive is no longer an
  automatic failure — a `practice.gas` block routes to the gas re-derivation.
- **Player:** `view.js` — practice-text prettification now works without an interactive block (lesson species
  tokens). The generic `PracticeQuestion` island renders it unchanged (no new frontend).
- **Tests:** `test_build.py` — the gas test now asserts the practice shape (family, gas constants, all three kinds,
  the limiting switch, the mode split) + a determinism test.
- **Docs:** ADR-0041 (second-increment note); CHANGELOG (practice bullet); ROADMAP (gas-lesson block + status);
  AGENTS (counters 264, where-next); architecture (counters, check-parity + practice.py rows); this log.

### Verification (exact counts)

- **264 producer tests green** (+2 from 263). **7 Node gates green:** check-parity = 320 closed-form points +
  **30 practice answers** (+6 gas), all else unchanged (validate-solutions = 5, check-ledger = 5 + 1 gas volume,
  check-katex = 468). `astro build` = 23 pages. `derived/` **byte-stable** — only the gas lesson's JSON changed
  (the practice block added).
- **Gas-practice branch 6-way tamper-tested, each caught then restored:** corrupt a volume answer (`!= re-derived
  nRT/P`), flip a limiting answer (`correct choice != answer`), corrupt a leftover (`!= re-derived`), corrupt a
  reaction constant (metal molar mass → the leftover re-derives wrong), a gameable menu on a numeric question, a
  diagnostic within 3% of the answer.
- **In-browser (dev preview):** the **Practice tab** renders 6 questions (H₂ subscripted via the generalised
  prettification); entering the **22.4-L slip (1.4 L)** on a volume question → "✗ Not quite … the answer is 1.53 L"
  + the named diagnostic ("Used 22.4 L/mol — that is the molar volume only at STP … use PV=nRT") + the worked
  explanation ("HCl limits, ξ = 62.5 mmol … × 24.47 L/mol = 1.53 L"); the limiting questions show Zn / HCl / Neither.
  No console errors.

### State at session end — PHASE 2 OPEN (gas-stoichiometry lesson + practice landed)

**5 lessons (2 precipitation + 1 percent-yield + 1 acid-base neutralization + 1 gas-stoichiometry, now with
practice) + 9 gyms (90 problems) + 1 Valence Table (23 elements) + 20 concepts + 7 reaction families (21 examples) +
14 species + 8 formula-sheet entries, 7 gates + CI + live Pages, 264 producer tests, 23 pages.** `build.py` emits
three reported-product shapes (precipitate / water / gas); the gas lesson has full free-entry practice re-derived by
check-parity from reaction constants — the first practice decoupled from an interactive block.
**Next up:** **thermochemistry** — the energy ledger (q=mcΔT with specific-heat data curated, ΔH_rxn·ξ, Hess); the
`formula-calorimetry` sheet entry is already in place. Smaller/optional: the gas lesson's **slider interactive**
(mass/volume/molarity → the gas volume — `ExtentBar` is cation/anion-locked, so it needs its own component).

## Increment 8 — Phase-2 calorimetry gym; the units engine gains an energy dimension (Opus 4.8 driving)

Continued straight on from the gas tier to the **thermochemistry opener** — the energy ledger's first rung, q=mcΔT.
Landed the `calorimetry_v1` gym + the `units.py` energy extension it needed, mirroring the gas-laws gym (ADR-0040)
as a template. One ADR (ADR-0042). No honesty-model change (the three badges stand — this is the first object to
wear TWO of them at once); additive schema growth; the `units.py` `Dim` extension is ADR-0040's anticipated
"add energy when thermochemistry needs it."

### Decision (full rationale in DECISIONS.md)

- **ADR-0042 — calorimetry gym; units engine gains an energy dimension; a gym with both the data-sourced and
  model-assumed badges.** (1) `units.py` `Dim` grows to 6 fields (energy); `J`/`kJ`/`J/(g*K)` registered; every
  answer is computed **through** the engine (g × J·g⁻¹·K⁻¹ × K → J, dimensions certified). Energy is kept
  **independent of pressure·volume** — a chemistry-bookkeeping basis, so a gas-law product (L·atm) and a heat (J)
  never silently equate. ΔT rides the temperature basis as a *difference* (1 °C = 1 K), distinct from the gas law's
  absolute kelvin. (2) The specific heat is a curated, **sourced** datum (`data/specific-heats.toml`, OpenStax
  Table 5.1) → the data/rule-sourced badge, AND the relation is exact only inside the **calorimetry model** → the
  model-assumed badge. First object with both. (3) Model-exact-then-rounded (3 sig figs; the gate re-derives q=mcΔT
  within 0.5%), solve for any of q/m/c/ΔT; solving for c is the identify-the-substance experiment. Diagnostics:
  using another substance's c, dropping a factor.

### Shipped

- **Data:** `data/specific-heats.toml` (7 substances — water + 6 metals, OpenStax Table 5.1); `data.py` loads it
  (`ChemData.specific_heats`, a `specific_heats` source key).
- **Producer:** `units.py` (`Dim` +energy; `J`/`kJ`/`J/(g*K)` registry + canonical labels); `gym.py`
  (+`_calorimetry_problem`/`_generate_calorimetry` computing through `Quantity`, consistent states, the
  `_solve_calor` helper, `_CAL_MASS`/`_CAL_DT` grids, pretty display units, `_FAMILY_ASSUMPTIONS` + the
  `specific_heats` provenance key).
- **Contracts:** `schemas/gym.schema.json` (+`calorimetry` kind, a `calorimetry` derivation shape, the
  `specific_heats` source key).
- **Gate:** `validate-gyms.mjs` (+`verifyCalorimetry` — re-derives q=mcΔT numerically, 0.5% tol; the kind added to
  `NUMERIC_KINDS` + the dispatch).
- **Content:** `gyms/calorimetry/calorimetry.gym.toml` (seed 3141592, 10 problems).
- **Player:** `gym/[slug].astro` (the calorimetry source badge — "specific heats sourced (…)"); `DimensionalGym.svelte`
  (a calorimetry chain caption). No new frontend otherwise (the free-entry numeric path is generic).
- **Tests:** `test_units.py` +2 (q=mcΔT lands in energy; energy independent of pressure·volume); `test_gym.py` +5
  (shape + both badges, q=mcΔT reproduced, c-solve = the sourced value, wrong-substance-c diagnostic, determinism).
- **Docs:** ADR-0042; CHANGELOG; ROADMAP (status + opening-tier calorimetry LANDED + thermo-lesson NEXT); AGENTS
  current-state + standing facts + where-next; architecture (as-built counters, units.py + data.py + gym.py +
  validate-gyms rows, Phase-2 row); regime-map (thermochemistry row → gym+atlas); authoring-gyms (`calorimetry_v1`);
  SOURCES (specific heats, Table 5.1); README; this log.

### Verification (exact counts)

- **271 producer tests green** (`uv --project producer run pytest`, ~14 s; +7 from 264).
- **7 Node gates green:** validate-solutions = 5, validate-reference = 50, **validate-gyms = 10 gyms / 100 problems**
  (+1 gym / +10), check-ledger = 5/20 + 1 gas volume, check-parity = 320 + 30, check-katex = 468, scan clean.
  **`astro build` = 24 pages** (+1: `/gym/calorimetry/`). `derived/` **byte-stable** — only `calorimetry.gym.json`
  added; no existing derived changed (the `specific_heats` source + energy `Dim` are calorimetry-only / internal).
- **Gate proven non-vacuous — 4 tamper tests, each caught then restored:** corrupt an answer (`999 != re-derived
  q=mcΔT 150`), corrupt a derivation input (`!= re-derived 38759…`), a gameable menu on a numeric problem, a
  diagnostic within 3% of the answer.
- **In-browser (dev preview):** `/gym/calorimetry/` shows the three badges (`10 machine-verified problems`,
  **`specific heats sourced (openstax-chemistry-2e)`**, **model-assumed**) + the model disclosure; problem 1 reads
  "Using q = m·c·ΔT: the specific heat of gold is c = 0.129 J/(g·°C), its temperature changes by 20 °C, and it
  absorbs 387 J of heat. Then what is the mass of the gold sample (g)?"; entering the wrong **4.625** (water's c) →
  "✗ Not quite — the answer is 150 g" + the named diagnostic ("Used the specific heat of another substance (4.184)
  instead of gold's c = 0.129") + the worked chain; the chips resolve to the calorimetry formula-sheet entry. No
  console errors.

### Deferred / open (recorded in ROADMAP + ADR-0042)

The **energy-ledger lesson** (ΔH_rxn·ξ attached to extent, Hess's law) is the next thermochemistry increment — the
gym + the `formula-calorimetry` sheet entry are the instrument it builds on. Within calorimetry: initial/final-
temperature framing (ΔT = T_f − T_i) + cooling (negative q) as distinct drills. Still open from the gas tier: the
gas lesson's slider interactive (`ExtentBar` cation/anion-locked).

### State at session end — PHASE 2 OPEN (calorimetry gym landed)

**5 lessons + 10 gyms (100 problems) + 1 Valence Table (23 elements) + 20 concepts + 7 reaction families (21
examples) + 14 species + 8 formula-sheet entries, 7 gates + CI + live Pages, 271 producer tests, 24 pages.** The
units engine now does energy (kept independent of pressure·volume); the calorimetry gym is the first object wearing
both the data-sourced and model-assumed badges. Thermochemistry has its instrument (gym + formula sheet); the energy
ledger is next.
**Next up:** the **energy-ledger lesson** — reaction enthalpy attached to extent (ΔH_rxn·ξ), Hess's law. Curate a
reaction-enthalpy datum (or a small ΔH_f table), extend the ledger/result with an energy row (q = ΔH_rxn·ξ, like the
gas result), and keep the misconception register + ledger view.

## Increment 9 — Phase-2 energy-ledger lesson: q = ΔH_rxn·ξ via Hess's law (Opus 4.8 driving)

Owner: "Read AGENTS.md. Continue the buildout autonomously. Work until you need my input. Multiple increments are
fine." Resumed from the flagged next item — the **energy-ledger lesson** — and landed its **vertical slice** (the
ledger drives a heat q = ΔH_rxn·ξ), generalising `build.py` to a fourth reported-product shape. One ADR (ADR-0043).
No honesty-model / house-convention / scope change (additive schema growth; the ΔH_rxn Hess sum is exact arithmetic
over a new sourced dataset).

### Decision (full rationale in DECISIONS.md)

- **ADR-0043 — energy-ledger lesson; `build.py`'s fourth reported-product shape; the first fully molecular lesson.**
  (1) **Hess's law over a sourced ΔH_f° table** (`data/formation-enthalpies.toml`, OpenStax Appendix G, keyed by
  formula AND phase — H₂O(l) −285.83 ≠ H₂O(g) −241.82): ΔH_rxn = Σν·ΔH_f°(products) − Σν·ΔH_f°(reactants), **exact
  Decimal arithmetic** over the sourced values; an element in its standard state contributes 0 by definition; the
  producer refuses to emit if any ΔH_f° is missing. (2) **The `result.energy` block** — a spec with `[energetics]`
  reports the **heat** q = ΔH_rxn·ξ (computed THROUGH the units engine, `kJ/mol` registered; no product mass —
  the energy is the headline). Honesty triple-layered: ξ ledger-exact, ΔH_f° data-sourced, the relations (Hess,
  q=ΔH·ξ to completion) model-assumed. q is **EXACT** here (all inputs terminate), 3-sf display — distinct from the
  gas volume's model-exact-then-rounded (ADR-0040). New regime facet `thermochemistry → model-exact`. (3) **A fully
  molecular reaction omits the ionic equations** (no charged term from `complete_ionic` → no ions in solution →
  omit complete/net ionic + spectators; schema `equations.required` → just `molecular`). Aqueous lessons keep all
  three, byte-identical.

### Shipped

- **Data:** `data/formation-enthalpies.toml` (12 species, OpenStax Appendix G, keyed formula+phase); `data.py` loads
  it (`ChemData.formation_enthalpies` + `formation_enthalpy(formula, phase)` + a `formation_enthalpies` source key).
- **Producer:** `units.py` (`kJ/mol`/`J/mol` registered — energy·amount⁻¹ — + canonical labels); `build.py`
  (`_energy_block` — Hess sum + q through the units engine + the ξ→q chain; a fourth result branch keyed on
  `[energetics]`; `has_ions` gating so a molecular reaction omits the ionic equations + skips the interactive;
  `thermochemistry` regime; `formation_enthalpies` provenance; a signed-zero normalization for an element's 0).
- **Contracts:** `schemas/solution.schema.json` — the `result.energy` block (delta_h_rxn / extent / q / classification
  / source / hess breakdown), `equations` complete/net-ionic + spectators now optional, `provenance.sources.formation_enthalpies`.
- **Gates:** `check-ledger.mjs` re-derives the **Hess sum** (Σ ±coeff·ΔH_f° = ΔH_rxn) + **q = ΔH_rxn·ξ** + the exo/endo
  sign, independent of Python; `validate-solutions.mjs` ties `result.energy` to a model-exact regime + a disclosed
  model assumption + the ΔH_f° source, forbids a product headline beside it, and cross-checks the Hess rows' role/phase
  against the ledger; `check-katex.mjs` already guarded per-equation-key so a molecular-only lesson passes untouched.
- **Content:** `problems/thermochemistry/methane-combustion-enthalpy.problem.toml` (CH₄ + 2 O₂ → CO₂ + 2 H₂O; 0.80215 g
  CH₄ + 3.83976 g O₂; the extent-scaling misconception; 14 reference_links); `reference/concepts/reaction-enthalpy.toml`
  (Hess's law + ΔH_f° + q = ΔH_rxn·ξ) → **21 concepts**.
- **Player:** `SolutionPlayer.svelte` — the heat headline card (model-assumed badge) + the **Hess breakdown table**
  (ν·ΔH_f° → ΔH_rxn) + the ξ×ΔH flow + the extent-scaling misconception branch (fires first); a data-sourced badge for
  the ΔH_f°; the equations panel guards the ionic views (a "no ions in solution" note otherwise); the results block
  guards the (absent) product mass. `view.js` — decorates `result.energy.hess` (symbol + pretty + phase), guards the
  equations render loop, adds ledger species to the prose token set.
- **Tests:** `test_build.py` +1 (`test_energy_ledger_lesson` — CH₄ limits, molecular-only equations, the energy
  headline, the Hess sum re-derived, q exact, no interactive/practice); `test_units.py` +1 (kJ/mol × mol → kJ);
  `test_data.py` +2 (ΔH_f° load by formula+phase; the missing-ΔH_f° / wrong-phase refusal).
- **Docs:** ADR-0043; CHANGELOG; ROADMAP (status + opening-tier energy-lesson LANDED); AGENTS current-state + standing
  facts + where-next; architecture (as-built counters, build.py/data.py/units.py rows, validate-solutions + check-ledger
  rows, result/equations block, thermo row); regime-map (thermochemistry row → lesson+gym+atlas); SOURCES (Appendix G
  formation enthalpies); authoring-problems (the `[energetics]` energy-lesson shape); README; this log.

### Verification (exact counts)

- **275 producer tests green** (`uv --project producer run pytest`, ~15 s; +4 from 271).
- **7 Node gates green:** **validate-solutions = 6**, **validate-reference = 51 objects** (1 table + 21 concepts + 7
  reaction families + 14 species + 8 formulas), validate-gyms = 10 gyms / 100, **check-ledger = 6 ledgers / 24 rows +
  1 gas volume + 1 reaction enthalpy re-derived from Hess's law**, check-parity = 320 + 30, **check-katex = 481**
  (+13: the energy lesson's molecular equation + ledger species + the reaction-enthalpy concept's math), scan clean.
  **`astro build` = 25 pages** (+1: `/lessons/methane-combustion-enthalpy/`). `derived/` **byte-stable** across two
  rebuilds (67 files, md5-compared); git confirms only the new lesson + concept added, **no existing solution changed**.
- **Gates proven non-vacuous — 7 tamper tests, each caught then restored:** corrupt q_kj (`!= ΔH_rxn·ξ = -44.5285`),
  corrupt a Hess contribution (`CO2 -1.0 != +1·ΔHf = -393.51`), corrupt ΔH_rxn (`-100 != Hess sum = -890.57`), flip the
  classification (`endothermic != exothermic (sign of q)`), drop the model-exact regime, drop the formation_enthalpies
  source, add a spurious product headline beside energy (`must not also carry a precipitate/product headline`).
- **In-browser (dev preview):** the lesson renders — badges (machine-checked + **Formation enthalpies data-sourced
  (openstax-chemistry-2e)** + 4 modeling assumptions); the **heat card** "Heat released [exothermic] −44.5 kJ, q =
  ΔH_rxn × ξ"; the **Hess table** (CH₄ −1/−74.6/74.6 · O₂ −2/0 (element → 0)/0 · CO₂ +1/−393.51/−393.51 · H₂O
  +2/−285.83/−571.66 → **ΔH_rxn = −890.57**); the flow **−890.57 kJ/mol × 0.05 mol = −44.5 kJ**; the three dimensional
  chains (2× g→mol, **ξ → q via ΔH_rxn**); the misconception refuted ("−890.57 kJ is per mole of reaction … this burn
  runs only ξ = 0.05 mol, so it releases −44.5 kJ, not 890.57 kJ"); the Equations tab shows only the molecular equation
  + the "no ions in solution" note; all **14 reference chips resolve** (Combustion, Reaction enthalpy and Hess's law,
  Moles from mass, …). An existing aqueous lesson still shows all 3 equations (no regression). No console errors.

### Deferred / open (recorded in ROADMAP + ADR-0043)

Generated **energy practice** (vary the masses → q, re-derived by check-parity from an energetics constants block — the
ADR-0041 gas-practice template) + a **Hess/enthalpy formula-sheet entry** (dimensional homogeneity of ΔH_rxn=Σν·ΔH_f°)
are the next thermochemistry-breadth increment. Also open: endothermic + multi-step (Hess-cycle) lessons; a
directly-sourced ΔH_rxn (single measured heat, alternative to the Hess sum); the gas lesson's slider interactive;
calorimetry initial/final-temperature framing + cooling drills.

### State at session end — PHASE 2 OPEN (energy-ledger lesson landed)

**6 lessons (2 precipitation + 1 percent-yield + 1 acid-base neutralization + 1 gas-stoichiometry + 1
thermochemistry/energy-ledger) + 10 gyms (100 problems) + 1 Valence Table (23 elements) + 21 concepts + 7 reaction
families (21 examples) + 14 species + 8 formula-sheet entries, 7 gates + CI + live Pages, 275 producer tests, 25
pages.** `build.py` now emits **four** reported-product shapes (precipitate / water / gas / energy); the energy lesson's
ξ is ledger-exact, its q = ΔH_rxn·ξ is exact (Hess sum over sourced ΔH_f°), re-derived by check-ledger — the first
triple-badged object and the first fully molecular lesson.
**Next up:** **generated energy practice** (vary the masses → q, re-derived by check-parity from an energetics constants
block — the ADR-0041 gas-practice template) + a **Hess/enthalpy formula-sheet entry**; then the later Phase-2 tiers
(bonding, equilibrium/acid-base, kinetics, electrochemistry).

## Increment 10 — energy-ledger generated practice (Opus 4.8 driving)

Continued straight on from increment 9 (the energy lesson) to its **generated practice** — the lower-risk, higher-value
half, mirroring the gas lesson's 7a/7b split (ADR-0041). Same ADR (ADR-0043 — recorded as its "second increment"). No
new decision; the move is the second practice family **decoupled from an interactive block**, re-derived from reaction
constants.

### Shipped

- **Producer** (`practice.py`): `generate_energy_practice` — deterministic variants (capacity grids so the limiting
  reagent switches regardless of coefficients) → free-entry **heat** (q=ΔH_rxn·ξ) + **leftover** (mmol), categorical
  **limiting reagent**. Heat diagnostics: the **naive ΔH_rxn-as-total** (forgot ξ) + **sizing ξ from the excess
  reactant**. The reaction constants (each reactant's molar mass + coefficient + ΔH_rxn) travel in a `practice.energetics`
  block so check-parity re-derives with no interactive. A new `_exact_grams` helper emits **both** masses at full
  precision (energy varies both, so the gas practice's 3-decimal rounding would compound past the leftover tolerance).
  `build.py` wires it (an energy lesson with `[practice]` → `generate_energy_practice` over its two reactants + the
  emitted ΔH_rxn). Seed 20260714 (scanned for a CH₄-limiting AND O₂-limiting categorical).
- **Contracts:** `schemas/solution.schema.json` — a `practice.energetics` constants block; a `heat` question kind.
- **Gate:** `check-parity.mjs` — a `checkEnergyPractice` branch re-derives heat (0.5%) / leftover (exact) / limiting from
  `practice.energetics` + the two per-question masses; `heat` joins `numericKinds` in the shared `validatePracticeMode`.
- **Player:** none — the generic free-entry `PracticeQuestion` island renders it unchanged (`view.js` already subscripts
  a no-interactive lesson's prose via the ledger species tokens, ADR-0043 increment 9).
- **Tests:** `test_build.py` +2 (`test_energy_practice` — family/energetics constants, all three kinds, the limiting
  switch, the mode split, a heat answer re-derived from its args; + a determinism test); `test_energy_ledger_lesson`
  updated (it now has practice, still no slider interactive).
- **Docs:** ADR-0043 (second-increment note); CHANGELOG (practice bullet + counters); ROADMAP (energy-lesson block +
  status); AGENTS (277 counter); architecture (counters, practice.py + check-parity rows); authoring-problems (the
  energy `[practice]` block); this log.

### Verification (exact counts)

- **277 producer tests green** (+2 from 275). **7 Node gates green:** **check-parity = 320 closed-form points + 36
  practice answers** (+6 energy), all else unchanged (validate-solutions = 6, check-ledger = 6 + 1 gas + 1 energy,
  check-katex = 481). `astro build` = 25 pages. `derived/` **byte-stable** — only the energy lesson's JSON changed (the
  practice block added; git confirms no other derived file touched).
- **Energy-practice gate 6-way tamper-tested, each caught then restored:** corrupt a heat answer (`!= ΔH_rxn·ξ`), flip a
  limiting answer (`!= re-derived`), corrupt a leftover (`!= re-derived`), corrupt the ΔH_rxn constant (heat re-derives
  wrong), a gameable menu on a numeric heat question, a diagnostic within 3% of the heat answer.
- **In-browser (dev preview):** the **Practice tab** renders 6 questions (CH₄/O₂ subscripted); entering the **naive
  −890.57** (ΔH_rxn as the total, forgetting ξ) on a heat question → "✗ Not quite … the answer is −35.6 kJ" + the named
  diagnostic ("Used ΔH_rxn as the total heat — that is per mole of reaction. Multiply by the extent ξ …") + the worked
  explanation ("CH₄ limits, so ξ = 40 mmol … q = ΔH_rxn × ξ = −890.57 kJ/mol × 40 mmol = −35.6 kJ"); the limiting
  questions show CH₄ / O₂ / Neither. No console errors.

### State at session end — PHASE 2 OPEN (energy-ledger lesson + practice landed)

**6 lessons (2 precipitation + 1 percent-yield + 1 acid-base neutralization + 1 gas-stoichiometry + 1
thermochemistry/energy-ledger, now with practice) + 10 gyms (100 problems) + 1 Valence Table (23 elements) + 21 concepts
+ 7 reaction families (21 examples) + 14 species + 8 formula-sheet entries, 7 gates + CI + live Pages, 277 producer
tests, 25 pages.** `build.py` emits four reported-product shapes (precipitate / water / gas / energy); the energy lesson
has full free-entry practice re-derived by check-parity from a `practice.energetics` constants block — the second
practice family decoupled from an interactive (after gas, ADR-0041).
**Next up:** a **Hess/enthalpy formula-sheet entry** (the reference backbone the energy lesson would link to — dimensional
homogeneity of ΔH_rxn=Σν·ΔH_f°); then endothermic / multi-step (Hess-cycle) lessons, and the later Phase-2 tiers
(bonding, equilibrium/acid-base, kinetics, electrochemistry).

## Increment 11 — Hess's-law formula-sheet entry (Opus 4.8 driving)

Continued from increment 10 to the reference half of the thermochemistry tier — the **Hess/enthalpy formula-sheet
entry** the energy lesson links to. Same ADR (ADR-0043 — recorded as its "third increment"); pure ADR-0039 formula-sheet
machinery, no new decision.

### Shipped

- **Content:** `reference/formulas/enthalpy-of-reaction.toml` (the 9th formula entry) — ΔH_rxn = Σν·ΔH_f°(products) −
  Σν·ΔH_f°(reactants), **model-exact** (enthalpy is a state function — Hess's law, disclosed), **dimensional homogeneity
  machine-checked**: both sides reduce to **molar energy** (kJ/mol). Discloses the state-function + standard-conditions
  assumptions; the reverse-reaction sign flip (ΔH_reverse = −ΔH_rxn) + solving for an unknown ΔH_f° ship as
  rearrangements; links to the lesson + the `reaction-enthalpy` concept + `formula-calorimetry`.
- **Producer:** `dimension.py` gained the display name **"molar energy"** for `[1,2,-2,-1,0,0]` (kJ/mol was already in
  the unit table since ADR-0039; the Node gate re-checks the vector, not the name, so `dimension.mjs` was untouched).
- **Content link:** the energy lesson's `reference_links` gained `formula-enthalpy-of-reaction` (14 → 15 chips).
- **Tests:** `test_reference.py` +1 (`test_formula_hess_enthalpy_of_reaction_is_molar_energy` — both terms molar
  energy, model-exact + assumptions, lesson link) + the authored-formulas count 8 → 9.
- **Docs:** ADR-0043 (third-increment note); CHANGELOG; ROADMAP (status + energy block + Phase-2 next); AGENTS counters
  (9 formulas, 278 tests); architecture (counters, formula-builder row); regime-map (thermo row); this log.

### Verification (exact counts)

- **278 producer tests green** (+1 from 277). **7 Node gates green:** **validate-reference = 52 objects** (1 table + 21
  concepts + 7 reaction families + 14 species + **9 formulas**), **check-katex = 487** (+6: the statement + 2
  rearrangements + inline summary/domain math), all else unchanged. `astro build` = 25 pages. `derived/` **byte-stable** —
  only the new formula file + the lesson's added reference link; no other reference/solution changed.
- **Entry 4-way tamper-tested, each caught then restored:** corrupt the RHS term dimension (energy, not molar energy),
  make ΔH_f° energy → non-homogeneous, empty the model assumptions (`model-exact formula discloses no assumption`),
  dangling lesson slug.
- **Static build (dist/):** `/reference/formulas/` renders the entry — the title "Enthalpy of reaction (Hess's law)",
  the dimensional-check line naming **molar energy**, the state-function assumption, and the KaTeX statement. (The dev
  server's Vite glob cache lagged the new file; the built artifact — what CI deploys — is authoritative and correct.)

### State at session end — PHASE 2 OPEN (thermochemistry tier: lesson + practice + reference)

**6 lessons + 10 gyms (100 problems) + 1 Valence Table (23 elements) + 21 concepts + 7 reaction families (21 examples) +
14 species + 9 formula-sheet entries, 7 gates + CI + live Pages, 278 producer tests, 25 pages.** The thermochemistry
tier is now complete across all three surfaces: the **energy-ledger lesson** (q=ΔH_rxn·ξ, with generated practice), the
**calorimetry gym** (q=mcΔT), and the reference (**Hess's-law** + calorimetry formula entries, the `reaction-enthalpy`
concept). `build.py` emits four reported-product shapes (precipitate / water / gas / energy).
**Next up:** the later Phase-2 tiers — **bonding & structure** (Lewis/VSEPR/polarity/IMFs; the Valence Table's bonding
mode is seeded, ADR-0033), then equilibrium/acid-base (ICE = ledger with reversible extent), kinetics (dξ/dt),
electrochemistry (electron ledger, ΔG=−nFE) — each opening with its stress scenario; or, always in season, endothermic /
multi-step Hess-cycle lessons, Atlas breadth-fill, further gym families.

## Increment 12 — Phase-2 bonding tier opened: the Lewis electron-ledger engine + the `molecule` Atlas kind (Opus 4.8 driving)

Owner: "Read AGENTS.md. Continue the buildout autonomously. Work until you need my input. Multiple increments are
fine." Resumed from the flagged next item — the **next Phase-2 tier, bonding & structure** — and opened it with its
machine-checkable instrument: the **Lewis electron-ledger engine** (`chemkernel.structure`) exposed as the `molecule`
Atlas kind. One ADR (ADR-0044, a tier-opening decision). No honesty-model / house-convention / scope change (additive
schema growth; opening a later Phase-2 tier proceeds inside the open phase per AGENTS.md; the octet rule + molecular
polarity ride the existing model-assumed badge, VSEPR + bond ΔEN the data-sourced badge).

### Decision (full rationale in DECISIONS.md)

- **ADR-0044 — open the bonding tier; the Lewis electron-ledger engine + `molecule` Atlas kind.** (1) The tier's
  machine-checkable core is an **electron ledger** — the electron-structure counterpart of the species ledger (the
  project thesis: "species accounting **plus electron structure**"; the regime map already tagged *Lewis structures*
  regimes **1, 4**). The author supplies the connectivity (atoms + lone pairs + bonds — the Lewis *model* is a modeling
  choice); the engine **derives + verifies + refuses**: valence total (Σ group electrons − charge), electron
  conservation (2·bonds + 2·lone pairs = V), per-atom octet/duet, per-atom formal charge (Σ = charge). Exact integer
  arithmetic → the machine-checked badge. (2) **VSEPR geometry** — the machine-derived electron-domain count keys a
  sourced table (`data/vsepr.toml`, OpenStax §7.6) for the geometry names + ideal angle (regime-3). (3) **Bond ΔEN**
  from the sourced Pauling values + `data/bonding.toml` (the ADR-0033 path). (4) **Molecular polarity** authored +
  disclosed (model-assumed), neutral molecules only — a charged ion carries a net charge, not a dipole. Atlas ids are
  kind-prefixed `molecule-*` (as `formula-*`/`reaction-*`) so a molecule + its same-named species coexist and cross-link.

### Shipped

- **Data:** `data/vsepr.toml` (the sourced VSEPR table, keyed (domains, lone_pairs) → electron/molecular geometry +
  ideal angle + a lone-pair `angle_note`); `data.py` loads it (`ChemData.vsepr`, a `vsepr` source key).
- **Producer:** `chemkernel/structure.py` (new — `build_molecule_entry`: the electron ledger + octet/formal-charge
  verification + refusals, the domain-count → VSEPR lookup, bond ΔEN classification, authored polarity; a TOML-trap-#4
  guard on the atom/bond tables); `build.py` (kind-dispatch for `molecule`).
- **Contracts:** `schemas/molecule.schema.json` (new — valence breakdown, electron_check, per-atom + per-bond arrays,
  geometry, polarity; `additionalProperties:false`).
- **Gates:** `validate-reference.mjs` (a `molecule` branch re-deriving the **whole electron ledger in pure Node** —
  valence total from the table's valence electrons, per-atom octet/formal charge + conservation, the domain count —
  plus bond ΔEN re-derived from the table's electronegativities + re-classified against the sourced thresholds
  (`classifyBond`), and polarity-present-iff-neutral); `check-katex.mjs` (the molecule formula symbol + inline
  summary/polarity/notes math).
- **Content:** `reference/molecules/{water,carbon-dioxide,ammonia,methane,formaldehyde,ammonium}.toml` (6 — the
  polar/nonpolar contrasts + the NH₄⁺ formal-charge case); `reference/concepts/{lewis-structure,vsepr}.toml`
  (`lewis-structure` ledger-exact, `vsepr` rule-sourced) → **23 concepts**.
- **Player:** `src/pages/reference/molecules.astro` (new — per molecule: the electron-ledger table (valence
  breakdown → total, electron_check, per-atom lone pairs/bonds/formal charge), the VSEPR shape, the per-bond-type ΔEN
  list, and the polarity verdict — each under its own machine-checked / sourced / model-assumed badge); `view.renderMolecule`
  (formal-charge signs, bond-type grouping); `reference/index.astro` (the molecules Atlas card); molecule routing in the
  page's cross-link `hrefFor`.
- **Tests:** `test_structure.py` (new, +14 — the water/CO₂/NH₄⁺ ledgers, and refusals: electrons-not-conserved,
  incomplete octet (BeH₂), off-dataset element, non-main-group (Fe), phase-in-formula, atoms≠formula, polarity on an
  ion, missing polarity on a neutral, uncovered geometry (HCl's (4,3)), the trap-#4 guard, all-authored-build);
  `test_data.py` +1 (the VSEPR table load + source).
- **Docs:** ADR-0044; CHANGELOG; ROADMAP (status + the bonding-tier OPENED block + the parallel-track fifth surface);
  AGENTS current-state (phase state + counters + standing facts + where-next); architecture (as-built header/counters,
  the `structure.py` + `data.py` rows, the validate-reference + check-katex gate rows, datasets list); regime-map
  (bonding + Lewis-structures + VSEPR rows filled); SOURCES (openstax §7.6 VSEPR coverage); README; this log.

### Verification (exact counts)

- **293 producer tests green** (`uv --project producer run pytest`, ~16 s; +15 from 278).
- **7 Node gates green:** validate-solutions = 6, **validate-reference = 60 objects** (1 table + 23 concepts + 7
  reaction families + 14 species + 9 formulas + **6 molecules**), validate-gyms = 10 gyms / 100, check-ledger = 6/24 +
  1 gas + 1 energy, check-parity = 320 + 36, **check-katex = 497** (+ the molecule LaTeX), scan clean. **`astro build`
  = 26 pages** (+1: `/reference/molecules/`). `derived/` **byte-stable** across two rebuilds; git confirms only the 6
  new molecule JSONs + the 2 new concept JSONs added — **no existing derived changed** (an early id collision, molecule
  `water`/`carbon-dioxide` vs the same-named species, was caught by the build's duplicate-id guard and fixed by
  kind-prefixing to `molecule-*`; the species JSONs were rebuilt intact).
- **Molecule gate proven non-vacuous — 7 tamper tests, each caught then restored:** corrupt a formal charge (`O1
  formal_charge 1 != re-derived 0`), the valence total (`99 != re-derived 16`), a bond ΔEN (`0.5 != re-derived
  0.89`), the geometry domains (`4 != re-derived 2`), the electron_check (`bonding 6 != re-derived 8`), a bond class
  (`nonpolar-covalent != re-derived polar-covalent`), and adding polarity to the ammonium ion (`charged species must
  not state a molecular polarity`).
- **In-browser (dev preview):** `/reference/molecules/` renders all 6 with a Unicode jump-nav (NH₃/NH₄⁺/CO₂/CH₂O/CH₄/H₂O).
  **CO₂** shows all four facet badges (machine-checked + geometry + EN + model-assumed), "C 1×4 = 4 + O 2×6 = 12 → 16
  valence electrons", "8 bonding (4 shared pairs) + 8 nonbonding (4 lone pairs) = 16 ✓", linear geometry, "C=O ×2 · ΔEN
  0.89 · polar covalent", and **nonpolar** with the dipole-cancellation reason (polar bonds, nonpolar molecule — the
  marquee contrast). **NH₄⁺** shows "H 4×1 = 4 + N 1×5 = 5 − 1 (charge) → 8", the **+1 formal charge on N** (highlighted),
  tetrahedral, and **no** polarity section (an ion). The Atlas index shows the Molecules card. No console errors/warnings.

### Deferred / open (recorded in ROADMAP + ADR-0044)

Octet exceptions (electron-deficient BeH₂/BF₃, expanded PCl₅/SF₆ — the opener enforces strict octets); resonance
(CO₃²⁻/NO₃⁻ — needs equivalent-structure handling); a `lewis_structures_v1` gym (generated valence-electron /
formal-charge / electron-domain counting drills off the corpus — the balancing-gym pattern, fully regime-1); a bonding
& structure lesson (the deep vertical slice); IMFs (build on molecular polarity).

### State at session end — PHASE 2 OPEN (bonding & structure tier opened)

**6 lessons + 10 gyms (100 problems) + 1 Valence Table (23 elements) + 23 concepts + 7 reaction families (21 examples)
+ 14 species + 6 molecule structure entries + 9 formula-sheet entries (= 60 Atlas reference objects), 7 gates + CI +
live Pages, 293 producer tests, 26 pages.** The bonding tier opened with its machine-checkable instrument: the **Lewis
electron ledger** (valence total → octet → formal charge, exact integer accounting, re-derived in pure Node) — the
electron-structure counterpart of the species ledger — plus VSEPR geometry (sourced) + bond ΔEN (sourced) + disclosed
molecular polarity. The Atlas now has a fifth reference surface (`molecule` kind); ids are kind-prefixed where they'd
collide.
**Next up:** the rest of the bonding tier — a **`lewis_structures_v1` gym** (generated counting drills off the molecule
corpus: valence electrons / formal charge / electron domains — the balancing-gym instrument, fully regime-1), then a
**bonding & structure lesson** and **IMFs**; octet exceptions + resonance are the corpus-depth deferrals. Then the later
Phase-2 tiers (equilibrium/acid-base, kinetics, electrochemistry), each opening with its stress scenario.

## Increment 13 — Phase-2 bonding tier: the `lewis_structures_v1` gym (Opus 4.8 driving)

Continued straight on from increment 12 to the flagged next item — the **Lewis-structures gym**, the electron ledger's
drill surface. Same ADR (ADR-0044 — recorded as its "second increment"); no new decision. The interesting move is a
**pure refactor** so the gym and the Atlas share one engine, then a gym family that is **back to regime-1** (exact
integer counting — the first Phase-2 gym with no model badge, after the model-exact gas/calorimetry gyms).

### Shipped

- **Refactor** (`structure.py`): the ledger computation extracted from `build_molecule_entry` into
  **`compute_ledger(atoms, bonds, central, formula, data, ctx)`** — the molecule Atlas builder and the gym now compute
  the electron ledger from the SAME verified function (no hard-coded counts). The molecule derived JSON is **byte-identical**
  after the refactor (verified by git diff).
- **Producer** (`gym.py`): the `lewis_structures_v1` family — an 8-molecule skeleton corpus (the 6 Atlas molecules +
  CCl₄ + PCl₃ for valence variety), three kinds (`lewis_valence`/`lewis_domains` free-entry numeric, `lewis_geometry`
  categorical), each answered via `compute_ledger`; named diagnostics (all-electrons vs valence, lone-pair-is-a-domain,
  double-bond-as-two-domains); the electron-domain-geometry + bonds-only shape distractors; provenance adds `position` +
  `vsepr`. `generate_gym` wires the family + its sources.
- **Contracts:** `schemas/gym.schema.json` (+3 kinds, a `lewis` derivation block, a `vsepr` provenance source).
- **Gate:** `validate-gyms.mjs` (+`verifyLewis` — re-derives valence Σ group-electrons−charge from `valence-table.json`
  + the electron-domain count from the structure; the geometry's correct choice must be the sourced shape; `lewis_valence`/
  `lewis_domains` join `NUMERIC_KINDS`).
- **Content:** `gyms/lewis-structures/lewis-structures.gym.toml` (seed 1732050, 10 problems).
- **Player:** `gym/[slug].astro` (the lewis source badge — "group positions + VSEPR sourced (…)"; concept chips resolve
  to `lewis-structure` + `vsepr`); `DimensionalGym.svelte` (a lewis chain caption). No new frontend — the generic
  free-entry island renders it.
- **Tests:** `test_gym.py` +5 (shape/kinds/sources; valence + domains re-derive from the structure; the
  electron-domain-geometry distractor; the all-electrons diagnostic; determinism).
- **Docs:** ADR-0044 (second-increment note + the bond-order design note); CHANGELOG; ROADMAP (status + the bonding
  block now lists the gym, gym removed from deferred); AGENTS (counters 11 gyms/110, 298 tests, 27 pages; where-next →
  the structure lesson); architecture (as-built counters, gym.py + structure.py + validate-gyms rows); regime-map
  (Lewis + VSEPR rows → atlas+gym); authoring-gyms (`lewis_structures_v1`); README; this log.

### Verification (exact counts)

- **298 producer tests green** (`uv --project producer run pytest`, ~16 s; +5 from 293).
- **7 Node gates green:** validate-solutions = 6, validate-reference = 60, **validate-gyms = 11 gyms / 110 problems**
  (+1 gym / +10), check-ledger = 6/24 + 1 gas + 1 energy, check-parity = 320 + 36, check-katex = 497, scan clean.
  **`astro build` = 27 pages** (+1: `/gym/lewis-structures/`). `derived/` **byte-stable** across two rebuilds — only the
  new gym added; the refactor left the molecule JSONs byte-identical; **no existing derived changed**.
- **Gym gate proven non-vacuous — 6 tamper tests, each caught then restored:** corrupt a valence answer (`99 !=
  re-derived 26`), a domains answer (`9 != re-derived 4`), a geometry shape (`answer 'tetrahedral' != shape 'linear'`),
  the central lone-pair count (`domains 4 != re-derived 6`), a numeric-question menu (`a numeric answer must not be a
  multiple-choice menu`), a too-close diagnostic (`26.1 within 3% of 26`). (A bond-order change is correctly a no-op —
  the electron-domain count is order-independent.)
- **In-browser (dev preview):** `/gym/lewis-structures/` shows the two badges (`10 machine-verified problems`, **group
  positions + VSEPR sourced (iupac-periodic-table, openstax-chemistry-2e)** — no model badge, it's regime-1) + both
  concept chips resolve. Entering the **all-electrons slip (66)** on the PCl₃ valence question → "✗ Not quite — you
  entered 66; the answer is 26 electrons" + the named diagnostic ("Counted every electron … not just the valence") + the
  chain "Cl 3×7 + P 1×5 = 26". No console errors.

### State at session end — PHASE 2 OPEN (bonding tier: engine + Atlas + gym)

**6 lessons + 11 gyms (110 problems) + 1 Valence Table (23 elements) + 23 concepts + 7 reaction families (21 examples) +
14 species + 6 molecule structure entries + 9 formula-sheet entries (= 60 Atlas reference objects), 7 gates + CI + live
Pages, 298 producer tests, 27 pages.** The bonding tier now has its engine (the Lewis electron ledger, `compute_ledger`),
its reference (the `molecule` Atlas kind), and its practice (the `lewis_structures_v1` gym — regime-1 counting, the
first Phase-2 gym with no model badge). The gym and the Atlas share one verified engine.
**Next up:** a **bonding & structure lesson** — the tier's deep vertical slice: a molecule stepped from valence
electrons → Lewis structure → VSEPR shape → polarity, with the misconception register (the ledger's presentation shape
generalised past a reaction to a single molecule). Then **IMFs** (build on molecular polarity); octet exceptions +
resonance + a formal-charge gym drill are corpus-depth deferrals. Then the later Phase-2 tiers (equilibrium/acid-base,
kinetics, electrochemistry).

## Increment 14 — Phase-2 bonding tier: the bonding & structure lesson (the `structure` lesson kind, Opus 4.8 driving)

Continued straight on to the flagged next item — the tier's **deep vertical slice**. The whole increment turned on one
architectural call: a single-molecule lesson has **no reaction** (no equations, no species ledger over extent, no reported
product), so `solution.schema.json` (deeply reaction-shaped) doesn't fit. **ADR-0045** — rather than bend the reaction
schema into a discriminated union (weakening the six reaction lessons' contract + forcing branches through every
reaction-shaped gate/player), add a **new tight `structure` lesson kind** (its own schema + builder + `*.structure.json`),
mirroring the house pattern of a new tight schema per new object kind. The pivot is the Lewis **electron** ledger, re-derived
by the SAME `compute_ledger` engine the Atlas + gym use; the lesson reuses a `molecule` Atlas entry's authored connectivity
(one source of truth, no drift).

### Shipped

- **Schema** (`schemas/structure-lesson.schema.json`): `additionalProperties:false`; a `molecule` block (mirrors the
  molecule Atlas structural fields + `ref_id`), four `steps` (keys valence/lewis/shape/polarity), `regimes`/`assumptions`/
  `misconception`/`checks`/`provenance`.
- **Producer** (`structure.py`): `build_structure_lesson(spec, molecule_spec, data, ctx)` — runs `compute_ledger`, refuses
  a charged molecule (polarity is the payoff), fixes each step's title + regime (author writes only prose), a trap-#4 guard
  on `[steps]`. `build.py`: `build_structure` + a molecule-spec resolver; `build_problems_main` dispatches `*.problem.toml`
  vs `*.structure.toml` by extension.
- **Content:** `problems/bonding/water-molecular-shape.structure.toml` — *why water is bent* (valence → Lewis → VSEPR →
  polarity; the "water is linear" misconception; three disclosed model assumptions). Wired the `lessons` backlink on
  `reference/molecules/water.toml` + the `lewis-structure`/`vsepr` concepts.
- **Gates:** the molecule electron-ledger re-derivation **factored out** of `validate-reference.mjs` into a shared
  `scripts/validate/structurecheck.mjs` (`verifyElectronLedger` + `ledgerTables` + `classifyBond`); validate-reference now
  calls it (molecule JSON byte-identical). `validate-solutions.mjs` walks `*.structure.json` (schema + re-derive the ledger
  in pure Node + cross-check byte-for-byte against the Atlas molecule with the same `ref_id` + step order + a ledger-exact
  regime + a disclosed model assumption). validate-reference's lesson-slug walk + `check-katex` learn the `.structure.json`
  suffix; the five reference pages' lesson-title maps glob both lesson kinds so backlinks resolve.
- **Player:** `src/components/StructureLesson.astro` (a fully **static** Astro component — no island, nothing to hydrate)
  + `view.renderStructureLesson`; `lessons/[slug].astro` + `lessons/index.astro` glob both lesson kinds and branch on kind;
  `molecule` added to the concept-chip `kindPage` map.
- **Tests:** `test_structure.py` +8 (shape; the embedded ledger equals the Atlas entry's — no drift; refuse a charged
  molecule / a missing or empty step / a `[steps]` trap-#4 absorption / a missing root key; the authored lesson builds).
- **Docs:** ADR-0045; CHANGELOG; ROADMAP (status + the bonding block); AGENTS (deep-swept `## Current state` — un-staled the
  phase paragraph, counters 7 lessons/306 tests/28 pages, distilled standing facts, where-next → IMFs); architecture
  (as-built header/counters, build.py + structure.py rows, the validate-solutions/reference/katex gate rows, the
  solution-object cross-ref); regime-map (Lewis + VSEPR rows → lesson+atlas+gym); authoring-problems (a Structure-lessons
  section + the top-of-guide pointer); README; this log.

### Verification (exact counts)

- **306 producer tests green** (`uv --project producer run pytest`, ~16 s; +8 from 298).
- **7 Node gates green:** **validate-solutions = 6 solutions + 1 structure lesson**, validate-reference = 60,
  validate-gyms = 11 gyms / 110, check-ledger = 6/24 + 1 gas + 1 energy, check-parity = 320 + 36, **check-katex = 498**
  (+1), scan clean. **`astro build` = 28 pages** (+1: `/lessons/water-molecular-shape/`). `derived/` **byte-stable** across
  two rebuilds; git confirms **no existing derived changed** except the 3 authored `lessons` backlinks (molecule-water,
  lewis-structure, vsepr) — the gate refactor left the molecule JSONs byte-identical.
- **Structure-lesson gate proven non-vacuous — 7 tamper tests, each caught by a distinct branch:** corrupt the valence
  total (`valence_electrons 99 != re-derived 8`), a formal charge (`atom 'O1' formal_charge 3 != re-derived 0`), the step
  order (`steps must be valence → lewis → shape → polarity`), the geometry shape (drift → `molecule.geometry drifts from the
  Atlas entry`), drop the model assumption (`discloses no model assumption`), a bad ref_id (`resolves to no molecule Atlas
  entry`), and a flipped check (schema `const`).
- **In-browser (dev preview):** `/lessons/water-molecular-shape/` renders the three honesty badges, the payoff cards
  (**H₂O** / shape **bent**, 4 domains / **polar**), the four stepped panels (valence "H 2×1 = 2 + O 1×6 = 6 → 8"; the
  Lewis atom table with O's 2 lone pairs + 0 formal charge; "4 electron domains → tetrahedral → bent, 104.5°"; the H–O ×2
  ΔEN 1.24 bond row → polar), the four SHOWN machine-checked facts, and the **data-driven** misconception refutation ("the
  2 lone pairs are electron domains → 4 domains → bent, not a straight line → polar"). The Lessons index lists it under
  **Bonding** (7 lessons); the molecule Atlas + both concepts backlink to it. No console errors.

### Deferred / open (recorded in ROADMAP + ADR-0045)

Generated practice on the structure lesson (the `lewis_structures_v1` gym already drills the counting); more structure
lessons (CO₂'s linear-nonpolar contrast as its own slice; NH₃/CH₄); **IMFs** (the tier's next increment, building on the
disclosed molecular polarity); octet exceptions + resonance + a formal-charge gym drill (corpus-depth).

### State at session end — PHASE 2 OPEN (bonding tier: engine + Atlas + gym + lesson)

**7 lessons + 11 gyms (110 problems) + 1 Valence Table (23 elements) + 23 concepts + 7 reaction families (21 examples) +
14 species + 6 molecule structure entries + 9 formula-sheet entries (= 60 Atlas reference objects), 7 gates + CI + live
Pages, 306 producer tests, 28 pages.** The bonding tier now has its engine (`compute_ledger`), reference (`molecule`
kind), practice (`lewis_structures_v1` gym), AND its deep vertical slice — the **`structure` lesson kind** (ADR-0045),
`bonding/water-molecular-shape`. A single-molecule lesson is a genuinely new object (no reaction) with its own tight
schema; `compute_ledger` is now shared by the Atlas builder, the gym, AND the lesson, and a shared `structurecheck.mjs`
re-derives the electron ledger in pure Node for both the reference + solutions gates. The player for it is fully static.
**Next up:** **IMFs** — the rest of the bonding tier, building on the disclosed molecular polarity (dipole–dipole,
hydrogen bonding, London dispersion; likely a new concept + boiling-point evidence, and possibly a second structure-lesson
framing). Corpus-depth deferrals: octet exceptions (BeH₂/BF₃/PCl₅/SF₆), resonance (CO₃²⁻/NO₃⁻), a formal-charge gym drill,
more structure lessons (CO₂ contrast; NH₃/CH₄). Then the later Phase-2 tiers (equilibrium/acid-base, kinetics,
electrochemistry), each opening with its stress scenario.

## Increment 15 — Phase-2 bonding tier: the CO₂ structure lesson (ADR-0045, 2nd increment, Opus 4.8 driving)

Continued straight on to a **second structure lesson** — chosen (over opening IMFs, which carries scope questions) as the
clean, fully-autonomous next move inside the just-settled ADR-0045 contract. CO₂ is the marquee VSEPR contrast to water
(*polar bonds, nonpolar molecule*) and stress-tests the new machinery on a different molecular shape — linear, double
bonds, no central lone pairs, nonpolar. No new decision (same ADR); no new schema/builder/gate — the whole increment is
authored content + one player branch.

### Shipped

- **Content:** `problems/bonding/carbon-dioxide-molecular-shape.structure.toml` — *why CO₂ is linear*. Reuses the
  `molecule-carbon-dioxide` Atlas entry's connectivity; 16 e⁻ → two C=O **double bonds** (the octet fix) → 2 domains, no
  central lone pairs → **linear** → the two polar C=O dipoles cancel → **nonpolar**. The same "central atom + two outer
  atoms" recipe as water, opposite result (lone pairs decide). Backlinks wired: `carbon-dioxide.toml` + the
  `lewis-structure`/`vsepr` concepts now list both structure lessons.
- **Player:** one new misconception-refutation branch in `StructureLesson.astro`, keyed `symmetric_geometry_cancels_dipoles`
  — data-driven from the verified geometry + polarity + bond ΔEN ("each bond is polar (ΔEN 0.89), but the central atom has
  no lone pairs → linear → equal opposite dipoles cancel → nonpolar"). No other frontend change (the kind is fully static).
- **Tests:** `test_structure.py`'s authored-lesson test **generalised to build every** `problems/**/*.structure.toml`
  (water + CO₂ + any future one) — asserts the ledger conserves + the steps are canonical + the molecule is neutral.
- **Docs:** CHANGELOG; ROADMAP (the lesson block + Lessons #7–#8); AGENTS + architecture counters (8 lessons, 29 pages);
  README (eight lessons, the CO₂ contrast); this log. No ADR (inside the ADR-0045 contract).

### Verification (exact counts)

- **306 producer tests green** (the authored-lesson test now loops over both structure lessons; net count unchanged — one
  test generalised).
- **7 Node gates green:** **validate-solutions = 6 solutions + 2 structure lessons** (8 unique ids), validate-reference =
  60, validate-gyms = 11/110, check-ledger = 6/24 + 1 gas + 1 energy, check-parity = 320 + 36, **check-katex = 499** (+1),
  scan clean. **`astro build` = 29 pages** (+1: `/lessons/carbon-dioxide-molecular-shape/`). `derived/` **byte-stable**;
  no existing derived changed except the CO₂ + 2 concept `lessons` backlinks.
- **In-browser (dev preview):** `/lessons/carbon-dioxide-molecular-shape/` renders the linear/nonpolar payoff cards, the
  valence line "C 1×4 = 4 + O 2×6 = 12 → 16", the Lewis table (C: 0 lp / 4 bond-order / 0 FC; each O: 2 lp), "2 electron
  domains → linear", and the **new** refutation ("polar bonds don't add up to a polar molecule … equal dipoles cancel →
  nonpolar; a bent molecule like water can't cancel its own and is polar"). The Lessons index lists both structure lessons
  under **Bonding** (8 lessons); CO₂ ↔ water cross-link as the contrast pair. No console errors.

### State at session end — PHASE 2 OPEN (bonding tier: engine + Atlas + gym + two structure lessons)

**8 lessons + 11 gyms (110 problems) + 1 Valence Table (23 elements) + 23 concepts + 7 reaction families (21 examples) +
14 species + 6 molecule structure entries + 9 formula-sheet entries (= 60 Atlas reference objects), 7 gates + CI + live
Pages, 306 producer tests, 29 pages.** The bonding tier now carries **two** structure lessons — water (bent-polar) and
CO₂ (linear-nonpolar), the canonical contrast pair — both on the shared `compute_ledger` + the `structure` lesson kind
(ADR-0045); the second was pure authored content inside the settled contract, proving the machinery generalises.
**Next up:** **IMFs** — the rest of the bonding tier, building on the disclosed molecular polarity (dipole–dipole,
hydrogen bonding, London dispersion). This one carries a **scope question** (how much is machine-checkable? a new concept
+ boiling-point evidence? a lesson vs. an Atlas concept?) — a natural point to check with the owner before opening it.
Corpus-depth deferrals: octet exceptions (BeH₂/BF₃/PCl₅/SF₆), resonance (CO₃²⁻/NO₃⁻), a formal-charge gym drill, more
structure lessons (NH₃ pyramidal-polar; CH₄ tetrahedral-nonpolar). Then the later Phase-2 tiers (equilibrium/acid-base,
kinetics, electrochemistry).

## Increment 16 — Phase-2 bonding tier: intermolecular forces (ADR-0046, Opus 4.8 driving)

**Owner-authorized** (an AskUserQuestion at the increment-15 boundary chose "Open IMFs (recommended)"). IMFs are the
bonding tier's last core topic and its first predominantly **empirical/interpretive** subject — the "keep it on-thesis"
question. **ADR-0046:** anchor IMFs to the already-verified structure. The machine-checkable spine is a **dominant-IMF
classifier** driven by the verified structure + polarity; the strength ordering is the sourced rule; a sourced boiling
point is the evidence. Delivered as a molecule-Atlas extension + a concept — NOT a lesson (the comparison lesson is a new
multi-molecule shape, deferred).

### Shipped

- **Data:** `data/boiling-points.toml` (normal boiling / sublimation points, sourced OpenStax §10.1: CH₄ −161.5, NH₃
  −33.3, H₂O 100, CO₂ sublimes −78.5 °C) + the `data.py` loader (+ a `boiling_points` source facet, keyed by formula).
- **Engine** (`structure.classify_imf`): a neutral molecule's dominant IMF from its polarity + an **exact H-on-N/O/F test**
  over the atoms + bonds — London dispersion (always) / dipole–dipole (polar) / hydrogen bonding (H bonded to N/O/F +
  polar); dominant = strongest present. Wired into `build_molecule_entry` as an optional **`intermolecular` block** (neutral
  molecules only — an ion's interactions are ionic; NH₄⁺ gets none).
- **Schema:** `molecule.schema.json` gains the optional `intermolecular` block (dominant/forces/h_bond_donor +
  boiling_point_c/phase_change/boiling_source).
- **Concept:** `reference/concepts/intermolecular-forces.toml` (rule-sourced) — the three forces, when each dominates, the
  ordering, the CH₄ ≪ NH₃ ≪ H₂O boiling-point evidence, and the disclosed dispersion-grows-with-size caveat.
- **Gate:** the IMF classification re-derives in pure Node — `classifyIMF` added to the shared `structurecheck.mjs`,
  called from `validate-reference` (h_bond_donor + forces + dominant re-computed; the boiling **source** register-checked,
  its value empirical; a neutral molecule must carry the block, a charged one must not).
- **Player:** `molecules.astro` shows each neutral molecule's dominant IMF + forces present + H-bond-donor flag + sourced
  boiling point under machine-derived + data-sourced badges, the label linking to the concept; the lede + `view.renderMolecule`
  updated (IMF display labels). No lesson-page change.
- **Docs:** ADR-0046; CHANGELOG; ROADMAP (status + IMF landed block + the bonding scope block); AGENTS (counters 24
  concepts / 61 objects / 311 tests, standing facts, where-next → the comparison lesson); architecture (as-built header +
  counters + the data.py/structure.py/validate-reference rows); regime-map (an IMF row + the bonding row un-hedged); SOURCES
  (the boiling-points dataset + §10.1 coverage); this log.

### Verification (exact counts)

- **311 producer tests green** (+5 — 4 `classify_imf` cases incl. the CH₂O polar-but-not-a-donor subtlety + the NH₄⁺-has-no-block
  case; +1 boiling-points load in `test_data.py`).
- **7 Node gates green:** validate-solutions = 6 + 2 structure, **validate-reference = 61 objects** (+1 concept),
  validate-gyms = 11/110, check-ledger = 6/24 + 1 gas + 1 energy, check-parity = 320 + 36, check-katex = 499, scan clean.
  **`astro build` = 29 pages** (no new page — the concept renders on the reference index, the IMF blocks on the molecules
  page). `derived/` **byte-stable**; the 5 **neutral** molecule JSONs gain the `intermolecular` block, **NH₄⁺ byte-identical**
  (an ion, no block); + the new concept JSON.
- **IMF re-derivation proven non-vacuous — 6 tamper tests, each caught by a distinct branch:** flip the dominant force
  (`'london-dispersion' != re-derived 'hydrogen-bonding'`), the h_bond_donor (`false != re-derived true`), the forces list;
  add a block to the **ammonium ion** (`present on a charged species`); an unregistered boiling source; drop a neutral
  molecule's block (`no intermolecular block`).
- **In-browser (dev preview):** `/reference/molecules/` shows the IMF section on every neutral molecule — **water**
  "Dominant: hydrogen bonding · present: London dispersion, dipole–dipole, hydrogen bonding · H-bond donor" + "boils at
  100.0 °C" (sourced); **CO₂** "London dispersion · sublimes at −78.5 °C"; **CH₂O** "dipole–dipole" (no bp, correctly no
  H-bond donor); **NH₄⁺** shows no IMF section. The `intermolecular-forces` concept renders on the reference index. No
  console errors.

### State at session end — PHASE 2 OPEN (bonding tier complete bar deferrals: engine + Atlas + gym + 2 lessons + IMFs)

**8 lessons + 11 gyms (110 problems) + 1 Valence Table (23 elements) + 24 concepts + 7 reaction families (21 examples) +
14 species + 6 molecule structure entries (Lewis ledger + VSEPR + dominant IMF) + 9 formula-sheet entries (= 61 Atlas
reference objects), 7 gates + CI + live Pages, 311 producer tests, 29 pages.** The bonding tier now covers its whole core:
the electron-ledger engine, the `molecule` Atlas kind, the `lewis_structures_v1` gym, two `structure` lessons (water, CO₂),
and **intermolecular forces** — a dominant-IMF classifier anchored to the verified structure (the H-on-N/O/F test exact),
delivered as the molecule `intermolecular` block + sourced boiling-point evidence + the `intermolecular-forces` concept.
Three increments landed this session (ADR-0045 ×2 + ADR-0046).
**Next up:** the **IMF comparison lesson** — a *new multi-molecule lesson shape* (the existing `structure` lesson is
single-molecule): line up the corpus's molecules and teach the boiling-point trend (CH₄ ≪ NH₃ ≪ H₂O) as the payoff of IMF
strength. **That new lesson shape is a design decision worth surfacing to the owner** (how to represent a multi-molecule
comparison — a new schema/builder, like `structure` was). Alternatively, open the next major tier — **equilibrium &
acid-base** (ICE = the species ledger with reversible extent). Corpus-depth deferrals remain (octet exceptions, resonance,
NH₃/CH₄ structure lessons, a "which IMF dominates?" gym drill). Then kinetics, electrochemistry.

## Increment 17 — Phase-2 bonding tier: the IMF comparison lesson (ADR-0047, Opus 4.8 driving)

**Owner-authorized** (an AskUserQuestion at the increment-16 boundary chose "IMF comparison lesson"). This is the bonding
tier's capstone and a **third lesson shape**. IMFs' teaching payoff is the boiling-point trend, which is inherently
*multi-molecule* — neither the reaction lesson (a species ledger) nor the structure lesson (one molecule) fits. **ADR-0047:**
a new `comparison` lesson kind, with an elegant on-thesis spine — **the trend itself is machine-checked**: sort by boiling
point, prove the dominant-IMF rank is non-decreasing (the builder refuses a non-monotonic corpus).

### Shipped

- **Schema + builder:** `schemas/comparison-lesson.schema.json` + `structure.build_comparison_lesson` (resolves each
  referenced `molecule` Atlas entry via `build_molecule_entry` — rows reuse the verified IMF + boiling point, no drift —
  sorts by boiling point, and **refuses to emit** if the dominant-IMF rank isn't non-decreasing). `build.py` gains
  `build_comparison` + the **third** extension dispatch (`*.comparison.toml`).
- **Content:** `problems/bonding/boiling-points-and-imfs.comparison.toml` — CH₄ (−161.5 °C, dispersion) ≪ NH₃ (−33.3 °C,
  hydrogen bonding) ≪ H₂O (100 °C, hydrogen bonding), the equal-mass hydride trio (16/17/18) so size is controlled. The
  misconception is the intramolecular-vs-intermolecular confusion, refuted from the table. Backlinks wired on the 3
  molecules + the `intermolecular-forces` concept.
- **Gate:** `validate-solutions` walks `*.comparison.json` — re-verifies the sort, each `imf_rank` consistent with its
  `dominant`, the rank non-decreasing (the trend), and each row's IMF re-derived from the Atlas structure (`classifyIMF`)
  + boiling point matched (no drift). `validate-reference`'s slug walk + `check-katex` + the five reference pages' lesson-
  title maps learn the `.comparison.json` suffix.
- **Player:** a fully static `src/components/ComparisonLesson.astro` (the sorted table, the machine-checked trend, the
  takeaway, the misconception, the SHOWN checks) + `view.renderComparisonLesson`; `lessons/[slug].astro` +
  `lessons/index.astro` now branch across all three lesson kinds.
- **Docs:** ADR-0047; CHANGELOG; ROADMAP (status + the comparison landed block + "bonding teaching arc complete");
  AGENTS (counters 9 lessons / 315 tests / 30 pages, the three-lesson-shapes standing fact, where-next → equilibrium);
  architecture (as-built header + counters + build.py/structure.py/validate-solutions rows); authoring-problems (a
  Comparison-lessons section + the intro pointer); README; this log.

### Verification (exact counts)

- **315 producer tests green** (+4 — the comparison sorts + verifies the trend; refuses a non-monotonic corpus / a
  molecule without a boiling point / a charged molecule).
- **7 Node gates green:** **validate-solutions = 6 solutions + 2 structure + 1 comparison** (9 unique ids),
  validate-reference = 61, validate-gyms = 11/110, check-ledger = 6/24 + 1 gas + 1 energy, check-parity = 320 + 36,
  **check-katex = 502** (+3, the row molecule symbols), scan clean. **`astro build` = 30 pages** (+1). `derived/`
  **byte-stable**; only the 4 authored backlinks changed (the concept + the 3 compared molecules).
- **Comparison gate proven non-vacuous — 5 tamper tests, each caught by a distinct branch:** unsort the rows (`not
  sorted ascending`), an inconsistent imf_rank (`imf_rank 3 != rank of 'london-dispersion' (1)`), a non-monotonic trend
  (`not monotonic — H2O … ranks below a lower-boiling molecule`), a boiling-point drift (`'-160' != Atlas '-161.5'`), a
  dominant-IMF drift (`'dipole-dipole' != re-derived 'hydrogen-bonding'`).
- **In-browser (dev preview):** `/lessons/boiling-points-and-imfs/` renders the three badges, the **sorted comparison
  table** (CH₄ dispersion −161.5 → NH₃ hydrogen bonding −33.3 → H₂O hydrogen bonding 100.0, the force climbing with the
  boiling point), the machine-checked trend, the takeaway, the three SHOWN checks, and the intramolecular-vs-intermolecular
  refutation. The Lessons index lists 3 bonding lessons (9 total); the concept + the 3 molecules backlink to it. No console errors.

### State at session end — PHASE 2 OPEN (bonding tier teaching arc COMPLETE)

**9 lessons + 11 gyms (110 problems) + 1 Valence Table (23 elements) + 24 concepts + 7 reaction families (21 examples) +
14 species + 6 molecule structure entries (Lewis ledger + VSEPR + dominant IMF) + 9 formula-sheet entries (= 61 Atlas
reference objects), 7 gates + CI + live Pages, 315 producer tests, 30 pages.** The bonding & structure tier's whole
teaching arc is now complete: the electron-ledger engine, the `molecule` Atlas kind (Lewis + VSEPR + IMF), the
`lewis_structures_v1` gym, and **three lessons across two new lesson shapes** — two `structure` lessons (water, CO₂) and
the `comparison` lesson (the boiling-point trend, its IMF-strength ordering machine-verified). **This session landed four
increments** (ADR-0045 ×2, ADR-0046, ADR-0047). There are now **three lesson shapes** — reaction / structure / comparison —
each a tight schema, dispatched by file extension.
**Next up:** the next major model-bearing tier — **equilibrium & acid-base** (ICE table = the species ledger with
**reversible** extent: n_i = n_{i,0} + ν_i·ξ solved for the ξ that satisfies mass action; K, Q, pH, weak acids, buffers,
titration curves). A large multi-increment undertaking that opens with its own stress scenario — a natural point to
confirm scope/appetite with the owner. Then **kinetics** (dξ/dt) and **electrochemistry** (oxidation numbers → electron
ledger, ΔG=−nFE). Bonding corpus-depth deferrals remain (octet exceptions, resonance, NH₃/CH₄ structure lessons, more
comparison axes, a "which IMF dominates?" gym drill).

## Increment 18 — Phase-2: open equilibrium & acid-base — the reversible-extent solver + weak-acid pH (ADR-0048, Opus 4.8 driving)

Owner: "Read AGENTS.md. Continue the buildout autonomously. Work until you need my input. Multiple increments are fine. You
can open equilibrium/acid-base if appropriate." **Owner-pre-authorized** — opened the **equilibrium & acid-base** tier (the
next flagged major model-bearing tier) on its stress scenario, **the pH of a weak acid**, end to end in one increment. One
ADR (ADR-0048, a tier-opening decision). No honesty-model / house-convention / stack / scope change beyond the
pre-authorized tier (additive schema growth; the model-exact-then-rounded root is the ADR-0040 pattern, not an ADR-0013
weakening — no escalation trigger hit).

### Decision (full rationale in DECISIONS.md)

- **ADR-0048 — the reversible-extent solver + the `equilibrium` lesson kind.** The thesis made literal: *the ICE table is
  the species ledger with the extent solved from mass action* ($Q(x)=K$), not driven to the limiting reagent. (1)
  `chemkernel.equilibrium.solve_equilibrium` builds the ICE ledger ($c_i=c_{i,0}+\nu_i x$) in concentrations and finds the
  root by **bisection to high precision** (exact `Decimal`, general beyond the quadratic — ready for Ksp/buffers/polyprotic);
  the root is **model-exact-then-rounded**, the machine-check the **residual** (Q at the committed concentrations reproduces
  K) + the gate's **independent re-solve**. (2) A new **`equilibrium` lesson kind** (the fourth shape — own tight schema,
  `*.equilibrium.json`, dispatched by extension). (3) Honesty layered: ICE identity ledger-exact (regime-1, re-derived), $K_a$
  data-sourced (regime-3), the equilibrium position/pH model-assumed (regime-2) — triple-badged. (4) $K_a$ from
  `data/ionization-constants.toml` (OpenStax App H); the HA ⇌ H⁺ + A⁻ dissociation **DRY-sourced from acids-bases.toml**.
  (5) pH = −log₁₀[H⁺]; two concepts (`chemical-equilibrium`, `ph`) rather than formula-sheet entries (K/pH need the activity
  treatment, deferred). (6) `equilibriumcheck.mjs` (shared) re-derives the whole spine in pure Node.

### Shipped

- **Data:** `data/ionization-constants.toml` (Kₐ for 4 weak acids, OpenStax App H) + `data.py` loader/accessor
  (`ionization_constant`) + the `ionization_constants` source key; SOURCES.md App H coverage.
- **Engine:** `chemkernel/equilibrium.py` — `solve_equilibrium` (ICE + mass-action bisection + residual; refuses an
  unbracketed root / nonpositive K / negative initial / a degenerate reaction) + `build_equilibrium_lesson` (the weak-acid
  pH; refuses a strong or polyprotic acid, or one with no curated Kₐ).
- **Contracts:** `schemas/equilibrium-lesson.schema.json` (new). **build.py:** `build_equilibrium` + the fourth
  `*.equilibrium.toml` dispatch.
- **Content:** `problems/equilibrium/acetic-acid-ph.equilibrium.toml`; `reference/concepts/{chemical-equilibrium,ph}.toml`.
- **Gates:** `scripts/validate/equilibriumcheck.mjs` (new, shared — the pure-Node reversible-extent re-solver:
  `reactionQuotient`/`solveEquilibrium`/`verifyEquilibrium`); `validate-solutions.mjs` (the `*.equilibrium.json` walk +
  schema + honesty + `verifyEquilibrium`); `check-katex.mjs` + `validate-reference.mjs` learn `.equilibrium.json`.
- **Player:** `src/components/EquilibriumLesson.astro` (static — the ICE table, the mass-action check, the pH headline, the
  refuted misconception, the SHOWN checks) + `view.renderEquilibriumLesson` (+ a `toSci` scientific-notation helper);
  `lessons/[slug].astro` + `lessons/index.astro` dispatch across all four lesson kinds.
- **Tests:** `producer/tests/test_equilibrium.py` (+18 — the solver's numerics/residual, ICE identity, pH, the refusals,
  the data accessor, the build round-trip).
- **Docs:** ADR-0048; CHANGELOG; ROADMAP (status Now line + the equilibrium landed line + a Phase-2 equilibrium scope block +
  the "Then" sketch); AGENTS (counters 10 lessons/26 concepts/63 objects/333 tests/31 pages, the four-lesson-shapes standing
  fact, where-next → the rest of equilibrium); architecture (as-built header + counters + the `equilibrium.py` row + data.py/
  build.py/validate-solutions/check-katex rows + the four-schemas note); regime-map (the equilibrium + acids-bases rows →
  lesson+atlas); authoring-problems (an Equilibrium-lessons section + the intro pointer); README; SOURCES; this log.

### Verification (exact counts)

- **333 producer tests green** (`uv --project producer run pytest`, ~16 s; +18).
- **7 Node gates green:** **validate-solutions = 6 solutions + 2 structure + 1 comparison + 1 equilibrium** (10 unique ids),
  **validate-reference = 63** (+2 concepts), validate-gyms = 11/110, check-ledger = 6/24 + 1 gas + 1 energy, check-parity =
  320 + 36, **check-katex = 530** (+28), scan clean. **`astro build` = 31 pages** (+1: `/lessons/acetic-acid-ph/`).
  `derived/` **byte-stable** — only the 3 new files added (the equilibrium lesson + 2 concepts); no existing derived changed.
- **Equilibrium gate proven non-vacuous — 5 tamper tests, each caught by a distinct branch:** corrupt the extent
  (`equilibrium_M != initial + ν·x` — ICE identity); a **coherent** wrong extent that satisfies the ICE identity
  (`extent_M != independently re-solved root` — the mass-action re-solve, the key proof the residual check is real); corrupt
  Kₐ (`!= re-solved root`); corrupt pH (`!= -log10[H+]`); corrupt the quotient (`quotient_at_equilibrium != K`).
- **In-browser (dev preview):** `/lessons/acetic-acid-ph/` renders the three badges (ICE machine-checked / Kₐ data-sourced
  (openstax-chemistry-2e) / 3 modeling assumptions), the reaction HC₂H₃O₂ ⇌ H⁺ + C₂H₃O₂⁻ with Kₐ = [H⁺][C₂H₃O₂⁻]/[HC₂H₃O₂]
  = 1.8×10⁻⁵, the **ICE table** (0.100 / −x=−0.00133 / 0.0987 …), the **mass-action check** "x = 0.00133 M — the extent
  where Q = Kₐ" with the substitution = 1.8×10⁻⁵ = Kₐ ✓ (residual 3.3×10⁻¹²), the result cards ([H⁺] 0.00133 M / **pH 2.88**
  / un-ionized 0.0987 M), the 4 SHOWN checks, and the misconception refuted from the ledger (1.33% ionizes, 98.67% intact).
  All 5 concept chips resolve. **No console errors or warnings**; KaTeX rendered (0 error nodes).

### Deferred / open (recorded in ROADMAP + ADR-0048)

The rest of the equilibrium tier, all reusing `solve_equilibrium`: the weak **base** (Kb/pOH/Kw); **Ksp** (the pure solid's
activity = 1, excluded from Q — a small solver refinement); **buffers** (both HA + A⁻ present — the reverse-direction bracket
the solver already supports; Henderson–Hasselbalch); **titration curves**; **polyprotic** staged equilibria (H₃PO₄); a
weak-acid **gym** (solve-for-pH / solve-for-Kₐ); the K/pH/Kw **formula-sheet** entries (need the activity treatment). Then
kinetics, electrochemistry.

### State at session end — PHASE 2 OPEN (equilibrium & acid-base opened)

**10 lessons + 11 gyms (110 problems) + 1 Valence Table (23 elements) + 26 concepts + 7 reaction families (21 examples) + 14
species + 6 molecule structure entries + 9 formula-sheet entries (= 63 Atlas reference objects), 7 gates + CI + live Pages,
333 producer tests, 31 pages.** The equilibrium & acid-base tier is open on its stress scenario: the **reversible-extent
solver** (`chemkernel.equilibrium.solve_equilibrium` — the ICE table = the species ledger with the extent solved from mass
action Q=K, by bisection to high precision, the machine-check the residual + an independent re-solve) + the new **`equilibrium`
lesson kind** (`equilibrium/acetic-acid-ph` — a weak acid's pH). There are now **four lesson shapes** (reaction / structure /
comparison / equilibrium), each a tight schema dispatched by file extension. Five increments landed this session (ADR-0045 ×2,
ADR-0046, ADR-0047, ADR-0048).
**Next up:** continue the equilibrium tier — the natural next increment is **Ksp solubility equilibria** (the smallest
extension: the pure solid's activity = 1, excluded from Q; reuses the solver + a curated Ksp datum) or the **weak base**
(Kb → pOH → pH via Kw). Buffers/titration/polyprotic + a weak-acid gym follow; then kinetics and electrochemistry. The K/pH
formula-sheet entries want the activity (dimensionless) treatment first.

## Increment 19 — Phase-2 equilibrium: Ksp solubility — the solver proven on the cubic (ADR-0048 2nd increment, Opus 4.8 driving)

Owner: "Context is good (42%). Continue." Continued the equilibrium tier with its flagged next increment — **Ksp solubility
equilibria** — which proves the reversible-extent solver **generalizes**: the same machine, a structurally different
equilibrium (a pure solid excluded from Q → a **cubic**, the bisection solver's whole justification). Inside the ADR-0048
contract (Ksp was the anticipated deferral) — **no new ADR**, a 2nd-increment note appended to ADR-0048. No honesty-model /
house-convention / scope change (additive: a `subtype` on the existing lesson kind + a solver flag).

### What shipped

- **Engine:** `solve_equilibrium` gains an **`in_quotient`** per-species flag — a pure solid (activity 1) is excluded from Q
  and, in excess, does not bound the forward extent (the bracket is grown until Q > K). `build_solubility_lesson` (the Ksp
  path — the solid excluded, K_sp=4s³ a cubic, → molar solubility s + g/L); the `equilibrium` lesson kind now has **two
  subtypes** (weak-acid / solubility) under one schema (a `subtype` discriminator), dispatched in `build_equilibrium` by
  `acid` vs `salt`. `_regimes(position_facet)` made subtype-aware (pH / solubility).
- **Data:** `data/solubility-products.toml` (K_sp, OpenStax App J: CaF₂ 3.45e-11, Mg(OH)₂ 5.61e-12) + the `data.py`
  loader/accessor — the ion counts derived by **charge crossover** + the salt composition machine-checked on load (regime-1,
  like acids-bases); the `solubility-product` concept (backlinks `precipitation` — closing the Phase-0 loop).
- **Contracts:** `equilibrium-lesson.schema.json` grew the `subtype` discriminator, the solid ICE row (`in_quotient` +
  optional concentration fields), and the solubility `reaction`/`result` fields (all optional in the base; the
  subtype-specific presence enforced in the gate — Ajv `strictRequired` can't cross subschemas, so the `allOf`/if-then was
  dropped for gate checks).
- **Gate:** `equilibriumcheck.mjs` handles the solid row (no concentration, excluded from Q via an inQ mask) + re-derives the
  molar solubility + s×molar mass = g/L; the re-solve grows the bracket for the dissolving solid; `validate-solutions` enforces
  the subtype-specific fields.
- **Player:** `EquilibriumLesson.astro` rewritten to render **both** subtypes (the solid row shows "— pure solid · excluded
  from Ksp"; the change column the symbolic ν·s incl. **+2s**; the solubility result cards; the cubic misconception).
  `view.renderEquilibriumLesson` subtype-aware tokens + `saltPretty`. The weak-acid lesson gained cleaner phase labels + the
  symbolic change column as a bonus.
- **Content:** `problems/equilibrium/calcium-fluoride-solubility.equilibrium.toml`.
- **Tests:** `test_equilibrium.py` +7 (the cubic root, the pure solid excluded from Q, the CaF₂ lesson build, the residual,
  the unknown-salt refusal, the data accessor + crossover, the build round-trip).
- **Docs:** CHANGELOG; ROADMAP (status + the equilibrium block Ksp-landed); AGENTS (counters 11 lessons/27 concepts/64
  objects/340 tests/32 pages, the two-subtypes standing fact, where-next); architecture (counters + equilibrium.py/data.py/
  build.py/validate-solutions rows); regime-map (the solubility-equilibria row → lesson+atlas); SOURCES (App J); ADR-0048
  2nd-increment note; authoring-problems (the salt subtype); README; this log.

### Verification (exact counts)

- **340 producer tests green** (+7).
- **7 Node gates green:** **validate-solutions = 6 + 2 structure + 1 comparison + 2 equilibrium** (11 unique ids),
  validate-reference = 64 (+1 concept), validate-gyms = 11/110, check-ledger = 6/24 + 1 gas + 1 energy, check-parity = 320 +
  36, **check-katex = 555** (+25), scan clean. **`astro build` = 32 pages** (+1). `derived/` byte-stable — the acetic lesson
  gained `subtype` (a deliberate one-field change, re-verified), + 2 new files (the CaF₂ lesson + the concept).
- **Ksp gate proven non-vacuous — 4 tamper tests, each a distinct branch:** a **coherent** wrong solubility (ICE identity
  holds) → the **independent re-solve** (extent != re-solved root); a corrupted fluoride coefficient (ν 2→1) → the ICE
  identity; a bad solubility g/L → solubility-consistent; the solid forced into Q with a fake concentration → the ICE identity.
- **In-browser (dev preview):** `/lessons/calcium-fluoride-solubility/` renders the badges (K sp data-sourced), the reaction
  CaF₂(s) ⇌ Ca²⁺(aq) + 2 F⁻(aq), K_sp = [Ca²⁺][F⁻]² = 3.45×10⁻¹¹, the **ICE table** with the solid row "— pure solid ·
  excluded from Ksp" + Ca²⁺ (+s → 0.000205) + F⁻ (**+2s** → 0.00041), the mass-action check "s = 0.000205 M … Q = Ksp"
  with the substitution = 3.45×10⁻¹¹ = Ksp ✓ (residual 1.7×10⁻¹², "a cubic — bisection, not a formula"), the result cards
  (molar solubility 0.000205 M / 0.016 g/L / saturated ions), the 4 SHOWN checks, and the cubic misconception ("~35× larger
  than √Ksp; the coefficient enters twice"). All 5 concept chips resolve incl. **Precipitation** (the Phase-0 loop closed).
  The weak-acid lesson still renders correctly (shared component, no regression). No console errors.

### Deferred / open (recorded in ROADMAP + ADR-0048)

Still the rest of the tier, all reusing `solve_equilibrium`: the weak **base** (Kb/pOH/Kw); **buffers** (both HA + A⁻ present
— the reverse bracket the solver already supports); **titration curves**; **polyprotic** (H₃PO₄); a weak-acid **gym**;
**common-ion Ksp** + Q-vs-Ksp **precipitation prediction** (the natural Ksp follow-on); the K/pH/Kw **formula-sheet** entries
(the activity treatment). Then kinetics + electrochemistry.

### State at session end — PHASE 2 OPEN (equilibrium: weak-acid pH + Ksp solubility)

**11 lessons + 11 gyms (110 problems) + 1 Valence Table (23 elements) + 27 concepts + 7 reaction families (21 examples) + 14
species + 6 molecule structure entries + 9 formula-sheet entries (= 64 Atlas reference objects), 7 gates + CI + live Pages,
340 producer tests, 32 pages.** The equilibrium & acid-base tier now has **two lessons across the `equilibrium` kind's two
subtypes** — weak-acid pH (a quadratic) and Ksp solubility (a **cubic**, the pure solid excluded from Q) — both on the one
`solve_equilibrium` reversible-extent solver (bisection, general beyond the quadratic; the machine-check the residual + an
independent re-solve). Two increments landed this session in the equilibrium tier (ADR-0048 opener + this 2nd increment).
There are **four lesson shapes** (reaction / structure / comparison / equilibrium), the last with two subtypes.
**Next up:** more of the equilibrium tier — the **weak base** (Kb → pOH → pH via Kw, reuses the solver unchanged; needs
ammonia + Kb curated) or **common-ion Ksp + Q-vs-Ksp precipitation prediction** (the natural Ksp follow-on, quantifying the
Phase-0 precipitation rules further). Then buffers, titration, polyprotic, a weak-acid gym; then kinetics, electrochemistry.

## Increment 20 — Phase-2 equilibrium: the weak base (Kb → pOH → pH via Kw) — the solver reused unchanged (ADR-0048 3rd increment, Opus 4.8 driving)

Owner: "Read AGENTS.md. Continue the buildout autonomously. Work until you need my input. Multiple increments are fine. You
can open equilibrium/acid-base if appropriate." Equilibrium was already open; continued it with the flagged next increment —
the **weak base**. It is the cleanest reuse in the tier so far: a weak base ionizes against water, and **water is the pure
solvent** (activity 1) excluded from Q by the *same* `in_quotient=False` flag the Ksp solid uses, so `solve_equilibrium`
needed **no change**. Inside the ADR-0048 contract (the weak base was the anticipated deferral) — **no new ADR**, a
3rd-increment note appended. No honesty-model / house-convention / scope change (additive: a `weak-base` subtype + a data
extension).

### What shipped

- **Engine:** `build_weak_base_lesson` — B + H₂O ⇌ BH⁺ + OH⁻; water passed to `solve_equilibrium` as an `in_quotient=False`,
  ν=−1 reactant (excluded from Q, exactly the Ksp solid). The extent is [OH⁻]; the **Kw bridge** gives the pH:
  [H⁺] = Kw/[OH⁻], pH = −log₁₀[H⁺], pH + pOH = pKw. `build_equilibrium` gains a `base` dispatch. The `equilibrium` lesson
  kind now has **three subtypes** (dispatched `salt`/`base`/`acid`).
- **Data:** `data/ionization-constants.toml` extended — a `[bases.*]` table (NH₃: Kb 1.8×10⁻⁵ + `conjugate_acid` NH4^+)
  and a `[water]` table (Kw 1.0×10⁻¹⁴). `data.py` gains `base_ionization_constant` + `water_ion_product`, and **machine-checks
  each base's proton accounting on load** (base + H⁺ = the conjugate-acid cation, a +1 ion; regime-1). `data/acids-bases.toml`
  (and the reaction classifier that reads it) is deliberately **left untouched** — a molecular weak base doesn't fit that
  file's hydroxide-base model, and modeling it in the equilibrium dataset avoids any classifier regression.
- **Contracts:** `equilibrium-lesson.schema.json` — `weak-base` added to the subtype enum; reaction `base`/`base_name`/
  `base_latex`/`conjugate_acid`; result `hydroxide_M`/`pOH`(+displays)/`kw`; the `kw_consistent` check.
- **Gate:** `equilibriumcheck.mjs` — a `weak-base` branch re-derives pOH = −log₁₀[OH⁻], the Kw bridge [H⁺] = Kw/[OH⁻], the
  pH, and **pH + pOH = pKw**; the water row (in_quotient false) is the excluded solvent. `validate-solutions` enforces the
  subtype fields.
- **Player:** `EquilibriumLesson.astro` renders all three subtypes (a phase-aware excluded row — "pure liquid" for water,
  "pure solid" for a Ksp salt; a new Kw-bridge note; base result cards [OH⁻]/pOH/pH; the strong-base + mirror misconception).
  `view.renderEquilibriumLesson` subtype-aware tokens + `basePretty` + `kwSci`.
- **Content:** `problems/equilibrium/ammonia-ph.equilibrium.toml`; a new **`water-autoionization` concept** (the Kw bridge);
  `ph`/`chemical-equilibrium` concepts cross-linked to the new lesson.
- **Tests:** `test_equilibrium.py` +10 (the base solve mirrors the acid; water excluded from Q; the ammonia build; the Kw
  bridge pH+pOH=14; the ICE identity with water excluded; the refusals; the data accessors; the build round-trip).
- **Docs:** ADR-0048 3rd-increment note; CHANGELOG; ROADMAP (Now + the equilibrium landed line + the scope block); AGENTS
  (counters 12 lessons/28 concepts/65 objects/350 tests/33 pages, the three-subtypes standing fact, where-next); architecture
  (counters + equilibrium.py/data.py/validate-solutions rows + the bottom module row); regime-map (acids-bases + equilibrium
  rows); SOURCES (Appendix I + §14.1 Kw); authoring-problems (the weak-base subtype); README; this log.

### Verification (exact counts)

- **350 producer tests green** (+10).
- **7 Node gates green:** **validate-solutions = 6 + 2 structure + 1 comparison + 3 equilibrium** (12 unique ids),
  **validate-reference = 65** (+1 concept), validate-gyms = 11/110, check-ledger = 6/24 + 1 gas + 1 energy, check-parity =
  320 + 36, **check-katex = 588** (+33), scan clean. **`astro build` = 33 pages** (+1: `/lessons/ammonia-ph/`). `derived/`
  **byte-stable** — 2 new files (the ammonia lesson + the water-autoionization concept) + the ph/chemical-equilibrium
  cross-link edits; no other derived changed.
- **Equilibrium gate proven non-vacuous for the weak base — 7 tamper tests, each caught by a distinct branch:** ICE identity
  (equilibrium_M ≠ initial + ν·x); a **coherent** wrong extent (satisfies ICE) → the independent re-solve; corrupt pOH; the
  Kw-bridge hydronium ([H⁺] ≠ Kw/[OH⁻]); corrupt pH (pH+pOH ≠ pKw); a corrupted Kw; water forced into Q (in_quotient true).
- **In-browser (dev preview):** `/lessons/ammonia-ph/` renders the three badges (Kb data-sourced), NH₃(aq) + H₂O(l) ⇌
  NH₄⁺(aq) + OH⁻(aq), Kb = [NH₄⁺][OH⁻]/[NH₃] = 1.8×10⁻⁵, the **ICE table** (NH₃ 0.100/−x/0.0987 · H₂O "— pure liquid ·
  excluded from Q" · NH₄⁺ & OH⁻ 0/+x/0.00133), the mass-action check (x = 0.00133 M = [OH⁻], Q = Kb ✓, residual 3.3×10⁻¹²),
  the **Kw bridge** ([H⁺] = Kw/[OH⁻] = 7.5×10⁻¹² M, pH + pOH = 11.12 + 2.88 = 14.00), the result cards ([OH⁻] 0.00133 M /
  pOH 2.88 / **pH 11.12**), the 4 SHOWN checks, and the mirror misconception. No regression on acetic-acid (pH 2.88) or CaF₂
  (0.000205 M). **No console errors; 0 KaTeX error nodes.**

### State at session end — PHASE 2 OPEN (equilibrium: weak-acid pH + weak-base pH + Ksp solubility)

**12 lessons + 11 gyms (110 problems) + 1 Valence Table (23 elements) + 28 concepts + 7 reaction families (21 examples) + 14
species + 6 molecule structure entries + 9 formula-sheet entries (= 65 Atlas reference objects), 7 gates + CI + live Pages,
350 producer tests, 33 pages.** The equilibrium & acid-base tier now has **three lessons across the `equilibrium` kind's
three subtypes** — weak-acid pH (a quadratic), weak-base pH (the Kw bridge, water excluded from Q — the solver reused
unchanged), and Ksp solubility (a cubic, the pure solid excluded from Q) — all on the one `solve_equilibrium` reversible-extent
solver. The acetic-acid and ammonia lessons are exact mirrors across neutral (pH 2.88 ↔ pH 11.12). Three increments landed
this session in the equilibrium tier (ADR-0048 opener + Ksp + this weak base). There are **four lesson shapes** (reaction /
structure / comparison / equilibrium), the last with three subtypes.
**Next up:** more of the equilibrium tier — **buffers** (both HA + A⁻ present initially — the reverse-direction bracket the
solver already supports; Henderson–Hasselbalch is then the ICE ledger read off), or a **weak-acid/base gym** (the tier's
drill instrument — solve-for-pH / solve-for-K, model-exact-then-rounded, free-entry per ADR-0032). Then titration,
polyprotic, common-ion Ksp + Q-vs-Ksp precipitation prediction; then kinetics, electrochemistry.

## Increment 21 — Phase-2 equilibrium: buffers — the common-ion effect + Henderson–Hasselbalch (ADR-0048 4th increment, Opus 4.8 driving)

Continued the equilibrium tier with the flagged **buffer** increment — the fourth subtype, and the one that exercises the
solver's **nonzero-initial-product** case for real. A buffer is the *same* weak-acid reaction (HA ⇌ H⁺ + A⁻) and the *same*
solver, but the conjugate base A⁻ is **already present** ([A⁻]₀ > 0, from a dissolved salt). Inside the ADR-0048 contract
(buffers were the anticipated deferral) — **no new ADR**, a 4th-increment note appended. No honesty-model / house-convention /
scope change (additive: a `buffer` subtype).

### What shipped

- **Engine:** `build_buffer_lesson` — HA ⇌ H⁺ + A⁻ with [A⁻]₀ > 0; the pre-loaded product is a **common ion** that (Le
  Chatelier) suppresses ionization, so the pH sits near pKₐ. The signature is **Henderson–Hasselbalch**, pH = pKₐ +
  log₁₀([A⁻]/[HA]) — which is just the mass-action law logged, so it is **machine-checked on the equilibrium concentrations**
  (it must reproduce −log₁₀[H⁺]). The builder also **re-solves the acid alone** ([A⁻]₀ = 0) to quantify the common-ion
  suppression. `build_equilibrium` dispatches `acid` **with** `conjugate_base_molarity_M` → buffer, `acid` alone → weak-acid.
  Four subtypes now.
- **Contracts:** `equilibrium-lesson.schema.json` — `buffer` added to the subtype enum; it reuses the weak-acid reaction
  fields; new result fields (pKa, buffer_ratio, hh_pH, the no-buffer contrast hydronium_no_buffer_M / pH_no_buffer /
  suppression_factor); the `hh_consistent` check.
- **Gate:** `equilibriumcheck.mjs` — a `buffer` branch re-derives pKₐ = −log₁₀(Kₐ), the buffer ratio [A⁻]/[HA], **Henderson–
  Hasselbalch** (pH = pKₐ + log₁₀(ratio) reproducing −log₁₀[H⁺]), and the common-ion contrast (by re-solving the acid alone).
  `validate-solutions` enforces the subtype fields.
- **Player:** `EquilibriumLesson.astro` renders the buffer (result cards pH/ratio/% ionized + an H–H note + the common-ion
  misconception). `view.renderEquilibriumLesson` gains `conjugateBasePretty`; the H–H note reuses the Kw-bridge styling.
- **Content:** `problems/equilibrium/acetate-buffer.equilibrium.toml`; a new **`buffer` concept** (common-ion +
  Henderson–Hasselbalch); `ph`/`chemical-equilibrium` cross-linked.
- **Tests:** `test_equilibrium.py` +6 (the buffer solve with nonzero initial product; the build; the H–H identity; the
  common-ion suppression; the dispatch — `acid` alone stays weak-acid; the refusal).
- **Docs:** ADR-0048 4th-increment note; CHANGELOG; ROADMAP (Now + the equilibrium landed line + the scope block); AGENTS
  (counters 13 lessons/29 concepts/66 objects/356 tests/34 pages, the four-subtypes standing fact, where-next); architecture
  (counters + equilibrium.py/validate-solutions rows + the bottom module row); regime-map (buffers & titrations + equilibrium
  rows); authoring-problems (the buffer subtype); README; this log.

### Verification (exact counts)

- **356 producer tests green** (+6).
- **7 Node gates green:** **validate-solutions = 6 + 2 structure + 1 comparison + 4 equilibrium** (13 unique ids),
  **validate-reference = 66** (+1 concept), validate-gyms = 11/110, check-ledger = 6/24 + 1 gas + 1 energy, check-parity =
  320 + 36, **check-katex = 619** (+31), scan clean. **`astro build` = 34 pages** (+1: `/lessons/acetate-buffer/`). `derived/`
  **byte-stable** — 2 new files (the buffer lesson + the buffer concept) + the ph/chemical-equilibrium cross-link edits.
- **Buffer gate proven non-vacuous — 8 tamper tests, each a distinct branch:** ICE identity; a coherent wrong extent → the
  independent re-solve; pH; pKₐ; the buffer ratio; the Henderson–Hasselbalch identity (hh_pH); the no-buffer pH → the
  acid-alone re-solve; the suppression factor.
- **In-browser (dev preview):** `/lessons/acetate-buffer/` renders the ICE table (**A⁻ starting at 0.100** — the buffer
  signature), the mass-action check (x = 0.000018 M, Q = Kₐ ✓), the result cards (**pH 4.74 ≈ pKₐ 4.74** / buffer ratio 1 /
  0.018% ionized), the **Henderson–Hasselbalch note** (pH = pKₐ + log₁₀(1) = 4.74 = −log₁₀[H⁺]), and the common-ion
  misconception (the acetate suppresses ionization **74×** → pH 4.74 not 2.88). No regression on the other three equilibrium
  lessons; 0 KaTeX errors.

### State at session end — PHASE 2 OPEN (equilibrium: weak-acid pH + buffer + weak-base pH + Ksp solubility)

**13 lessons + 11 gyms (110 problems) + 1 Valence Table (23 elements) + 29 concepts + 7 reaction families (21 examples) + 14
species + 6 molecule structure entries + 9 formula-sheet entries (= 66 Atlas reference objects), 7 gates + CI + live Pages,
356 producer tests, 34 pages.** The equilibrium & acid-base tier now has **four lessons across the `equilibrium` kind's four
subtypes** — weak-acid pH (a quadratic), buffer (the common-ion effect + Henderson–Hasselbalch, the nonzero-initial-product
case), weak-base pH (the Kw bridge, water excluded from Q), and Ksp solubility (a cubic, the pure solid excluded from Q) —
all on the one `solve_equilibrium` reversible-extent solver. Four increments landed this session in the equilibrium tier
(ADR-0048 opener + Ksp + weak base + this buffer). There are **four lesson shapes** (reaction / structure / comparison /
equilibrium), the last with four subtypes.
**Next up:** more of the equilibrium tier — a **weak-acid/base gym** (the tier's drill instrument — solve-for-pH /
solve-for-K, model-exact-then-rounded, free-entry per ADR-0032; the one surface the tier still lacks that every other tier
has), or **titration curves** (the ledger marched as titrant is added — a multi-point version of the same solve). Then
polyprotic (H₃PO₄ staged Kₐ₁/Kₐ₂/Kₐ₃), common-ion Ksp + Q-vs-Ksp precipitation prediction, the K/pH/Kw formula-sheet entries
(the activity treatment); then kinetics ($d\xi/dt$) and electrochemistry.

## Increment 22 — Phase-2 equilibrium: the weak-acid pH gym — the tier's drill instrument (ADR-0048 5th increment, Opus 4.8 driving)

Continued the equilibrium tier with the flagged **gym** — the one surface it still lacked (every other tier has one). The
drill is the **pH of a weak acid**, generated over the curated weak acids + concentrations. Inside the ADR-0048 contract (the
gym was the anticipated deferral) — **no new ADR**, a 5th-increment note appended. No honesty-model / house-convention /
scope change (additive: a new gym family).

### What shipped

- **Engine:** `gym._generate_weak_acid_ph` — the pH found the honest way, via `equilibrium.solve_equilibrium`'s mass-action
  root (Q=Kₐ by bisection, **no small-x approximation**), the same machine as the acetic-acid lesson. A `_sci` helper renders
  Kₐ in Unicode scientific notation for the prompt. The `weak_acid_ph_v1` family registered (+ its model-assumed disclosure
  + the `ionization_constants` provenance source). **Model-exact-then-rounded AND data-sourced** (both badges, like calorimetry).
- **One solver, three call sites:** the gate's re-derivation reuses `solveEquilibrium` from `equilibriumcheck.mjs` — the same
  shared Node solver the equilibrium lessons' gate uses — so the Python builder, the lesson gate, and the gym gate all agree.
- **Contracts:** `gym.schema.json` — the `weak_acid_ph` derivation kind + an `equilibrium` block (acid/ka/c0) + the
  `ionization_constants` provenance source key.
- **Gate:** `verifyWeakAcidPh` in `validate-gyms.mjs` (re-solve the root + re-check pH = −log₁₀[H⁺], model-exact tolerance).
- **Content:** `gyms/equilibrium/weak-acid-ph.gym.toml` (10 drills, seed 1618033). Free-entry numeric (ADR-0032); the named
  diagnostics are the canonical weak-acid errors — treated-as-strong (pH = −log[HA]₀), Kₐ-as-[H⁺] (pKₐ), dropped-the-√.
- **Review catch:** a scientific-notation Decimal (Kₐ) must be emitted with `format(d, "f")`, not `_trim(str(d))` — the latter
  strips the exponent's trailing zero (4.9E-10 → 4.9E-1), which the gate's `Number()` would misread. Guarded by a test.
- **Tests:** `test_gym.py` +6 (shape/both-badges; answers reproduce the root; the Kₐ round-trip guard; the strong-acid
  diagnostic present; learnable range; determinism).
- **Docs:** ADR-0048 5th-increment note; CHANGELOG; ROADMAP (Now + the equilibrium landed line + the scope block); AGENTS
  (counters 12 gyms/120 problems/362 tests/35 pages, where-next); architecture (counters + gym.py + validate-gyms rows);
  regime-map (acids & bases row → + gym); authoring-gyms (the family); README; this log.

### Verification (exact counts)

- **362 producer tests green** (+6).
- **7 Node gates green:** validate-solutions = 6 + 2 structure + 1 comparison + 4 equilibrium (13 ids), validate-reference =
  66, **validate-gyms = 12 gyms / 120 problems** (+1 gym / +10), check-ledger = 6/24 + 1 gas + 1 energy, check-parity =
  320 + 36, check-katex = 619 (**unchanged** — gym prompts are Unicode prose, not KaTeX), scan clean. **`astro build` = 35
  pages** (+1: `/gym/weak-acid-ph/`). `derived/` **byte-stable** — 1 new file (the gym); no existing derived changed.
- **In-browser (dev preview):** `/gym/weak-acid-ph/` renders the blurb, the model-assumed + data-sourced badges, the model
  disclosure, and a drill ("A 0.2 M solution of hydrocyanic acid (HCN) has Kₐ = 4.9×10⁻¹⁰. What is the pH?" — the formula +
  Kₐ scientific notation both render). **Free-entry works end-to-end:** typing the correct pH ("5.00") gives "✓ Correct — 5"
  with the work-through chain ([H⁺] = 9.9×10⁻⁶ M → pH 5) + the WHY; typing the strong-acid mistake ("0.7") fires the exact
  named diagnostic ("Treated the weak acid as strong … Only a small fraction of a WEAK acid ionizes"). Concept chips resolve.

### State at session end — PHASE 2 OPEN (equilibrium: 4 lesson subtypes + the weak-acid pH gym)

**13 lessons + 12 gyms (120 problems) + 1 Valence Table (23 elements) + 29 concepts + 7 reaction families (21 examples) + 14
species + 6 molecule structure entries + 9 formula-sheet entries (= 66 Atlas reference objects), 7 gates + CI + live Pages,
362 producer tests, 35 pages.** The equilibrium & acid-base tier now has **all four `equilibrium` lesson subtypes**
(weak-acid pH · buffer · weak-base pH · Ksp solubility) **plus its `weak_acid_ph_v1` gym** — the drill instrument every other
tier has. One `solve_equilibrium` reversible-extent solver underlies all of them, with **one shared Node re-solver**
(`equilibriumcheck.mjs`'s `solveEquilibrium`) now used by three gates (lessons + the gym). Five increments landed this session
in the equilibrium tier (ADR-0048 opener + Ksp + weak base + buffer + this gym).
**Next up:** more of the equilibrium tier — **titration curves** (the ledger marched as titrant is added — the natural
multi-point extension of the same solve, and the tier's remaining marquee topic; likely wants a small interactive/plot), or
**polyprotic** staged equilibria (H₃PO₄, Kₐ₁/Kₐ₂/Kₐ₃ — sequential solves), or **common-ion Ksp + Q-vs-Ksp** precipitation
prediction (the natural Ksp follow-on). The K/pH/Kw **formula-sheet** entries want the activity (dimensionless) treatment
first. Then **kinetics** ($d\xi/dt$, rate laws, half-life) and **electrochemistry** (oxidation numbers → electron ledger,
ΔG = −nFE, Nernst). A weak-**base**/buffer gym extension is a smaller optional follow-on.

## Increment 23 — Phase-2 equilibrium: the common-ion effect on solubility (ADR-0048 6th increment, Opus 4.8 driving)

Resumed from the increment-22 pointer and took the flagged **common-ion Ksp** follow-on. A sparingly soluble salt dissolves
not into pure water but into a solution that **already contains one of its own ions** (from a fully-dissociated soluble salt).
That shared ion is a **nonzero initial product concentration** — the *exact* case the buffer proved on the quadratic — so
`solve_equilibrium` is **reused unchanged** on the Ksp **cubic**, and Le Chatelier drives the dissolution left. Inside the
ADR-0048 contract (common-ion Ksp was an anticipated deferral) — **no new ADR**, a 6th-increment note appended. No
honesty-model / house-convention / scope change (additive: a variant of the `solubility` subtype, not a new subtype).

### What shipped

- **Engine:** `build_solubility_lesson` gained an optional `common_ion` (one of the salt's own ions) + `common_ion_molarity_M`.
  It sets that ion's initial concentration, solves (the solver already handled nonzero initial products), and **re-solves the
  salt in pure water** for the suppression contrast — mirroring the buffer's acid-alone re-solve. Refuses a `common_ion` not
  shared with the salt, or a non-positive molarity.
- **Contracts:** `equilibrium-lesson.schema.json` — additive: reaction `common_ion`/`common_ion_latex`/`common_ion_molarity_M`;
  result `molar_solubility_pure_water_M`(`_display`); `suppression_factor_display` **reused** from the buffer. The subtype enum
  stays four; `checks` unchanged.
- **Gate:** `equilibriumcheck.mjs`'s solubility branch — when `reaction.common_ion` is present, re-checks the ion's initial vs
  the reaction block and **re-solves pure water** to reproduce `molar_solubility_pure_water_M` + the suppression factor. The
  core re-solve already accounts for the nonzero initial. `validate-solutions` enforces the variant's fields.
- **Player:** `EquilibriumLesson.astro` — an `isCommonIon` branch: a "vs. pure water" result card, a **common-ion effect** note
  (pure-water vs suppressed solubility + the Le Chatelier reason), and a distinct misconception refutation ("solubility is not
  a fixed property"). `view.js` adds the common ion to the formula tokens + `commonIonPretty`.
- **Content:** `problems/equilibrium/calcium-fluoride-common-ion.equilibrium.toml` (CaF₂ into 0.10 M F⁻ from NaF); a new
  **`common-ion-effect` concept** (the one principle behind buffers + suppressed solubility), cross-linked from `buffer` and
  `solubility-product` (the latter also now lists both Ksp lessons).
- **Tests:** `test_equilibrium.py` +6 (the solver's nonzero-initial-product on the cubic; the build + suppression; mass action
  reproduces Ksp; the foreign-ion refusal; the non-positive-molarity refusal; the round-trip; a guard that a *plain* Ksp lesson
  still emits no common-ion fields).
- **Docs:** ADR-0048 6th-increment note; CHANGELOG; ROADMAP (Now + both equilibrium scope blocks); AGENTS (counters 14
  lessons / 30 concepts / 67 objects / 369 tests / 36 pages, the solubility standing fact, where-next); architecture (counters
  + the `build_solubility_lesson` + validate-solutions rows); regime-map (the solubility row); authoring-problems (the
  common-ion variant); README; this log.

### Verification (exact counts)

- **369 producer tests green** (+7).
- **7 Node gates green:** **validate-solutions = 6 + 2 structure + 1 comparison + 5 equilibrium** (14 unique ids),
  **validate-reference = 67** (+1 concept), validate-gyms = 12/120, check-ledger = 6/24 + 1 gas + 1 energy, check-parity =
  320 + 36, **check-katex = 643** (+24), scan clean. **`astro build` = 36 pages** (+1: `/lessons/calcium-fluoride-common-ion/`).
  `derived/` **byte-stable** — 2 new files (the lesson + the concept) + the buffer/solubility-product cross-link edits.
- **Common-ion gate proven non-vacuous — 3 tamper tests**, each a distinct new branch: the suppression factor (59400→100);
  the pure-water contrast solubility; the common ion's initial concentration vs the reaction block. Each flips PASS→FAIL, and
  the restored (regenerated) file passes.
- **In-browser (dev preview):** `/lessons/calcium-fluoride-common-ion/` renders the ICE table with **F⁻ pre-loaded at 0.10**
  (Ca²⁺ 0 → +s → 3.45×10⁻⁹, F⁻ 0.10 → +2s → ~0.100), the mass-action check (Q = Ksp ✓), the result cards including **"59400×
  less"** (0.000205 M → 0.00000000345 M), the common-ion effect note, and the refuted "solubility is a fixed property"
  misconception. 0 KaTeX errors; no regression on the other four equilibrium lessons.

### State at session end — PHASE 2 OPEN (equilibrium: 4 lesson subtypes incl. a common-ion solubility variant + the weak-acid gym)

**14 lessons + 12 gyms (120 problems) + 1 Valence Table (23 elements) + 30 concepts + 7 reaction families (21 examples) + 14
species + 6 molecule structure entries + 9 formula-sheet entries (= 67 Atlas reference objects), 7 gates + CI + live Pages,
369 producer tests, 36 pages.** The equilibrium & acid-base tier now has **five lessons across four subtypes** — weak-acid pH,
buffer, weak-base pH, Ksp solubility, and (new) a **common-ion** solubility variant — all on the one `solve_equilibrium`
reversible-extent solver, plus the `weak_acid_ph_v1` gym. Six increments landed this session in the equilibrium tier
(ADR-0048 opener + Ksp + weak base + buffer + gym + this common-ion variant).
**Next up:** more of the equilibrium tier — **titration curves** (the ledger marched as titrant is added — the tier's
remaining marquee topic; the producer computes every point via `solve_equilibrium` and emits the pH curve for a static SVG,
no client chemistry), or **polyprotic** staged equilibria (H₃PO₄, sequential Kₐ₁/Kₐ₂/Kₐ₃ solves — a new subtype), or the
**Q-vs-Ksp precipitation prediction** (a *prediction* face on the now-in-place common-ion Ksp machinery). Then the K/pH/Kw
**formula-sheet** entries (the activity treatment), then **kinetics** and **electrochemistry**.

## Increment 24 — Phase-2 equilibrium: polyprotic staged ionization (ADR-0048 7th increment, Opus 4.8 driving)

Took the flagged **polyprotic** increment — the fifth `equilibrium` subtype, and the first that runs `solve_equilibrium`
**more than once**. A polyprotic acid loses its protons in stages (H₃PO₄ ⇌ H⁺ + H₂PO₄⁻, then H₂PO₄⁻ ⇌ H⁺ + HPO₄²⁻, then
HPO₄²⁻ ⇌ H⁺ + PO₄³⁻), each with its own Kₐ (Kₐ1 ≫ Kₐ2 ≫ Kₐ3). The solver runs once per stage, each seeded with the previous
stage's equilibrium concentrations — the standard successive treatment (a disclosed model assumption, exact in the
well-separated-Kₐ limit). Inside the ADR-0048 contract — **no new ADR**, a 7th-increment note appended. No honesty-model /
house-convention / scope change (additive: a new subtype + regime-3 data curation).

### What shipped

- **Data (regime-3, OpenStax App H):** a `[polyprotic]` table in `data/ionization-constants.toml` (H₃PO₄'s three ordered
  `{acid, ka, anion}` stages) — each stage's composition machine-checked on load (acid = anion + H⁺, charge one more positive;
  Kₐ required strictly decreasing) via a new `data.polyprotic_constant` accessor; two new ion-table anions (H₂PO₄⁻ dihydrogen
  phosphate, HPO₄²⁻ hydrogen phosphate).
- **Engine:** `build_polyprotic_lesson` — solves the stages in sequence on running concentrations; the FIRST (dominant)
  ionization populates the top-level `ice`/`mass_action`/`reaction`/`equilibrium_constant` (so the existing ICE renderer + the
  required schema fields stay meaningful), and stages 2..n go under `result.later_stages` (compact, re-solvable) alongside a
  `result.species_ladder` (every species' equilibrium concentration) + `proton_count`. Dispatched in `build_equilibrium` by
  the acid's proton count (≥ 2 → polyprotic). Refuses a monoprotic acid.
- **Contracts:** `equilibrium-lesson.schema.json` — the `polyprotic` subtype + `result.species_ladder`/`later_stages`/
  `proton_count` (additive; no `required` relaxation — stage 1 fills the existing required fields; the 4th check reuses
  `ph_consistent`).
- **Gate:** `equilibriumcheck.mjs` — a `polyprotic` branch re-solves each later stage on the CHAINED initials (verifying each
  stage's initials = the previous stage's outputs), re-checks Q=Kₐ per stage, the accumulated [H⁺]/pH, and reconstructs the
  species ladder ([acid]=c₀−x₁; [anionₖ]=xₖ−xₖ₊₁; [last]=x_last). `validate-solutions` enforces the subtype fields.
- **Player:** `EquilibriumLesson.astro` — an `isPoly` branch: the top ICE is captioned "first of N ionizations", a pH/[H⁺]/
  stages result trio, a **later-ionizations table** (each stage's reaction/Kₐ/extent/product), the **species ladder**, the
  amphiprotic-[HPO₄²⁻]≈Kₐ2 payoff note, and the staged-ionization misconception. `view.js` preps the ladder + later-stage
  KaTeX/scientific-notation. A fix: the emitted `symbol` uses `K_a1` (the split-on-"_" HTML renderer) while the LaTeX
  expression uses `K_{a1}`.
- **Content:** `problems/equilibrium/phosphoric-acid-ph.equilibrium.toml`; a new **`polyprotic-acid` concept**.
- **Tests:** `test_equilibrium.py` +7 (the data load + decreasing Kₐ; the build + pH/ladder/later-stages; the amphiprotic
  [HPO₄²⁻]≈Kₐ2 + vanishing [PO₄³⁻]; each stage Q=Kₐ; the chain; the monoprotic refusal; the dispatch by proton count).
- **Docs:** ADR-0048 7th-increment note; CHANGELOG; ROADMAP (Now + both equilibrium scope blocks); AGENTS (counters + the
  compressed five-subtype standing fact + where-next); architecture (counters + the `build_polyprotic_lesson` + validate-solutions
  rows); regime-map (acids row → 3 lessons); SOURCES (App H phosphoric Kₐ); authoring-problems (the polyprotic subtype);
  README; this log. **Deep-sweep bonus:** compressed the over-budget `AGENTS.md ## Current state` equilibrium enumeration (it
  duplicated ROADMAP/DECISIONS) and fixed a stale "10 lessons" counter in architecture.md.

### Verification (exact counts)

- **376 producer tests green** (+7).
- **7 Node gates green:** **validate-solutions = 6 + 2 structure + 1 comparison + 6 equilibrium** (15 unique ids),
  **validate-reference = 68** (+1 concept), validate-gyms = 12/120, check-ledger = 6/24 + 1 gas + 1 energy, check-parity =
  320 + 36, **check-katex = 702** (+59), scan clean. **`astro build` = 37 pages** (+1: `/lessons/phosphoric-acid-ph/`).
  2 new derived files (the lesson + the concept). **Expected ion-table ripple:** the two new anions flow through the single
  ion source into the **Valence Table** (its formula-mode crossover grew 156 → **182** machine-verified salts — new real
  compounds like Ca(H₂PO₄)₂) and **deterministically reshuffled** the `ionic_nomenclature_v1` gym (same 10 problems, a
  different but still-valid set of standard salts — the new phosphate anions land in 0 drills; validate-gyms re-derives all
  120). Salt counter updated 156 → 182.
- **Polyprotic gate proven non-vacuous — 5 tamper tests**, each a distinct branch: a later-stage extent; the chain link
  (stage 2's initial reactant); the pH; a species-ladder concentration; the accumulated hydronium. Each flips PASS→FAIL; the
  restored (regenerated) file passes.
- **In-browser (dev preview):** `/lessons/phosphoric-acid-ph/` renders the stage-1 ICE (H₃PO₄ 0.1 → 0.0761, H⁺/H₂PO₄⁻ → 0.0239),
  the mass-action check (Q = Kₐ1 ✓), the pH 1.62 / 23.9% / 3-stages result trio, the **later-ionizations table** (stage 2:
  Kₐ 6.2×10⁻⁸, x 6.2×10⁻⁸, [HPO₄²⁻] = 6.2×10⁻⁸; stage 3: [PO₄³⁻] = 1.09×10⁻¹⁸), the **species ladder** (7.61×10⁻² down to
  1.09×10⁻¹⁸ M), the amphiprotic **[HPO₄²⁻] ≈ Kₐ2** payoff, and the refuted "three protons = triple [H⁺]" misconception.
  Kₐ1 renders correctly (K + subscript). 0 KaTeX errors; no regression on the other five equilibrium lessons.

### State at session end — PHASE 2 OPEN (equilibrium: 5 lesson subtypes + the weak-acid gym)

**15 lessons + 12 gyms (120 problems) + 1 Valence Table (23 elements) + 31 concepts + 7 reaction families (21 examples) + 14
species + 6 molecule structure entries + 9 formula-sheet entries (= 68 Atlas reference objects), 7 gates + CI + live Pages,
376 producer tests, 37 pages.** The equilibrium & acid-base tier now has **six lessons across five subtypes** — weak-acid pH,
buffer, weak-base pH, Ksp solubility (+ a common-ion variant), and polyprotic staged ionization — all on the one
`solve_equilibrium` reversible-extent solver (now called once per stage for polyprotic), with one shared Node re-solver
(`equilibriumcheck.mjs`'s `solveEquilibrium`) used across the lesson + gym gates. Seven increments landed this session in the
equilibrium tier (ADR-0048 opener + Ksp + weak base + buffer + gym + common-ion + this polyprotic).
**Next up:** the equilibrium tier's remaining marquee is **titration curves** — the ledger marched as titrant is added, a
weak acid + strong base sweeping through the buffer region (half-equivalence pH = pKₐ), the equivalence point (basic, the
conjugate base), and past it (excess OH⁻). Each point is a `solve_equilibrium` call (a buffer solve, then the conjugate's
weak-base solve at equivalence, then Kw for excess base); the producer computes ~30 points and emits the (V, pH) curve for a
**static SVG** in the player (no client chemistry — reuse acetic acid's Kₐ + NaOH, no new data). Then **Q-vs-Ksp precipitation
prediction** (a *prediction* face on the common-ion Ksp machinery), the K/pH/Kw **formula-sheet** entries (the activity
treatment), then **kinetics** (dξ/dt, rate laws, half-life) and **electrochemistry** (oxidation numbers → electron ledger,
ΔG = −nFE, Nernst). A weak-base/buffer/polyprotic **gym** extension is a smaller optional follow-on.

## Increment 25 — Phase-2 equilibrium: titration curves — the ledger marched, and the tier's first plot (ADR-0048 8th increment, Opus 4.8 driving)

Took the flagged marquee — a **titration curve**, the payoff of the whole "ICE = ledger" thesis: the pH curve is nothing but
the species ledger solved over and over as titrant is added, the region deciding which equilibrium dominates. A weak acid
titrated by a strong base, the sixth `equilibrium` subtype, and the first lesson with a **chart**. Inside the ADR-0048
contract (titration was the flagged remaining marquee) — **no new ADR**, an 8th-increment note appended. Additive: a new
subtype + a top-level `titration` block + a build-time SVG in the existing player. Static rendering (the established pattern —
the gas slider was deferred), so no escalation. **No new data** (reuses acetic acid's Kₐ + NaOH).

### What shipped

- **Engine:** `build_titration_lesson` (dispatched by a `titrant` key). `_titration_point` computes the pH at one added-base
  volume **by region** — a weak-acid/buffer solve before equivalence (HA ⇌ H⁺ + A⁻ with [A⁻] from the neutralized acid), the
  conjugate base's **weak-base solve** at equivalence (A⁻ + H₂O ⇌ HA + OH⁻, Kᵦ = K_w/Kₐ → the K_w bridge), **excess strong
  base** after — each reusing `solve_equilibrium`. The curve samples the volume at fixed fractions of the equivalence volume
  (denser near equivalence). The top-level ice = the **initial** point (the pure weak acid), so the required fields + the ICE
  renderer stay meaningful. Refuses a strong/polyprotic acid or a non-strong titrant.
- **Contracts:** `equilibrium-lesson.schema.json` — the `titration` subtype + a top-level **`titration` block** (titrant +
  acid inputs, the (volume, pH) `curve`, the three `landmarks`, equivalence/half-equivalence volumes, pKₐ, K_w) + a
  `titrationLandmark` $def + result `pH_half_equivalence_display`/`pH_equivalence_display`. No `required` relaxation.
- **Gate:** `equilibriumcheck.mjs`'s `titrationPh` + a `titration` branch **recompute the entire curve** independently (region
  + pH per point, from the titrant/acid inputs + Kₐ + K_w — a tampered region label is rejected) and re-check the landmarks
  (half-eq pH ≈ pKₐ; equivalence > 7; initial = result.pH) + the equivalence volume. `validate-solutions` enforces the fields.
- **Player:** `EquilibriumLesson.astro` — an `isTitration` branch computes the **SVG geometry at build time** (map each verified
  (volume, pH) to px) and draws a `<polyline>` curve + pH/volume gridlines + a pH-7 reference line + three landmark markers +
  axis titles, then landmark readout cards + a "read the curve" note + the basic-equivalence misconception. `view.js` preps
  the neutralization equation + titrant pretty. No client JS — a static chart of verified points.
- **Content:** `problems/equilibrium/acetic-acid-titration.equilibrium.toml` (25.0 mL 0.100 M acetic acid vs 0.100 M NaOH); a
  new **`titration` concept**.
- **Tests:** `test_equilibrium.py` +7 (the build + V_eq/pKₐ/landmarks; half-eq pH = pKₐ; equivalence basic; the curve
  monotonic + regioned; the strong-acid refusal; the non-base-titrant refusal; the dispatch + round-trip).
- **Docs:** ADR-0048 8th-increment note; CHANGELOG; ROADMAP (Now + both equilibrium scope blocks); AGENTS (counters + the
  six-subtype standing fact + where-next); architecture (counters + the `build_titration_lesson` + validate-solutions rows +
  the roadmap-table row); regime-map (buffers & titrations row → 2 lessons); authoring-problems (the titration subtype);
  README; this log.

### Verification (exact counts)

- **383 producer tests green** (+7).
- **7 Node gates green:** **validate-solutions = 6 + 2 structure + 1 comparison + 7 equilibrium** (16 unique ids),
  **validate-reference = 69** (+1 concept), validate-gyms = 12/120, check-ledger = 6/24 + 1 gas + 1 energy, check-parity =
  320 + 36, **check-katex = 716** (+14), scan clean. **`astro build` = 38 pages** (+1: `/lessons/acetic-acid-titration/`).
  `derived/` **byte-stable** — 2 new files (the lesson + the concept); no new data, so nothing else moved.
- **Titration gate proven non-vacuous — 5 tamper tests**, each a distinct branch: a mid-curve pH; a curve region label; the
  equivalence pH forced acidic (<7); the equivalence volume; the half-equivalence pH. Each flips PASS→FAIL; restored passes.
- **In-browser (dev preview):** `/lessons/acetic-acid-titration/` renders the initial ICE (acetic acid 0.1 → 0.0987), the
  mass-action check (Q = Kₐ ✓), and the **SVG titration curve** in the textbook shape — a buffer plateau, the steep jump at
  the equivalence point (25 mL), and the excess-base tail — with the three landmark markers (start pH 2.88, ½-eq pH 4.75 = pKₐ,
  equivalence pH 8.72), a pH-7 reference line, the landmark cards, and the refuted "pH 7 at equivalence" misconception. 0 KaTeX
  errors; no regression on the other six equilibrium lessons. Screenshot confirmed the curve shape.

### State at session end — PHASE 2 OPEN (equilibrium: 6 lesson subtypes + the weak-acid gym; titration = the tier's first chart)

**16 lessons + 12 gyms (120 problems) + 1 Valence Table (23 elements) + 32 concepts + 7 reaction families (21 examples) + 14
species + 6 molecule structure entries + 9 formula-sheet entries (= 69 Atlas reference objects), 7 gates + CI + live Pages,
383 producer tests, 38 pages.** The equilibrium & acid-base tier now has **seven lessons across six subtypes** — weak-acid pH,
buffer, weak-base pH, Ksp solubility (+ a common-ion variant), polyprotic staged ionization, and a titration curve — all on
the one `solve_equilibrium` reversible-extent solver, with one shared Node re-solver (`equilibriumcheck.mjs`) across the
lesson + gym gates. **Eight increments landed this session** in the equilibrium tier (ADR-0048 opener + Ksp + weak base +
buffer + gym + common-ion + polyprotic + titration). The three increments this session (common-ion Ksp, polyprotic,
titration) each shipped: engine + schema + gate + player + concept + tests + docs, tamper-tested + browser-verified + the
first two pushed & deployed & live-verified.
**Next up:** the equilibrium tier's remaining smaller items — **Q-vs-Ksp precipitation prediction** (a *prediction* face on the
now-in-place common-ion Ksp machinery: given two ion concentrations from mixing, compute Q = [cat]ᵃ[an]ᵇ and compare to Kₛₚ →
does a precipitate form? — a snapshot comparison, not a solve, so it may want its own compact representation) and the
**K/pH/Kw formula-sheet** entries (they need the *activity* — dimensionless — treatment so the dimension engine stays
homogeneous). Optional: a titration **slider interactive** (drag the volume, watch the point move on the now-verified curve),
a weak-base/buffer/polyprotic **gym** extension. Then the next Phase-2 tiers open with their stress scenarios: **kinetics**
(dξ/dt, rate laws, integrated rate laws, half-life) and **electrochemistry** (oxidation numbers → completing the free-element
redox flag → the electron ledger, E°, ΔG = −nFE, Nernst).

## Increment 26 — Phase-2 equilibrium: Q-vs-Kₛₚ precipitation prediction — the `prediction` lesson kind (ADR-0048 9th increment, Opus 4.8 driving)

Took the flagged remaining equilibrium item, and the first face that is **not** a solve: *"will a precipitate form when two
solutions are mixed?"* is decided by the **reaction quotient**, not the reversible-extent solver. Mix, dilute each ion into the
combined volume, evaluate Q = [cation]ᵃ[anion]ᵇ at that instant, and compare to Kₛₚ — Q > Kₛₚ ⇒ supersaturated ⇒ a precipitate
forms. A **snapshot**, not an equilibrium solve. Because there is no extent to solve (no ICE table, Q ≠ K by design), it does not
fit the equilibrium-lesson schema — so, following the house pattern (structure/comparison/equilibrium each got their own tight
schema, not a bent union), it is a **new `prediction` lesson kind**, the fifth lesson shape. Inside the ADR-0048 contract — **no
new ADR**, a 9th-increment note appended. Additive; no honesty-model / house-convention / scope change; static rendering; **no new
data** (reuses CaF₂'s curated Kₛₚ).

### What shipped

- **Engine:** `build_prediction_lesson` (in `equilibrium.py`, reusing `_quotient` + `_regimes` + the sourced Ksp data). Author a
  target `salt` + a `[cation_source]` and `[anion_source]` (each a soluble salt: formula, molarity, volume, `per_formula`); the
  producer computes each ion's source concentration, the mixing dilution, Q, and the verdict. The source→ion multiplicity is
  **machine-checked for a monatomic ion** (its element appears `per_formula` times in the neutral source formula — Ca(NO₃)₂ has
  one Ca), full dissociation the disclosed model assumption. Refuses an unknown salt, a non-neutral source, a source missing its
  ion, or a non-positive input.
- **A new kind, not a subtype.** Own tight `schemas/prediction-lesson.schema.json` (a `mixing` block + a `quotient` + a verdict
  `result`, no ICE table); dispatched by the `*.prediction.toml` → `*.prediction.json` extension in `build_problems_main` (a fifth
  builder). Machine-checked facts: `mixing_dilution` / `quotient_computed` / `verdict_consistent` (forms_precipitate ⇔ Q > Kₛₚ,
  an **exact Decimal comparison**).
- **Gate:** `equilibriumcheck.mjs` gains `verifyPrediction` — re-derives the mixing dilution + Q + verdict + the source
  composition in pure Node (reusing `parseFormula` + `reactionQuotient`). `validate-solutions.mjs` compiles the prediction schema,
  walks `*.prediction.json`, enforces the three-regime honesty + a disclosed model assumption + the Kₛₚ source, and calls
  `verifyPrediction`. The `check-katex` / `validate-reference` lesson-slug / lesson-route walks gained the `.prediction.json`
  suffix; a `predictionLatex` collector renders the dissolution reaction + Kₛₚ/Q expressions + source/ion symbols.
- **Player:** a static `PredictionLesson.astro` — badges, the dissolution reaction + Kₛₚ, a **mixing table** (each source → its
  ion → after-mixing concentration), the **Q substitution**, a **verdict banner** (Q vs Kₛₚ → "✓ A precipitate forms", the
  many-fold margin), the three machine-checked facts, the misconception, and the assumptions. `view.js` `renderPredictionLesson`
  preps the tokens/LaTeX/sci; `lessons/[slug].astro` + `lessons/index.astro` dispatch the new kind.
- **Content:** `problems/equilibrium/calcium-fluoride-precipitation.prediction.toml` — 40.0 mL 0.010 M Ca(NO₃)₂ + 60.0 mL 0.010 M
  NaF → [Ca²⁺] = 0.0040 M, [F⁻] = 0.0060 M, **Q = 1.44×10⁻⁷ ≫ Kₛₚ = 3.45×10⁻¹¹ (≈ 4170×)** → **precipitates** (the **third view of
  CaF₂**: dissolve / suppress / **predict**). A new **`reaction-quotient` concept** (Q has K's form, evaluated anywhere; Q vs K
  predicts direction; vs Kₛₚ, precipitation); the `solubility-product` concept cross-links it + the new lesson.
- **Tests:** `test_equilibrium.py` +9 (build + verdict/Q/margin; the mixing dilution exact; the dilute-mix "no precipitate" flip;
  the unknown-salt / source-without-its-ion / non-neutral-source / non-positive-volume refusals; the round-trip).
- **Docs:** ADR-0048 9th-increment note; CHANGELOG; ROADMAP (Now + both equilibrium scope blocks); AGENTS (counters + the
  five-shapes standing fact + where-next, and **fixed a stale "four subtypes" line** the earlier increments left); architecture
  (counters + `build_prediction_lesson` + `build_prediction` dispatch + the validate-solutions / check-katex / solution-object /
  equilibrium-row entries); regime-map (solubility row → 3, and fixed the stale "four subtypes" ICE row); authoring-problems (the
  prediction kind); README; this log.

### Verification (exact counts)

- **392 producer tests green** (+9). (Note: the reaction-families gym tests resolve `data/` relative to CWD, so run pytest from
  the **repo root** — `uv --project producer run pytest producer` — not from inside `producer/`.)
- **7 Node gates green:** **validate-solutions = 6 + 2 structure + 1 comparison + 7 equilibrium + 1 prediction** (17 unique ids),
  **validate-reference = 70** (+1 concept), validate-gyms = 12/120, check-ledger = 6/24 + 1 gas + 1 energy, check-parity =
  320 + 36, **check-katex = 752** (+36), scan clean. **`astro build` = 39 pages** (+1: `/lessons/calcium-fluoride-precipitation/`).
  `derived/` **byte-stable** — 2 new files (the lesson + the concept); no new data, so nothing else moved.
- **Prediction gate proven non-vacuous — 5 tamper tests**, each a distinct branch: the verdict flipped against Q (forms_precipitate
  false while Q > Kₛₚ); a mixed concentration; the Q value; the margin; the source `per_formula`. Each flips PASS→FAIL; the restored
  (regenerated) file passes.
- **In-browser (dev preview):** `/lessons/calcium-fluoride-precipitation/` renders the mixing table (Ca(NO₃)₂ 40.0 mL·0.010 M →
  [Ca²⁺] 0.004; NaF 60.0 mL·0.010 M → [F⁻] 0.006; combined 100 mL), the Q substitution Q = (0.004)(0.006)² = 1.44×10⁻⁷, and the
  **verdict banner** "Q = 1.44×10⁻⁷ > Kₛₚ = 3.45×10⁻¹¹ · ✓ A precipitate forms" (≈4170×), the three machine-checked facts, and the
  refuted "both clear + dilute → nothing happens" misconception. 0 KaTeX errors; all 5 concept chips resolve (incl. the new
  `reaction-quotient`); the lessons index lists it; no regression on the titration lesson.

### State at session end — PHASE 2 OPEN (equilibrium: 6 subtypes + a gym + the `prediction` kind; five lesson shapes)

**17 lessons + 12 gyms (120 problems) + 1 Valence Table (23 elements) + 33 concepts + 7 reaction families (21 examples) + 14
species + 6 molecule structure entries + 9 formula-sheet entries (= 70 Atlas reference objects), 7 gates + CI + live Pages, 392
producer tests, 39 pages.** Lessons now come in **five shapes** — reaction · structure · comparison · equilibrium · **prediction**.
The equilibrium & acid-base tier spans **six `equilibrium` subtypes + the `weak_acid_ph_v1` gym + the new `prediction` kind**
(Q vs Kₛₚ — a snapshot, not a solve). Nine increments landed this session in the equilibrium tier (ADR-0048 opener + Ksp + weak
base + buffer + gym + common-ion + polyprotic + titration + this prediction).
**Next up:** the equilibrium tier's last item — the **K/pH/Kw formula-sheet** entries (they need the *activity* — dimensionless —
treatment so the `chemkernel.dimension` engine stays homogeneous; add them as `formula` Atlas entries à la ADR-0039). Optional
smaller follow-ons: a titration **slider interactive** (drag the volume along the now-verified curve); a weak-base/buffer/polyprotic
**gym** extension; a "no-precipitate" prediction variant (a dilute mix where Q < Kₛₚ — the engine already handles it, the test
`test_prediction_dilute_mix_stays_clear` proves it). Then the next Phase-2 tiers open with their stress scenarios: **kinetics**
(dξ/dt, rate laws, integrated rate laws, half-life) and **electrochemistry** (oxidation numbers → completing the free-element
redox flag → the electron ledger, E°, ΔG = −nFE, Nernst).

## Increment 27 — Phase-2 equilibrium: the other verdict — a "stays clear" prediction lesson (ADR-0048 9th-increment follow-on, Opus 4.8 driving)

Took the flagged "no-precipitate" prediction variant right after building the kind — a content-plus-robustness increment that
**completes the Q-vs-Kₛₚ story**: increment 26 showed Q > Kₛₚ (a precipitate forms); this shows Q < Kₛₚ (the solution stays
clear). No new engine/schema/gate — the `prediction` kind (ADR-0048 9th increment) already handles it; the value is a second
lesson exercising the **second** curated Kₛₚ salt + the "no" verdict, plus a player fix it surfaced. Inside the ADR-0048
contract — a **9th-increment follow-on** note, no new ADR. Additive, content-only + a small player branch; no honesty /
convention / scope change; static rendering; **no new data**.

### What shipped

- **Content:** `problems/equilibrium/magnesium-hydroxide-no-precipitate.prediction.toml` — 50.0 mL 0.00010 M Mg(NO₃)₂ + 50.0 mL
  0.00010 M NaOH → [Mg²⁺] = [OH⁻] = 5.0×10⁻⁵ M, **Q = 1.25×10⁻¹³ < Kₛₚ = 5.61×10⁻¹² (≈45× below)** → **stays clear**. The
  misconception is the *reverse* of the CaF₂ lesson's: "an insoluble compound must precipitate" — refuted because a precipitate
  forms only when Q *exceeds* Kₛₚ, so "insoluble" is quantitative, not a label. Exercises the **second** curated Kₛₚ salt
  (Mg(OH)₂ — never used in a lesson before) and a **polyatomic-ion** (OH⁻) source (whose multiplicity rides the disclosed
  full-dissociation model — the monatomic composition check applies only to a monatomic ion like Mg²⁺, which is machine-checked).
- **Verdict-general player.** Authoring the "no" lesson surfaced a latent gap: `PredictionLesson.astro`'s misconception
  refutation was hardcoded for the "precipitate forms / Q above Kₛₚ" case (the verdict banner's margin line was already
  conditional). Now branched on the verdict, so the kind renders both outcomes correctly (the "stays clear" branch teaches the
  quantitative reading of "insoluble").
- **Test:** `test_equilibrium.py` +1 (`test_prediction_no_precipitate_round_trip` — the Mg(OH)₂ spec builds, verdict
  `no-precipitate`, comparison `<`).
- **Docs:** DECISIONS (a 9th-increment follow-on note); CHANGELOG; regime-map (solubility row → 4); AGENTS + architecture +
  README counters; this log.

### Verification (exact counts)

- **393 producer tests green** (+1). (Run pytest from the **repo root** — the reaction-families gym tests resolve `data/`
  relative to CWD.)
- **7 Node gates green:** **validate-solutions = 6 + 2 structure + 1 comparison + 7 equilibrium + 2 prediction** (18 unique ids),
  validate-reference = 70 (unchanged — no new concept), validate-gyms = 12/120, check-ledger 6/24 + gas + energy, check-parity
  320 + 36, **check-katex = 773** (+21), scan clean. **`astro build` = 40 pages** (+1). `derived/` byte-stable — 1 new file.
- **"No" verdict tamper-tested:** flipping `forms_precipitate`/verdict/comparison to "precipitate" on the Q<Kₛₚ file is caught
  (PASS→FAIL); the restored file passes. (Note: on Windows, tamper via Python must pass `encoding='utf-8'` — the derived JSON
  carries em-dashes + curly quotes; cp1252 chokes on the U+201D byte and silently no-ops the edit.)
- **In-browser (dev preview):** `/lessons/magnesium-hydroxide-no-precipitate/` renders the mixing table, Q = [Mg²⁺][OH⁻]² =
  1.25×10⁻¹³, the **"No precipitate — the solution stays clear"** banner (the `clear` style, not the accent `forms` style), and
  the verdict-general misconception ("'insoluble' is not a yes/no label … quantitative … reputation"). 0 KaTeX errors; the
  hardcoded forms-only text does **not** leak into the "no" lesson.

### State at session end — PHASE 2 OPEN (equilibrium: 6 subtypes + a gym + 2 `prediction` lessons; five lesson shapes)

**18 lessons + 12 gyms (120 problems) + 1 Valence Table (23 elements) + 33 concepts + 7 reaction families (21 examples) + 14
species + 6 molecule structure entries + 9 formula-sheet entries (= 70 Atlas reference objects), 7 gates + CI + live Pages, 393
producer tests, 40 pages.** The `prediction` kind now has **both verdicts** — CaF₂ forms (Q ≫ Kₛₚ), dilute Mg(OH)₂ stays clear
(Q < Kₛₚ) — and the player renders each cleanly. Two increments landed this session's chapter (the `prediction` kind + this "no"
follow-on); **increment 26 is committed + pushed + CI-green + live-verified**, this follow-on is committed with it or next.
**Next up:** the equilibrium tier's last reference item — the **K/pH/Kw formula-sheet** entries. These need the *activity*
(dimensionless) treatment so the `chemkernel.dimension` engine stays homogeneous; the open design question is how a monomial
dimension checker handles a **log-based relation** (pH = −log₁₀(a_H⁺) with a_H⁺ = [H⁺]/c°) — likely declaring the quantities
dimensionless and treating the identity as dimensionless-consistent, but it may warrant a short design note (possibly ADR-level)
before implementing. Then **kinetics** (dξ/dt, rate laws, half-life) and **electrochemistry** (oxidation numbers → the electron
ledger, ΔG = −nFE, Nernst), each opening with its stress scenario.

## Increment 28 — Phase-2 equilibrium: the K/K_w/K_sp formula-sheet entries — dimensionless activity relations (ADR-0048 10th increment, Opus 4.8 driving)

Took the flagged design fork **to the owner** first (via a question): how should the K/pH/K_w formula-sheet entries handle the
*activity* treatment, given the dimension engine is monomial and pH is a log? The owner chose **"K-relations only"** — register
the constants (Kₐ/K_b/K_w/K_sp) as dimensionless activity relations, keep pH/pOH in the `ph` concept. The insight that makes it
**pure content, no engine change**: an activity $a_X = [X]/c^\circ$ (concentration against the 1 M standard state) is
dimensionless, so a K is a **monomial of dimensionless quantities**, and the existing ADR-0039 engine (which already registers
the dimensionless unit `"1"`) checks "both sides → the zero SI vector" unchanged. No new ADR — a 10th-increment note on ADR-0048.

### What shipped

- **5 formula entries** (`reference/formulas/`, sheet 9 → **14**): `acid-ionization-constant` (Kₐ = [H⁺][A⁻]/[HA]),
  `base-ionization-constant` (K_b), `water-ion-product` (K_w = [H⁺][OH⁻]), `solubility-product` (K_sp = [Mⁿ⁺]ᵃ[Xᵐ⁻]ᵇ), and
  `conjugate-ka-kb` (Kₐ·K_b = K_w). Each variable is an **activity** with `unit = "1"` (dimensionless); every term reduces to the
  zero vector → dimensionally homogeneous, machine-checked + re-derived in pure Node (`validate-reference` / `dimension.mjs`),
  **no engine change**. All model-exact, disclosing the activity/standard-state idealization. Cross-linked to the tier's
  concepts + lessons.
- **Two display fixes.** The formulas page groups entries by a hardcoded id list, so a **"Equilibrium & acid–base"** section was
  added — which revealed that `formula-enthalpy-of-reaction` (Hess's law, ADR-0043) had **never been in any group** and so had
  silently not rendered on the page since it was authored; now added to "Gases & energy". And because a variable's `meaning`
  renders as **plain text** (not KaTeX — the page uses `{v.meaning}`), the activity notation was moved out of `$…$` math into
  prose (the summary/domain/assumptions still carry rendered inline math).
- **Test:** `test_reference.py` — bumped the formula-count assertion (9 → 14) + a new `test_equilibrium_constant_formulas_are_dimensionless`
  (each of the 5 is model-exact, both terms the zero dimension, ≥1 disclosed model assumption).
- **Docs:** DECISIONS (a 10th-increment note recording the owner-decided activity convention + the enthalpy display-gap fix);
  CHANGELOG; ROADMAP (Now + both equilibrium scope blocks → the tier is now **feature-complete**); AGENTS (counters + where-next);
  architecture (counters + the formula-entries module row); regime-map (the equilibrium row); README; this log.

### Verification (exact counts)

- **394 producer tests green** (+1). (Run pytest from the **repo root**.)
- **7 Node gates green:** validate-solutions = 6 + 2 structure + 1 comparison + 7 equilibrium + 2 prediction (18 ids),
  **validate-reference = 75** (+5 formula entries — the dimension re-derivation passes with all-dimensionless terms),
  validate-gyms 12/120, check-ledger 6/24 + gas + energy, check-parity 320 + 36, **check-katex = 830** (+57), scan clean.
  **`astro build` = 40 pages** (the formula entries live on `/reference/formulas/`, no new route). `derived/` byte-stable —
  5 new files. **No engine change** — the dimensionless treatment is pure content on the existing ADR-0039 machinery.
- **In-browser (dev preview):** `/reference/formulas/` renders **14 entries across 3 sections** (Amount/mass/solutions · Gases &
  energy · **Equilibrium & acid–base**); the 5 new K entries each show the statement (KaTeX), the "both reduce to
  **dimensionless**, machine-verified" dimcheck, the plain-text activity meanings, and the disclosed activity assumption; the
  previously-missing Hess's-law entry now appears too. 0 KaTeX errors; meaning cells carry no raw `$…$`. (A caching gotcha
  recorded: `npm run build` does **not** run `produce` — after editing a spec you must `npm run produce` or the dev server + the
  gates read stale `derived/`.)

### State at session end — PHASE 2 OPEN (equilibrium & acid-base tier FEATURE-COMPLETE)

**18 lessons + 12 gyms (120 problems) + 1 Valence Table (23 elements) + 33 concepts + 7 reaction families (21 examples) + 14
species + 6 molecule structure entries + 14 formula-sheet entries (= 75 Atlas reference objects), 7 gates + CI + live Pages, 394
producer tests, 40 pages.** The **equilibrium & acid-base tier is now feature-complete**: the reversible-extent solver with
**six `equilibrium` subtypes** (weak-acid · buffer · weak-base · Ksp solubility + common-ion · polyprotic · titration) + the
**`weak_acid_ph_v1` gym** + the **`prediction` lesson kind** with **both verdicts** (CaF₂ forms, dilute Mg(OH)₂ stays clear) +
the **K/K_w/K_sp formula-sheet entries** (dimensionless activity relations). Three increments landed this session's later chapters
(the prediction kind + the "no" verdict follow-on + the K formula sheet); all committed, and the first two pushed + CI-green +
live-verified.
**Next up:** the model-bearing tiers that remain in Phase 2 — **kinetics** (the ledger in time: dξ/dt, rate laws, integrated rate
laws, half-life) and **electrochemistry** (oxidation numbers — completing the free-element redox flag from ADR-0035 — the
electron ledger, E°, ΔG = −nFE, Nernst). Each opens with its stress scenario, mirroring how equilibrium opened on acetic-acid pH.
Optional equilibrium leftovers: a titration **slider interactive**; a weak-base/buffer/polyprotic **gym** extension. Before opening
kinetics, a session might confirm with the owner whether to proceed to it or to firm up the **Phase-2 definition of done** (the
ROADMAP leaves it to firm up as the later tiers scope).

## Increment 29 — Phase-2: OPEN THE KINETICS TIER — the ledger in time, first-order decay (ADR-0049, Opus 4.8 driving)

Asked the owner which tier to open next (equilibrium being feature-complete); they chose **kinetics** (with the recommended
first-order stress scenario). Kinetics is the thesis's most literal time extension: the reaction extent ξ — static in a completed
ledger, solved-from-mass-action in equilibrium — now **evolves in time**, dξ/dt = the rate. A new tier → a **new ADR-0049**, a
new engine module, a new sourced dataset, a new lesson kind (the **6th lesson shape**), a gate, and a player with a decay curve.
Opened on the iconic first-order case. No honesty-model change (the three badges apply unchanged); no house-convention/stack/scope
change beyond opening the planned tier. The rate constant was **sourced via the web** (OpenStax Chemistry 2e §12.4, Example 12.11)
so the regime-3 datum is honest, not invented.

### What shipped

- **Data (regime-3):** `data/rate-constants.toml` — the first-order k for H₂O₂ decomposition (3.21×10⁻⁵ s⁻¹, OpenStax §12.4),
  machine-checked on load (`data.rate_constant`: the reactant parses, k > 0, order a nonnegative int, k_unit known). Registered in
  SOURCES (§12.4).
- **Engine:** `chemkernel.kinetics` — `concentration_first_order` ([A]=[A]₀·e^(−kt), Decimal `.exp()`), `half_life_first_order`
  (t½=ln2/k, Decimal `.ln()`) — **model-exact-then-rounded** (ADR-0040 pattern); `build_kinetics_lesson` requires first order,
  samples the decay curve at multiples of t½, emits the halving landmarks, and machine-checks the reaction balance (a small
  element-conservation parser, `_balance_ok`). Dispatched by the `*.kinetics.toml` extension (`build_kinetics` in build.py).
- **Contracts:** `schemas/kinetics-lesson.schema.json` (tight; `subtype: first-order`, a `rate_law` + `integrated` + `half_life`
  + `curve` + `result.landmarks`).
- **Gate:** `scripts/validate/kineticscheck.mjs` (`verifyKinetics`) — re-derives the whole spine in pure Node: the balance
  re-parsed, every curve point c(t)=c₀·e^(−kt), k·t½=ln2, and the halving landmarks c(n·t½)=c₀/2ⁿ. Wired into validate-solutions
  (schema + honesty + branch), check-katex (`kineticsLatex`), validate-reference (lesson-slug walk), and the lesson routes.
- **Player:** `src/components/KineticsLesson.astro` — static, with a **build-time decay-curve SVG** (reusing the titration-plot
  pattern): the exponential decay + the three halving landmarks at evenly-spaced 6-h intervals (the constant half-life made
  visible via dashed drop-lines). `view.js` `renderKineticsLesson`; `lessons/[slug].astro` + `lessons/index.astro` dispatch the
  new kind.
- **Content:** `problems/kinetics/hydrogen-peroxide-decomposition.kinetics.toml` (2 H₂O₂ → 2 H₂O + O₂, first order, 1.000 M,
  t½ 6.00 h; the shrinking-half-life misconception). Three concepts: `reaction-rate` (dξ/dt, the tier gateway), `rate-law`
  (rate=k[A]ⁿ, the order measured not the coefficient — a `contrast` edge to `balancing-equations`), `half-life`.
- **Tests:** `test_kinetics.py` +10 (the exp/ln math; c halves each t½; the balance accept/refuse; the build + landmarks + curve
  monotonic; refusals — unknown reactant, nonpositive concentration; the data load + source; the round-trip).
- **Docs:** ADR-0049; CHANGELOG; ROADMAP (Now + a new kinetics scope block + the Phase-2 sketch); AGENTS (counters + the six-shapes
  standing fact + top paragraph + where-next); architecture (counters + as-built header + the `kinetics.py`/data.py/build.py rows
  + validate-solutions/check-katex/solution-object/equilibrium-row entries); regime-map (the kinetics row, topic slug `kinetics`);
  SOURCES (§12.4 k); authoring-problems (the kinetics kind); README; this log.

### Verification (exact counts)

- **404 producer tests green** (+10). (Run pytest from the **repo root**.)
- **7 Node gates green:** **validate-solutions = 6 + 2 structure + 1 comparison + 7 equilibrium + 2 prediction + 1 kinetics**
  (19 unique ids), **validate-reference = 78** (+3 concepts), validate-gyms 12/120, check-ledger 6/24 + gas + energy, check-parity
  320 + 36, **check-katex = 857** (+27), scan clean. **`astro build` = 41 pages** (+1: `/lessons/hydrogen-peroxide-decomposition/`).
  `derived/` byte-stable — 4 new files (the lesson + 3 concepts).
- **Kinetics gate proven non-vacuous — 5 tamper tests**, each a distinct branch: a mid-curve concentration (integrated law); the
  half-life (t½=ln2/k); a halving landmark (c=c₀/2ⁿ); the reaction balance; a curve percent-remaining. Each flips PASS→FAIL; the
  restored (regenerated) file passes.
- **In-browser (dev preview):** `/lessons/hydrogen-peroxide-decomposition/` renders the reaction + rate law (k = 3.21×10⁻⁵ s⁻¹),
  the integrated rate law, and the **decay-curve SVG** in the textbook shape (exponential decay 1.00 → 0 with the three halving
  landmarks at 6/12/18 h, dashed drop-lines showing 0.500 → 0.250 → 0.125), the half-life 6.00 h, and the refuted shrinking-half-life
  misconception. 0 KaTeX errors; all 4 concept chips resolve (the 3 new + extent-of-reaction).

### State at session end — PHASE 2 OPEN (kinetics tier OPEN — first-order decay; equilibrium feature-complete)

**19 lessons + 12 gyms (120 problems) + 1 Valence Table (23 elements) + 36 concepts + 7 reaction families (21 examples) + 14
species + 6 molecule structure entries + 14 formula-sheet entries (= 78 Atlas reference objects), 7 gates + CI + live Pages, 404
producer tests, 41 pages.** Lessons now come in **six shapes** — reaction · structure · comparison · equilibrium · prediction ·
**kinetics**. Phase 2 has: gases + thermochemistry (complete), bonding & structure (complete), **equilibrium & acid-base
(feature-complete** — 6 subtypes + gym + prediction kind + K formula sheet), and now the **kinetics tier OPEN** (first-order decay,
ADR-0049). Four increments landed this session's later chapters (prediction kind + the "no" verdict + the K formula sheet +
the kinetics opener); the first three are committed + pushed + CI-green + live-verified; the kinetics opener is committed with them
or next.
**Next up (in kinetics, all reusing `chemkernel.kinetics` + the `kinetics` schema/gate/player):** **second-order** and **zero-order**
decay — the contrast that makes "constant t½" meaningful (2nd-order t½ = 1/(k[A]₀) grows as [A] falls; 0th-order t½ = [A]₀/(2k)
shrinks). Each needs a curated k of the right unit (`data/rate-constants.toml` already discriminates `1/(M*s)` / `M/s`), the
integrated law for that order ([A]=[A]₀/(1+k[A]₀t) for 2nd; [A]=[A]₀−kt for 0th), and a `subtype` on the kinetics schema. Then a
**kinetics gym** (compute [A](t) / t½ / determine the order from concentration-time data), and the **Arrhenius** temperature
dependence of k (k = A·e^(−Eₐ/RT) — a formula-sheet entry + a lesson). After kinetics, the last Phase-2 tier is **electrochemistry**
(oxidation numbers — completing the free-element redox flag from ADR-0035 — the electron ledger, E°, ΔG = −nFE, the Nernst equation).
A **Phase-2 definition of done** could also be firmed up now that most tiers have landed (the ROADMAP left it to firm up as the
later tiers scoped).
