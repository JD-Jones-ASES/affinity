# Session 2026-07-10 — docs-only: record the public flip (ADR-0054)

First session from the Windows machine since incorporation into the hub. Scope: pay the one debt the
incorporation logged — the repo went public at the v1.0.0 release with no ADR and stale deploy docs.

- **Resumed from:** v1.0.0 RELEASED (2026-07-09); v1.1 backlog untouched.
- **Decisions recorded:** ADR-0054 — the repo is public (verified `gh repo view` today); supersedes
  ADR-0010's visibility clause only; licensing (MIT code + CC BY-SA 4.0 content) and `package.json`
  `private: true` (= never npm-published, ADR-0053) unchanged.
- **Shipped:** `AGENTS.md §Deploy` corrected from "repo stays private" (+ Educator-plan Pages caveat, now
  moot) to the public state citing ADR-0054. Historical texts (ADR-0010, the v1.0.0 CHANGELOG entry) left
  as written per ADR hygiene.
- **Not touched:** code, content, gates, counters, ROADMAP `## Status` (no phase movement); CHANGELOG (no
  product change shipped).

**Verification:** docs-only diff (`DECISIONS.md`, `AGENTS.md`, this log — `git diff --stat` 2 files + 1 new);
CI re-runs the 7 gates + astro build on push and is the check that matters for a docs commit (scan gate
covers session logs; this one names no board/exam terms).

### State at session end — v1.0.0 public, docs consistent

**Shipped:** ADR-0054 (public flip recorded), AGENTS §Deploy corrected, this log.
**Deferred:** the whole v1.1 backlog, unchanged (`ROADMAP.md ## v1.1 backlog`).
**Next up:** unchanged from 2026-07-09 — v1.1 opens from the backlog when usage resets; Arrhenius or B7
build-time KaTeX are the natural first picks.
