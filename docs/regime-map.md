# Regime map — the breadth scope tracker

Every v1 topic (brief §15), classified by the regimes it spans (ADR-0003): **1** ledger-exact ·
**2** model-exact · **3** empirical/rule-sourced · **4** mechanistic/interpretive. A topic listing
regime 3 or 4 can never ship its rule/interpretive claims without badges. Status is hand-updated as
content lands (`lesson`, `atlas`, `practice`, combinations, or `—`). This table doubles as the coverage
dashboard; phases are defined in [`ROADMAP.md`](../ROADMAP.md).

| Topic | Regimes | Phase | Status |
|---|---|---|---|
| measurement, units, dimensional analysis | 1 | **1** | **gym** (solution conversions) + atlas (dimensional-analysis concept) |
| atoms, isotopes, average atomic mass | 1, 3 | **1** | **atlas** (average-atomic-mass concept — the abundance-weighted mean of isotope masses, exact arithmetic over sourced CIAAW data; the species atlas shows those atomic weights summed into every molar mass — ADR-0038) |
| mole concept & molar mass | 1, 3 | 1 | **atlas** (molar-mass concept + the **species atlas** — every entry's molar mass derived by re-summing the sourced atomic weights, ADR-0038; **formula sheet** — mole–mass $n=m/M$ and Avogadro $N=n\,N_A$, dimensionally verified, ADR-0039) |
| ions & formula writing | 1, 3 | 1 | **gym** (nomenclature, both directions) + **atlas** (Valence Table; polyatomic-ion concept; species entries for the carbonate/hydroxide ions with derived composition + molar mass) |
| nomenclature (ionic, covalent, acids) | 1, 3 | **1** | **gym** (ionic, both directions — Stock system) + atlas (nomenclature concept) |
| periodic table & periodic trends | 3, 4 | **1** | **gym + atlas** (Valence Table flagship: five lenses with interpretive pattern panels, trend graphs, the 156-salt formula builder, ΔEN bonding mode — ADR-0031/0033; `periodic-trends` gym drilled from the same data, exceptions named — ADR-0034; periodic-trends/electronegativity/ionization-energy concepts) |
| balancing equations | 1 | **1** | **gym** (balancing, conservation-matrix + charge tally) + atlas (balancing-equations concept) |
| reaction classes | 3, 4 | **1** | **lesson + gym + atlas** (acid-base neutralization lesson — ADR-0037; `reaction_families_v1` gym: classify-the-family + name-the-spectators — ADR-0036; **7 reaction-family Atlas entries** / 21 engine-classified example reactions with net-ionic views + free-element redox — ADR-0035; precipitation/net-ionic/solubility-rules/dissociation/spectator-ion concepts) |
| stoichiometry (mass/volume/solution/particle) | 1 | **1** | **2 gyms** (mass → mass; limiting reagent from masses) + atlas (stoichiometry, conservation-of-mass concepts) — particle stoich (Avogadro) → Phase 2 |
| limiting reagents | 1, 2 | **0** | **2 lessons, practice, gym, atlas** (Phase 0 slice + non-1:1 phosphate; limiting-from-mass gym) |
| percent yield | 1, 2 | **1** | **lesson + gym + atlas** (zinc-carbonate gravimetric lesson; percent-yield gym; percent-yield concept + formula-sheet entry, ADR-0039) |
| solutions & molarity | 1, 2 | **0** | **2 lessons** + atlas (molarity concept + formula sheet: molarity $c=n/V$, dilution $M_1V_1=M_2V_2$ — ADR-0039) |
| precipitation & net ionic equations | 1, 2, 3 | **0** | **2 lessons + atlas** (carbonate + calcium phosphate) |
| gases (ideal-gas model) | 1, 2 | **2** | **lesson + gym + atlas** (`gas-stoichiometry/zinc-hydrochloric-hydrogen` lesson: the extent ledger drives a gas volume via PV=nRT — a weighed-mass given + a `result.gas` block, moles ledger-exact / volume model-exact-then-rounded under the model-assumed badge, check-ledger re-derives V=nRT/P — ADR-0041; `gas_laws_v1` gym: PV=nRT + combined gas law, solve for any variable — ADR-0040; formula sheet: ideal gas law + combined gas law, dimensional homogeneity machine-checked — ADR-0039) |
| thermochemistry & calorimetry | 2, 3 | **2** | **lesson + gym + atlas** (`thermochemistry/methane-combustion-enthalpy` lesson: the extent ledger drives an **energy** — $q = \Delta H_\text{rxn}\cdot\xi$, with $\Delta H_\text{rxn}$ from **Hess's law** over sourced $\Delta H_f^\circ$ (`data/formation-enthalpies.toml`, OpenStax Appendix G); `build.py`'s fourth reported-product shape (`result.energy`), the first fully molecular lesson, triple-badged (machine-checked ξ + data-sourced ΔH_f° + model-assumed Hess), $q$ exact — ADR-0043; `calorimetry_v1` gym: $q=mc\Delta T$, sourced specific heats, both honesty badges — ADR-0042; formula sheet: calorimetry $q=mc\Delta T$ + **Hess's law** $\Delta H_\text{rxn}=\sum\nu\Delta H_f^\circ$ (both sides molar energy, machine-checked — ADR-0043); `reaction-enthalpy` concept). Endothermic + multi-step Hess-cycle lessons follow |
| electronic structure & configurations | 3, 4 | 2+ | — |
| bonding (ionic/covalent, electronegativity) | 3, 4 | 2+ | **atlas + gym**: the Valence Table's bonding mode (sourced ΔEN classification + caution, ADR-0033) + per-bond ΔEN on every molecule Atlas entry (ADR-0044) |
| Lewis structures | 1, 4 | 2 | **lesson + atlas + gym** (molecule kind + `lewis_structures_v1` gym, ADR-0044; the `bonding/water-molecular-shape` **structure lesson**, ADR-0045): the **Lewis electron ledger** — valence total, octet/duet, formal charge, and their conservation — **machine-checked** (exact integer accounting, re-derived in pure Node); the lesson steps one molecule valence→Lewis→shape→polarity, the gym drills valence-total + electron-domain counting (free-entry). Octet exceptions + resonance deferred |
| VSEPR & polarity | 3, 4 | 2 | **lesson + atlas + gym** (molecule kind + `lewis_structures_v1`, ADR-0044; the water structure lesson, ADR-0045): VSEPR geometry from the sourced `data/vsepr.toml` table keyed on the machine-derived electron-domain count; the gym drills molecular-shape (categorical, electron-domain-geometry distractor); the lesson refutes "water is linear" from the verified geometry. Molecular polarity authored + disclosed (model-assumed, neutral molecules) |
| intermolecular forces (IMFs) | 3, 4 | 2 | **atlas + concept** (ADR-0046): `structure.classify_imf` derives the **dominant IMF** (London dispersion / dipole–dipole / hydrogen bonding) of a neutral molecule from its polarity + an **exact H-on-N/O/F test** — the presence machine-derived, the strength ranking the sourced rule; the `molecule` Atlas `intermolecular` block + a sourced normal boiling point (`data/boiling-points.toml`) as evidence + the `intermolecular-forces` concept. A comparison **lesson** (multi-molecule shape) deferred |
| intermolecular forces | 4 | 2+ | — |
| kinetics (rate laws, half-life) | 2, 3 | **3** | **lessons + atlas** (ADR-0049 — the tier; topic slug `kinetics`): the species ledger with the extent evolving in **time**, one lesson kind over **orders 0/1/2** — the order fixes the integrated law and the half-life behavior, the contrast that makes "constant $t_{1/2}$" mean something. **First** `hydrogen-peroxide-decomposition` — $[\mathrm{A}]=[\mathrm{A}]_0 e^{-kt}$ (2 H₂O₂ → 2 H₂O + O₂, $k=3.21\times10^{-5}\ \mathrm{s^{-1}}$) → $t_{1/2}=\ln2/k=6.00$ h **constant** (halving on the same clock 1.000→0.500→0.250→0.125 M; the shrinking-$t_{1/2}$ misconception refuted). **Second** `butadiene-dimerization` — $1/[\mathrm{A}]=1/[\mathrm{A}]_0+kt$ (2 C₄H₆ → C₈H₁₂, $k=5.76\times10^{-2}\ \mathrm{M^{-1}min^{-1}}$) → $t_{1/2}=1/k[\mathrm{A}]_0$ **grows** 1.45→2.89→5.79 h. **Zero** `ammonia-decomposition` — $[\mathrm{A}]=[\mathrm{A}]_0-kt$ (2 NH₃ → N₂ + 3 H₂ on hot W, $k=1.3\times10^{-6}\ \mathrm{M\,s^{-1}}$) → $t_{1/2}=[\mathrm{A}]_0/2k$ **shrinks** 1.07→0.534→0.267 h, reaching **exactly 0 at 2.14 h**. All OpenStax §12.4. `chemkernel.kinetics` dispatches on order (Decimal exp/ln, model-exact-then-rounded; $k$ in native units, sourced `data/rate-constants.toml`); the rate law + order are model-assumed (the order is measured, not the coefficient); the gate re-derives the balance + curve + the order's $t_{1/2}$ + the halving landmarks + the successive-half-life progression. A **build-time decay-curve SVG** per lesson. The tier's **`kinetics_v1` gym** drills the three orders too (apply the integrated law for [A](t) · apply the order's t½ · **read the order** off three successive half-lives), the gate re-deriving each per order in pure Node. Concepts `reaction-rate` / `rate-law` / `integrated-rate-law` / `half-life`. **Next:** Arrhenius |
| equilibrium (ICE, K, Q) | 2, 3 | **2** | **lesson + atlas** (`equilibrium/acetic-acid-ph`: the **ICE table = the species ledger** with the extent solved from **mass action** $Q(x)=K$, not a limiting reagent — `chemkernel.equilibrium.solve_equilibrium` finds the root by bisection to high precision, model-exact-then-rounded, the machine-check the residual $Q(\text{committed})=K$ + the gate's independent re-solve; a new `equilibrium` lesson kind, now **six subtypes** — weak-acid / buffer / weak-base / solubility (+ common-ion) / polyprotic / titration — plus a **`prediction` kind** (Q vs Kₛₚ — a snapshot, not a solve), ADR-0048; the `chemical-equilibrium` + `reaction-quotient` concepts). Q-vs-Kₛₚ precipitation prediction landed (9th increment, both verdicts). The **Kₐ/K_b/K_w/K_sp formula-sheet entries** landed as **dimensionless activity relations** (10th increment; $a_X=[X]/c^\circ$ against the 1 M standard state → K is dimensionless, fitting the monomial dimension engine; pH/pOH stay in the `ph` concept) |
| acids & bases (pH, strength) | 2, 3 | **3** | **3 lessons + gym + atlas** (the `weak_acid_ph_v1` gym drills the pH of a weak acid — the mass-action root, model-exact-then-rounded + Kₐ-sourced, the gate re-solving in pure Node — ADR-0048. `equilibrium/acetic-acid-ph`: a weak **acid**'s pH — $\mathrm{pH}=-\log_{10}[\mathrm{H^+}]$, weak-vs-strong, % ionization; $K_a$ from `data/ionization-constants.toml` OpenStax App H, the HA ⇌ H⁺ + A⁻ dissociation DRY-sourced from `data/acids-bases.toml`. `equilibrium/ammonia-ph`: a weak **base**'s pH — B + H₂O ⇌ BH⁺ + OH⁻ with water the **pure solvent excluded from Q**, → [OH⁻] → pOH → **pH via the $K_w$ bridge** $[\mathrm{H^+}][\mathrm{OH^-}]=K_w$; the exact mirror of acetic acid (pOH 2.88 ↔ pH 11.12); $K_b$/$K_w$ from `data/ionization-constants.toml` App I/§14.1, proton accounting machine-checked on load; the `water-autoionization` concept. `equilibrium/phosphoric-acid-ph`: a **polyprotic** acid — H₃PO₄ ionizing in stages $K_{a1}\gg K_{a2}\gg K_{a3}$, the solver run once per stage on the previous stage's equilibrium (the successive treatment) → pH 1.62; [H⁺] tracks stage 1 and the amphiprotic $[\mathrm{HPO_4^{2-}}]\approx K_{a2}$ fall out of the solve; staged Kₐ from `data/ionization-constants.toml` `[polyprotic]` App H, composition machine-checked on load; the `polyprotic-acid` concept. All triple-badged; the `ph` concept — ADR-0048) |
| buffers & titrations | 2, 3 | **2** | **2 lessons + atlas** (`equilibrium/acetate-buffer`: a weak acid + its conjugate base together — the **common-ion effect** (the pre-loaded A⁻ suppresses ionization, Le Chatelier) + **Henderson–Hasselbalch** pH = pKₐ + log([A⁻]/[HA]), machine-checked as the mass-action law in log form on the equilibrium concentrations; 0.100 M acetic acid + 0.100 M acetate → pH 4.74 = pKₐ, 74× less ionized than the acid alone (re-solved for the contrast); the common-ion-oversight misconception refuted; the `buffer` concept — ADR-0048, 4th increment, the `buffer` subtype). `equilibrium/acetic-acid-titration`: a **titration curve** — a weak acid (acetic) titrated by a strong base (NaOH), the ledger *marched* by region (weak acid/buffer → the conjugate base's weak-base solve at equivalence → excess base), each point reusing `solve_equilibrium`; the player draws a **build-time SVG** of the verified points — initial pH 2.88, pH = pKₐ = 4.74 at half-equivalence, basic at equivalence (pH 8.72); the pH-7-at-equivalence misconception refuted; the `titration` concept — ADR-0048, 8th increment, the `titration` subtype) |
| solubility equilibria (Ksp) | 2, 3 | **4** | **lesson + atlas** (`equilibrium/calcium-fluoride-solubility`: the SAME reversible-extent solver as the weak acid, but the dissolving species is a **pure solid** excluded from $Q$ — $K_{sp} = [\mathrm{Ca^{2+}}][\mathrm{F^-}]^2 = 4s^3$, a **cubic**, solved by bisection for the molar solubility $s$; the coefficient-forgetting misconception ($s=\sqrt{K_{sp}}$) refuted; $K_{sp}$ from `data/solubility-products.toml` OpenStax App J, the ion composition machine-checked by charge crossover; the `equilibrium` lesson kind's **solubility** subtype + the `solubility-product` concept — ADR-0048, 2nd increment). A **common-ion variant** `equilibrium/calcium-fluoride-common-ion` (CaF₂ into 0.10 M F⁻ — a shared ion **pre-loaded**, the buffer's nonzero-initial-product case now on the cubic, the solver reused unchanged; dissolution suppressed **59 400×** to $3.45\times10^{-9}$ M; the fixed-solubility misconception refuted; the `common-ion-effect` concept — ADR-0048 6th increment). And a **prediction** face `equilibrium/calcium-fluoride-precipitation` (the third view of CaF₂ — dissolve/suppress/**predict**): mix Ca(NO₃)₂ + NaF, dilute each ion into the combined volume, compute the **reaction quotient** $Q=[\mathrm{Ca^{2+}}][\mathrm{F^-}]^2$ and compare to $K_{sp}$ → $Q=1.44\times10^{-7}\gg K_{sp}$ (≈4170×) → **a precipitate forms**; a snapshot, not a solve — the new `prediction` lesson kind + the `reaction-quotient` concept — ADR-0048 9th increment. A second prediction `equilibrium/magnesium-hydroxide-no-precipitate` shows the **other verdict**: dilute Mg(NO₃)₂ + NaOH give $Q = 1.25\times10^{-13} < K_{sp} = 5.61\times10^{-12}$ (≈45× below) → **stays clear** — so "insoluble" is quantitative, not a yes/no label; the 2nd curated Ksp salt (Mg(OH)₂) + a polyatomic-ion (OH⁻) source. Quantifies the Phase-0 precipitation rules |
| redox & oxidation states | 1, 3 | **2** | **engine + concepts** (ADR-0050): `chemkernel.redox.oxidation_states` assigns each atom its oxidation number by the first-course rule hierarchy (free element 0; monatomic ion = charge; F −1, group-1 +1, group-2 +2, H +1, O −2) + **solves the one remaining element by the sum-to-charge constraint** — machine-checked, and it refuses a formula it cannot resolve to one unknown (SO₄²⁻→S+6, MnO₄⁻→Mn+7 work; SiC refused). This **completes the free-element redox flag** of ADR-0035 (which detected redox *without* numbers). Concepts `redox` (the gateway) + `oxidation-number`. The rules are a sourced convention (regime-3); the sum accounting is exact (regime-1) |
| electrochemistry (cells, E°, Nernst) | 1, 2, 3 | **1** | **lesson + atlas** (ADR-0050 — the tier opener; topic slug `electrochemistry`): `electrochemistry/daniell-cell` — the species ledger with **electrons** as the tracked quantity (*electron bookkeeping plus free energy per charge*). Zn + Cu²⁺ → Zn²⁺ + Cu: oxidation numbers (Zn 0→+2 oxidized, Cu +2→0 reduced) → the two half-reactions → the electron ledger (**n = 2** e⁻, lost = gained) → **E°cell = E°(cathode) − E°(anode) = 1.10 V** → **ΔG° = −nFE° = −212 kJ/mol**, spontaneous. `build_electrochemistry_lesson` takes two sourced couples, assigns cathode (higher E°)/anode, and computes the cell; E° from `data/reduction-potentials.toml` (Appendix L), F exact. Triple-badged (electron ledger + oxidation numbers machine-checked / E° sourced / cell model assumed); the gate re-derives the whole spine in Node. Concepts `half-reaction` / `electron-ledger` / `cell-potential` / `standard-reduction-potential`. **Next:** the Nernst equation, more cells |
| organic lens (electron movement, functional-group preview) | 4 | 2+ | — |

Notes:

- Regime assignments are the *span* a topic touches; individual claims within a lesson get per-facet
  regime tags and per-claim badges in the solution object (architecture §solution-object).
- The organic lens is explanatory only — no synthesis content (ADR-0007, ROADMAP out-of-scope).
- Physical-chemistry seeds (extent, free energy, entropy-as-dispersal) appear inside host topics, not as
  standalone rows, until the owner opens a dedicated track.
